-- ============================================================
-- 車両に EV フラグを追加。EV 車は提出フォームで走行距離(メーター)入力欄を出さない。
-- 冪等。
-- ============================================================

ALTER TABLE vehicles ADD COLUMN IF NOT EXISTS is_ev boolean NOT NULL DEFAULT false;
