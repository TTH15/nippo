# 公開済みコードのmain同期とアプリCI

2026/09/17、ユーザーの「順に進めましょう」に基づき、公開・基盤の第1項目を進める。

## 同期対象

- 着手時のGitHub main: `72842f1f0cc199e9d7fb415c8b2e0b3b73aa5fed`。
- 本番Vercel `nippo-ace` のコード: `aa40e2db14d3ad6c4d568f1f0c1f24f08dc6207c`。現行デプロイのreleaseCommitと照合済み。
- 同期する公開済み変更: `a8e0484`（会社境界・非公開ファイル・住所描画）、`b009d45`（Next.js/sharp）、`aa40e2d`（シフトメモの日別必要人数）。この履歴を維持する。
- 追加する変更: GitHub Actions、型/所属検査のnpm scripts、CI手順と公開状態の記録。アプリの実行コード・依存lockfile・DBは変更しない。
- 未公開の車両モデル/車検証読取/部位別色、シフトメモの正式反映、migration 164/165は含めない。元の作業ツリーを保持し、公開済みコミットから分けた作業ツリーで進める。

## 確認と適用手順

1. 本番デプロイ・GitHub main・VercelのGit連携を読み取り照合する。mainへのpushは既存の本番自動公開を起動し得るため、同期の基点を旧mainではなく本番コードに置く。
2. lockfileから依存を新規インストール。Web/プレビュー/モバイル型検査、Web/共有テスト、会社境界の静的検査、Node.js 24での本番ビルドを検証する。秘密値・本番DBを使わない。
3. 同期用ブランチをpushし、GitHub ActionsのApp CI成功を確認してからmainへfast-forwardする。直前のmainが想定から変わっていたら再照合し、force pushしない。
4. mainのコミット、App CI、既存Vercel連携の公開状態を確認する。公開後はログイン、未認証APIの拒否、公開済みシフトメモの配信を読み取り確認する。

## 現在の結果（送信承認待ち）

- 専用作業ツリー: `/private/tmp/hakotora-main-sync-20260917`、ブランチ `codex/main-sync-ci-20260917`、コミット `68e62bc`。
- 全126ファイル・1,184テスト、Web/プレビュー/モバイル型検査、48テーブルの会社境界検査、Node.js 24.21.0でのWeb本番ビルドが成功。CIのYAMLをパースし、差分の空白エラーも確認済み。
- 既存mainからは125ファイル・2,011行追加/1,490行削除。[送信対象一覧](main-sync-2026-09-17-files.txt)。公開済みaa40e2d以降の差分はCI・npm scripts・文書8ファイルのみ。アプリ実行コード・mobile・共有パッケージ・lockfile・migrationに追加変更がないことをGitで確認。
- 新たに追加される行を一般的な秘密キー・秘密鍵・DB接続文字列・JWTのパターンで走査し、検出0件。これは任意の秘密情報が存在しないことの完全な証明ではない。環境ファイル・ローカル接続設定・請求書保管庫は送信差分に追加しない。
- 自動承認審査が、公開リポジトリ `TTH15/nippo` への具体的なコード・CI・運用文書の送信に明示承認がないとしてpushを拒否。GitHubブランチ/mainと本番は未変更、GitHub上のApp CIは未実行。
- 次の操作は、このコミットを同リポジトリへ送信→同期用ブランチのApp CIを確認→mainへfast-forward→mainのCIとVercel公開状態を確認。CIの検査対象外は [アプリCI](../development/app-ci.md)。
