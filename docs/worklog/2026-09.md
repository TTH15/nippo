# 作業ログ 2026年9月

月別に分割した作業ログ。索引は [../worklog.md](../worklog.md)。

## 2026-09-01 00:00 日別配車PNGの文字下ずれ・欠けを修正

- 原因: html2canvas 1.4の文字計測は元documentへ置く非表示の1px画像に依存し、Tailwindの `img { display: block }` で基準位置が下がっていた。通常一覧では正常でも、日別PNGの氏名・コース札が下へずれて欠ける状態を再現。
- 変更: `captureDispatchImage.ts` の画像生成中だけ、非表示の計測画像へ `display: inline-block` を適用。成功・失敗とも一時CSSを撤去する。通常画面・プレート・行高・文字サイズ・共有や保存方法・半月PNG/PDFは変更しない。
- 検証: 本番ページを直接再利用する隔離プレビューを再ビルド。スマホ430px／320px・PC1440pxでPNG内の氏名、コース、契約区分、集合時刻、段分け、未割当、2枚目を目視・実操作確認。320pxの横はみ出しなし。画像生成・ダイアログ・シフトページの11テスト、TypeScript、差分の空白チェック成功。一時CSSの適用範囲と成功／失敗時の後始末を回帰テストへ追加。
- 状態: `http://127.0.0.1:3193/preview/shifts` の画像プレビューを開いた状態で維持。架空データ・本番DB/API/通知から隔離。写真ライブラリへの保存は実機未確認、commit／push／デプロイ未実施。

## 2026-09-01 00:04 画像・PDFエクスポートの共通知見を記録

- 依頼: 画像／PDF出力は今後も使うため、シフト表・シフトメモと今回の日別配車の対処を一般的な画像化処理の資料へまとめる。
- 変更: `docs/development/image-pdf-export.md` に、帳票データの共通描画・補正済みPNGのPDF埋込み・DOM複製の使い分け、実装参照先、html2canvasの文字計測とCSS干渉、既存オフセットとの二重補正防止、フォント・SVG・範囲・分割・保存・実ファイルの検証手順を記録。AGENTS.md、design.md、プレビュー手順から参照を追加。
- 検証・範囲: 現行ソースと過去の該当作業ログを照合し、資料の相対リンク・差分の空白を確認。今回は文書のみの変更で、出力コードの共通関数化・追加補正・UI変更・デプロイは行わない。前の修正で起動した隔離プレビューは維持し、アプリのテストや実機保存確認は今回再実施していない。

## 2026-09-01 00:17 シフトメモ・シフト表のPDFを画質維持で軽量化

- 原因・変更: jsPDFの圧縮なし設定により、シフトメモのPNGが展開済み画素のままPDFへ格納されていた。`pdfExport.ts` に圧縮オプションと `pngToPdf` を追加し、本番・旧プレビューのシフトメモ、シフト表のベクターPDFへ適用。PNG・文字位置・ページ寸法を変えず可逆圧縮する。メモのPDFボタンに容量を表示し、1MB以上は範囲を絞る案内を追加。
- 実測: 架空データの9月前半15日で、メモ12枠25,762,755 B→671,220 B（画面672 KB）、40枠75,813,317 B→2,092,668 B、シフト表12人412,925 B→109,148 B。12／40枠のPDF埋込み画像と元PNGは全画素一致。12枠メモと12人シフト表はPopplerで描画した圧縮前後のPDFも全画素一致。通常は数百KBが目安で、大きな表の厳密な上限保証や低画質化は行わない。
- プレビュー: 本番ページをそのまま使う3193番に「メモ12枠／40枠のサンプル」を追加。プレビュー専用ユーザーの8月・9月の架空メモだけをローカル保存し、API／DB／本番認証から隔離。PC1440px・スマホ390／320pxで容量表示・PDF保存の開始・横はみ出しなし、40枠の容量案内を確認し、12枠のプレビューを残した。
- 検証・記録: 実jsPDFの圧縮回帰テストを含む関連5ファイル29テスト、型チェック、本番ページと旧メモプレビューの独立ビルド、文書リンクと差分の空白チェック成功。通常PDFを数百KBにする方針と実測を共通資料・AGENTSへ追記。端末実機の保存先／共有先は未確認。commit／push／デプロイ未実施。

## 2026-09-01 00:22 PDFボタンの容量表示・範囲縮小案内を撤去

- 依頼・変更: ユーザーから不要との指摘を受け、本番・旧プレビューのシフトメモからPDFボタンの容量表示と1MB超の範囲縮小案内を削除。容量用のstate・表示関数も撤去し、従来の「PDF」ボタンへ戻した。画質を変えない圧縮・PNG・範囲選択・保存方法は維持する。
- 記録: AGENTSと画像／PDF共通資料・プレビュー手順を修正。容量削減は出力処理側で行い、容量表示や範囲縮小を促す案内は追加しない方針を明記。直前の00:17の実装記録は履歴として残す。
- 検証: PDF圧縮の既存4テスト、型チェック、本番ページと旧メモプレビューの独立ビルドに成功。本番コードを使う隔離プレビューで12枠・40枠、PC1440px・スマホ390／320pxを操作し、容量なしのPDFボタン・案内なし・保存開始・横はみ出しなしを確認。圧縮処理は変更しておらず、PDF全画素比較は前項の結果を維持し今回は再実施していない。
- 状態: `http://127.0.0.1:3193/preview/shifts` の12枠出力プレビューを開いて維持。架空データ・本番DB／API／通知から隔離。スマホ実機の保存先／共有先は未確認、commit／push／デプロイ未実施。

## 2026-09-01 00:23 軽配送の現場に合わせた画面文言を共通方針へ記録

- 依頼: 利用者は軽配送業者なので、細かな容量や小難しい説明は画面に不要。
- 変更: AGENTS・design・デザインシステムへ、現場で使う短い言葉で操作を伝え、業務に不要な技術説明を表示しない方針を記録。必要な注意は影響と対処を簡潔に伝える。画像／PDFの共通資料にも、容量・圧縮の詳細は開発者向けであることを明記。
- 範囲・確認: 文書のみの変更。先の容量表示・案内の撤去を維持し、アプリの追加変更・再ビルド・ブラウザ再検証・デプロイは行わない。文書の参照先と差分の空白を確認。

## 2026-09-01 00:30 オイル交換の補足が固定列の下に隠れる問題を修正

- 原因: `VehiclePlate` の補足が親セルの `group-hover` に反応し、マーク以外へのホバーでも表示。プレート内の描画階層に閉じ込められ、左の氏名固定列に文頭が隠れていた。実際の本番コードの隔離プレビューで再現を確認。
- 変更: 共通 `Tooltip.tsx` を追加して補足をbodyの前面へ描画し、画面端では位置を調整。開く対象を警告マーク自身のホバー／フォーカスに限定し、離脱・クリック・Escape・マウス表示中のスクロールで閉じる。キーボード移動の自動スクロールには追従する。文言は「オイル交換が必要です」「もうすぐオイル交換です」に短縮。警告の判定・色・プレート・配車編集は維持。
- 検証: PC1440pxで固定列に隠れないこと・セル全体では開かないこと・クリックで従来の編集が開くこと、スマホ390／320pxで横はみ出しなし・画面下端の上向き表示・スクロール時の消去を確認。キーボードのフォーカス移動とEscapeもブラウザで確認。関連3ファイル14テスト、型チェック、プレビュー独立ビルド、差分の空白チェック成功。タッチのホバー抑止は自動テストのみで実機未確認。
- 状態・記録: デザインシステムとプレビュー手順に共通部品と確認方法を記録。既存の架空データを使う `http://127.0.0.1:3193/preview/shifts` を開いて維持。本番DB／API／通知から隔離し、画像出力の実ファイルは今回再生成していない。commit／push／デプロイ未実施。

## 2026-09-01 00:53 貸出通知の本番未接続を確認し、車両移動と時刻指定通知を試作

- 依頼: ユーザーは直近の修正をデプロイ済みと報告。月額紐付け車を他の利用者に貸す通知が見当たらないため現状を確認し、前日の指定時刻で通知したい。また、9/3豊中→9/4京都のような個々の移動を管理したい。
- 調査: 開始時HEAD `e9c08ad`、作業差分なし。給油・貸出・受け渡しの確認は旧プレビュー `MonthlyVehicleDialog` に残り、本番シフトの配車操作／APIには未接続。直近の表示修正で削除したものではなかった。既存の翌日配車通知は別経路で、毎時実行・時単位判定のため分単位の予約には別途対応が必要。本番DB・稼働環境・実送信は照合していない。
- 試作: 本番シフトページを直接使う隔離プレビューへ「車両移動を試す」を追加。架空の1201について豊中→京都、京都→豊中、同じ車庫での受け渡しを個別に管理。担当者・出発地・届け先・期限・前日指定時刻／個別日時・給油の宛先・鍵の連絡、宛先別文面を用意。共通EditorModal・VehiclePlate・選択・日時・CheckboxField・SmoothCollapse・ConfirmDialogを再利用した。
- 状態: 保存、通知予約／送信／失敗、実際の到着を分離。保存失敗の入力保持、通知再予約、取消時の古い予約解除、送信後の変更・取消連絡、役割重複の文面統合、未保存保護、完了時の実際の場所を試せる。実通知は送らず、確認用時計で動作させる。
- 検証: 状態遷移7件＋本番シフトページ4件の11テスト、TypeScript、独立ビルド、差分チェック成功。PC1440px／スマホ390px・320pxで担当選択・19:30への変更・給油と文面・保存再表示・未保存保護・保存／通知失敗からの復帰・取消・完了・絞り込みを確認。320pxでページ幅／scrollWidthとも320px、ダイアログは288pxで横はみ出しなし。
- 残課題・記録: `docs/design/vehicle-handoffs-and-notifications.md` に運用案・本番接続条件、プレビュー手順と本番反映調査に境界を記録。今回は固定サンプル・画面内保存のみ。実配車選択からの確認、任意の配車変更からの自動追従、会社共通の通知初期設定、同日複数便、永続保存・予約ワーカー・実通知・地図更新は未実装。`http://127.0.0.1:3193/preview/shifts` を開いて維持。本番コード・API・DB・料金は未変更、commit／push／デプロイ未実施。

## 2026-09-01 01:04 駐車場所・返却予定をシフトへ反映するUI計画と画面画像を作成

- 依頼: 車がどこに停まっていて、どこへ返却されるかをシフト画面などへ表示するUIを計画し、画面画像を生成する。
- 方針: 通常のドライバー別シフトに返却先を1行追加。車両軸では車ごとの日付・利用者・受取／返却先と選んだ車の移動を表示。詳細は最後の駐車記録・次の受け渡し・返却予定を分け、停めた場所の記録からのみ実績を更新する。PCは表の横、スマホは詳細画面へ展開し、地図は必要時だけ開く。
- 成果物: `docs/design/shift-vehicle-location-ui.md` に表示・操作・未設定／失敗・既存部品・段階的実装の計画を記録。内蔵image_genでPC・スマホ（一覧／詳細の2状態）を生成し、`docs/design/assets/shift-vehicle-location/desktop-v1.png` と `mobile-v1.png` に保存。プロンプト一式は同階層の `shift-vehicle-location-ui-prompts.md` に記録した。
- 確認: 既存の本番ページを再利用する3193番の隔離プレビューをPC1440×1000・スマホ390×844で確認し、参照画像として使用。生成後に車両軸では不要な契約フィルターを検索／要確認へ差し替え、鍵の文言も修正。日本語、駐車と予定の区切り、画像寸法（PC1586×992／スマホ1300×1210）、ファイル保存・差分チェックを確認した。
- 状態: 今回はUI計画・静止画の依頼範囲。新しい車両軸・返却先表示・駐車記録は未実装で、新UIの実操作・実機・保存の検証は未実施。前回の操作プレビュー・本番コード・API・DB・通知は変更なし。3193番を通常幅に戻して維持。commit／push／デプロイ未実施。

## 2026-09-01 01:10 日報を入口にする駐車記録とQR・端末位置への共通基盤を設計

- 方針: ユーザーは、当面は日報と一緒に駐車場所を送信し、将来はモバイルの車両QR＋端末位置情報で地図へ記録する意向。日報を最初の入力窓口、車両ごとの駐車履歴を共通の保存先とする実装計画を作成した。
- 調査: WebのSubmitPageClientV2とmobileのDailyReportFormは同じ `/api/reports/v2` を使用。既存の地点map_places・区画parking_slots・位置履歴vehicle_positions、QRトークン解決・会社認可、モバイルの位置取得と業務終了後の日報送信を確認。日報のコース別個別保存と打刻位置のベストエフォート保存は、そのままでは駐車も含めた保存成功の保証にならない。
- 設計: 登録車庫選択・別の場所・まだ使用中／引渡し／あとで記録を用意し、駐車日時と提出日時を分離。位置履歴に駐車申告の種別・場所・取得方法・関連日報を追加する方針。日報と一括保存、再送の重複防止、古い日報の再提出・訂正、座標なし、複数コース／乗換え、日報なしの単独記録を整理。日報承認・返却予定・打刻・GPS観測から駐車済みを推測しない。
- 記録: `docs/design/daily-report-parking-foundation.md` を追加。旧駐車設計の未作成テーブルとQRの車両ID直書き、地図設計の起票時点、車両セッション設計の駐車先を現行計画へ接続。シフトUI計画・移動通知計画・プレビュー手順も日報起点へ更新した。
- 検証・範囲: 対象コード・migration・既存資料の静的照合、差分の空白チェック。文書のみの変更で、新機能のテスト・実機確認、API／DB／日報UI／プレビューの追加は未実施。本番データ・実通知・公開を変更せず、migration作成・適用、commit／push／デプロイも行っていない。

## 2026-09-01 12:53 シフト表示パネルの速度・契約操作・余白を微調整

- 依頼: 「表示」を押したときの開閉がまだ遅い。独立した契約区分の選択欄は上下余白が大きく、使用頻度も低いため「表示」へまとめたい。以前のラベルによる表示対象切替の状況も確認したい。
- 変更: 本番シフトページの契約区分選択・段分け・判定日を `ShiftDisplayOptions` の2段目へ移し、独立カードを撤去。パネルの上下余白と段間を縮小し、PCの選択・段分けを44pxから36pxへ変更、スマホは44pxを維持した。`ShiftDisplayPanel` は共通 `SmoothCollapse` のquick指定で140msにし、通常の250msは他画面へ残した。
- ラベルの現状: 複数ラベルのOR絞り込みと「ラベル・契約」表示は旧 `/preview/driver-leases` の架空データだけにあり、本番ページへは契約区分だけが移植されていた。会社別ラベルの保存・管理・認可は本番反映計画の段階3で未実装。今回の微修正では架空ラベルを本番用APIへ混ぜず、未移植であることを正本へ明記した。
- 検証: 対象3ファイル6テストに続けてWeb全103ファイル968テスト、TypeScript、3194番の独立プレビュービルド、差分の空白チェックに成功。アプリ内ブラウザのPC1440×1000でパネル115px・契約操作36px・段間8px、スマホ390×844でパネル181px・操作44px・ページ幅／scrollWidth 390pxを確認。契約操作が閉じた状態では表示されず、開いて絞り込み・段分け・表示項目を操作できる。本番DB・API・通知・migrationは変更せず、commit／push／デプロイ未実施。

## 2026-09-01 17:37 日別配車画像を1枚優先にし、行順と日付表示を整理

- 変更: 日別配車PNGは12人まで1枚、超える場合は最小枚数で契約区分／コースの境界を優先し、1人だけの次ページを避ける方式へ変更。画像の日付を中央の `YYYY/MM/DD（曜）` のみにし、「日別配車」と端末内生成・写真保存方法の補足文を撤去した。利用者向けのハイフン日付禁止をAGENTS・design・デザインシステムへ記録した。
- 行順: 「表示」内に共通SortableListを使う「行の並び順」を追加。手動ドラッグに加え、契約区分／選択日のコースでまとめる下書きを用意。シフト専用の `shift_sort_order` と保存API・migration 156を追加し、名簿No.や請求・日報の順序は変えない。半月表・日別一覧・日別PNGで同じ順を使う。
- 検証: 対象5ファイル57テスト、更新した説明・分割の11テスト、Web全105ファイル979テスト、TypeScript、4503モジュールの隔離プレビュービルド、空白チェックに成功。PC1440×1000／スマホ390×844で並べ替え・保存反映・横はみ出しなしを確認。稼働11人は1224×2763px、全員12人は1224×2895pxの各1枚。画像の日付中央・見出し／補足文の撤去、ブラウザ警告・エラーなしを確認した。
- 残課題: migration 156のDB適用、commit／push／デプロイは未実施。13人以上の実画面サンプルとiPhone／Android実機の共有・写真保存は未確認。会社別ラベルの保存・管理・認可と本番シフトのラベル絞り込みは従来どおり未実装。

## 2026-09-01 17:56 車両一覧を登録順にし、新規追加の入力エラーを具体化

- 原因・順序: 車両APIと画面がメーカー→車種→UUIDで並べていたため、同一車種では利用者からランダムに見えていた。APIと画面を登録日時→IDの安定順へ変更し、従来の稼働中→使用不可→廃車の状態分けを維持。画面へ「各登録順」と明記した。
- 入力改善: 新規追加で必須なのは車種だけと明示。空欄保存ではモーダル内の一覧・赤枠・項目下メッセージを表示し、該当タブと項目へ移動する。任意のメーター空欄は0km、交換間隔は3,000kmへ正規化し、DBのNOT NULL違反による500を防止。数値は0以上の整数としてクライアント／API双方で検証し、サーバー失敗の長文と内部エラー詳細は短い再試行案内へ置き換えた。
- プレビュー・検証: 本番車両ページを直接使い、API・DB・3Dモデルを架空データへ差し替える `/preview/vehicles` を追加。IDと登録日時を逆転した同一車種、未入力、正常保存、模擬失敗後の入力保持をPC1440×1000／スマホ390×844で確認。Web全106ファイル985テスト、TypeScript、本番ビルド、車両プレビュー独立ビルド、差分の空白チェックに成功。
- 残課題: commit／push／デプロイは未実施。追加migrationは不要。本番データ・実APIへの保存確認は未実施で、プレビューは `http://127.0.0.1:3195/preview/vehicles` を開いて維持している。
