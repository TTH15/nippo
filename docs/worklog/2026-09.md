# 作業ログ 2026年9月

月別に分割した作業ログ。索引は [../worklog.md](../worklog.md)。

## 2026-09-01 00:00 日別配車PNGの文字下ずれ・欠けを修正

- 原因: html2canvas 1.4の文字計測は元documentへ置く非表示の1px画像に依存し、Tailwindの `img { display: block }` で基準位置が下がっていた。通常一覧では正常でも、日別PNGの氏名・コース札が下へずれて欠ける状態を再現。
- 変更: `captureDispatchImage.ts` の画像生成中だけ、非表示の計測画像へ `display: inline-block` を適用。成功・失敗とも一時CSSを撤去する。通常画面・プレート・行高・文字サイズ・共有や保存方法・半月PNG/PDFは変更しない。
- 検証: 本番ページを直接再利用する隔離プレビューを再ビルド。スマホ430px／320px・PC1440pxでPNG内の氏名、コース、契約区分、集合時刻、段分け、未割当、2枚目を目視・実操作確認。320pxの横はみ出しなし。画像生成・ダイアログ・シフトページの11テスト、TypeScript、差分の空白チェック成功。一時CSSの適用範囲と成功／失敗時の後始末を回帰テストへ追加。
- 状態: `http://127.0.0.1:3193/preview/shifts` の画像プレビューを開いた状態で維持。架空データ・本番DB/API/通知から隔離。写真ライブラリへの保存は実機未確認、commit／push／デプロイ未実施。

## 2026-09-01 00:04 画像・PDFエクスポートの共通知見を記録

- 依頼: 画像／PDF出力は今後も使うため、シフト表・シフトメモと今回の日別配車の対処を一般的な画像化処理の資料へまとめる。
- 変更: `docs/development/image-pdf-export.md` に、帳票データの共通描画・補正済みPNGのPDF埋込み・DOM複製の使い分け、実装参照先、html2canvasの文字計測とCSS干渉、既存オフセットとの二重補正防止、フォント・SVG・範囲・分割・保存・実ファイルの検証手順を記録。AGENTS.md、design.md、プレビュー手順から参照を追加。
- 検証・範囲: 現行ソースと過去の該当作業ログを照合し、資料の相対リンク・差分の空白を確認。今回は文書のみの変更で、出力コードの共通関数化・追加補正・UI変更・デプロイは行わない。前の修正で起動した隔離プレビューは維持し、アプリのテストや実機保存確認は今回再実施していない。

## 2026-09-01 00:17 シフトメモ・シフト表のPDFを画質維持で軽量化

- 原因・変更: jsPDFの圧縮なし設定により、シフトメモのPNGが展開済み画素のままPDFへ格納されていた。`pdfExport.ts` に圧縮オプションと `pngToPdf` を追加し、本番・旧プレビューのシフトメモ、シフト表のベクターPDFへ適用。PNG・文字位置・ページ寸法を変えず可逆圧縮する。メモのPDFボタンに容量を表示し、1MB以上は範囲を絞る案内を追加。
- 実測: 架空データの9月前半15日で、メモ12枠25,762,755 B→671,220 B（画面672 KB）、40枠75,813,317 B→2,092,668 B、シフト表12人412,925 B→109,148 B。12／40枠のPDF埋込み画像と元PNGは全画素一致。12枠メモと12人シフト表はPopplerで描画した圧縮前後のPDFも全画素一致。通常は数百KBが目安で、大きな表の厳密な上限保証や低画質化は行わない。
- プレビュー: 本番ページをそのまま使う3193番に「メモ12枠／40枠のサンプル」を追加。プレビュー専用ユーザーの8月・9月の架空メモだけをローカル保存し、API／DB／本番認証から隔離。PC1440px・スマホ390／320pxで容量表示・PDF保存の開始・横はみ出しなし、40枠の容量案内を確認し、12枠のプレビューを残した。
- 検証・記録: 実jsPDFの圧縮回帰テストを含む関連5ファイル29テスト、型チェック、本番ページと旧メモプレビューの独立ビルド、文書リンクと差分の空白チェック成功。通常PDFを数百KBにする方針と実測を共通資料・AGENTSへ追記。端末実機の保存先／共有先は未確認。commit／push／デプロイ未実施。

## 2026-09-01 00:22 PDFボタンの容量表示・範囲縮小案内を撤去

- 依頼・変更: ユーザーから不要との指摘を受け、本番・旧プレビューのシフトメモからPDFボタンの容量表示と1MB超の範囲縮小案内を削除。容量用のstate・表示関数も撤去し、従来の「PDF」ボタンへ戻した。画質を変えない圧縮・PNG・範囲選択・保存方法は維持する。
- 記録: AGENTSと画像／PDF共通資料・プレビュー手順を修正。容量削減は出力処理側で行い、容量表示や範囲縮小を促す案内は追加しない方針を明記。直前の00:17の実装記録は履歴として残す。
- 検証: PDF圧縮の既存4テスト、型チェック、本番ページと旧メモプレビューの独立ビルドに成功。本番コードを使う隔離プレビューで12枠・40枠、PC1440px・スマホ390／320pxを操作し、容量なしのPDFボタン・案内なし・保存開始・横はみ出しなしを確認。圧縮処理は変更しておらず、PDF全画素比較は前項の結果を維持し今回は再実施していない。
- 状態: `http://127.0.0.1:3193/preview/shifts` の12枠出力プレビューを開いて維持。架空データ・本番DB／API／通知から隔離。スマホ実機の保存先／共有先は未確認、commit／push／デプロイ未実施。

## 2026-09-01 00:23 軽配送の現場に合わせた画面文言を共通方針へ記録

- 依頼: 利用者は軽配送業者なので、細かな容量や小難しい説明は画面に不要。
- 変更: AGENTS・design・デザインシステムへ、現場で使う短い言葉で操作を伝え、業務に不要な技術説明を表示しない方針を記録。必要な注意は影響と対処を簡潔に伝える。画像／PDFの共通資料にも、容量・圧縮の詳細は開発者向けであることを明記。
- 範囲・確認: 文書のみの変更。先の容量表示・案内の撤去を維持し、アプリの追加変更・再ビルド・ブラウザ再検証・デプロイは行わない。文書の参照先と差分の空白を確認。

## 2026-09-01 00:30 オイル交換の補足が固定列の下に隠れる問題を修正

- 原因: `VehiclePlate` の補足が親セルの `group-hover` に反応し、マーク以外へのホバーでも表示。プレート内の描画階層に閉じ込められ、左の氏名固定列に文頭が隠れていた。実際の本番コードの隔離プレビューで再現を確認。
- 変更: 共通 `Tooltip.tsx` を追加して補足をbodyの前面へ描画し、画面端では位置を調整。開く対象を警告マーク自身のホバー／フォーカスに限定し、離脱・クリック・Escape・マウス表示中のスクロールで閉じる。キーボード移動の自動スクロールには追従する。文言は「オイル交換が必要です」「もうすぐオイル交換です」に短縮。警告の判定・色・プレート・配車編集は維持。
- 検証: PC1440pxで固定列に隠れないこと・セル全体では開かないこと・クリックで従来の編集が開くこと、スマホ390／320pxで横はみ出しなし・画面下端の上向き表示・スクロール時の消去を確認。キーボードのフォーカス移動とEscapeもブラウザで確認。関連3ファイル14テスト、型チェック、プレビュー独立ビルド、差分の空白チェック成功。タッチのホバー抑止は自動テストのみで実機未確認。
- 状態・記録: デザインシステムとプレビュー手順に共通部品と確認方法を記録。既存の架空データを使う `http://127.0.0.1:3193/preview/shifts` を開いて維持。本番DB／API／通知から隔離し、画像出力の実ファイルは今回再生成していない。commit／push／デプロイ未実施。

## 2026-09-01 00:53 貸出通知の本番未接続を確認し、車両移動と時刻指定通知を試作

- 依頼: ユーザーは直近の修正をデプロイ済みと報告。月額紐付け車を他の利用者に貸す通知が見当たらないため現状を確認し、前日の指定時刻で通知したい。また、9/3豊中→9/4京都のような個々の移動を管理したい。
- 調査: 開始時HEAD `e9c08ad`、作業差分なし。給油・貸出・受け渡しの確認は旧プレビュー `MonthlyVehicleDialog` に残り、本番シフトの配車操作／APIには未接続。直近の表示修正で削除したものではなかった。既存の翌日配車通知は別経路で、毎時実行・時単位判定のため分単位の予約には別途対応が必要。本番DB・稼働環境・実送信は照合していない。
- 試作: 本番シフトページを直接使う隔離プレビューへ「車両移動を試す」を追加。架空の1201について豊中→京都、京都→豊中、同じ車庫での受け渡しを個別に管理。担当者・出発地・届け先・期限・前日指定時刻／個別日時・給油の宛先・鍵の連絡、宛先別文面を用意。共通EditorModal・VehiclePlate・選択・日時・CheckboxField・SmoothCollapse・ConfirmDialogを再利用した。
- 状態: 保存、通知予約／送信／失敗、実際の到着を分離。保存失敗の入力保持、通知再予約、取消時の古い予約解除、送信後の変更・取消連絡、役割重複の文面統合、未保存保護、完了時の実際の場所を試せる。実通知は送らず、確認用時計で動作させる。
- 検証: 状態遷移7件＋本番シフトページ4件の11テスト、TypeScript、独立ビルド、差分チェック成功。PC1440px／スマホ390px・320pxで担当選択・19:30への変更・給油と文面・保存再表示・未保存保護・保存／通知失敗からの復帰・取消・完了・絞り込みを確認。320pxでページ幅／scrollWidthとも320px、ダイアログは288pxで横はみ出しなし。
- 残課題・記録: `docs/design/vehicle-handoffs-and-notifications.md` に運用案・本番接続条件、プレビュー手順と本番反映調査に境界を記録。今回は固定サンプル・画面内保存のみ。実配車選択からの確認、任意の配車変更からの自動追従、会社共通の通知初期設定、同日複数便、永続保存・予約ワーカー・実通知・地図更新は未実装。`http://127.0.0.1:3193/preview/shifts` を開いて維持。本番コード・API・DB・料金は未変更、commit／push／デプロイ未実施。

## 2026-09-01 01:04 駐車場所・返却予定をシフトへ反映するUI計画と画面画像を作成

- 依頼: 車がどこに停まっていて、どこへ返却されるかをシフト画面などへ表示するUIを計画し、画面画像を生成する。
- 方針: 通常のドライバー別シフトに返却先を1行追加。車両軸では車ごとの日付・利用者・受取／返却先と選んだ車の移動を表示。詳細は最後の駐車記録・次の受け渡し・返却予定を分け、停めた場所の記録からのみ実績を更新する。PCは表の横、スマホは詳細画面へ展開し、地図は必要時だけ開く。
- 成果物: `docs/design/shift-vehicle-location-ui.md` に表示・操作・未設定／失敗・既存部品・段階的実装の計画を記録。内蔵image_genでPC・スマホ（一覧／詳細の2状態）を生成し、`docs/design/assets/shift-vehicle-location/desktop-v1.png` と `mobile-v1.png` に保存。プロンプト一式は同階層の `shift-vehicle-location-ui-prompts.md` に記録した。
- 確認: 既存の本番ページを再利用する3193番の隔離プレビューをPC1440×1000・スマホ390×844で確認し、参照画像として使用。生成後に車両軸では不要な契約フィルターを検索／要確認へ差し替え、鍵の文言も修正。日本語、駐車と予定の区切り、画像寸法（PC1586×992／スマホ1300×1210）、ファイル保存・差分チェックを確認した。
- 状態: 今回はUI計画・静止画の依頼範囲。新しい車両軸・返却先表示・駐車記録は未実装で、新UIの実操作・実機・保存の検証は未実施。前回の操作プレビュー・本番コード・API・DB・通知は変更なし。3193番を通常幅に戻して維持。commit／push／デプロイ未実施。

## 2026-09-01 01:10 日報を入口にする駐車記録とQR・端末位置への共通基盤を設計

- 方針: ユーザーは、当面は日報と一緒に駐車場所を送信し、将来はモバイルの車両QR＋端末位置情報で地図へ記録する意向。日報を最初の入力窓口、車両ごとの駐車履歴を共通の保存先とする実装計画を作成した。
- 調査: WebのSubmitPageClientV2とmobileのDailyReportFormは同じ `/api/reports/v2` を使用。既存の地点map_places・区画parking_slots・位置履歴vehicle_positions、QRトークン解決・会社認可、モバイルの位置取得と業務終了後の日報送信を確認。日報のコース別個別保存と打刻位置のベストエフォート保存は、そのままでは駐車も含めた保存成功の保証にならない。
- 設計: 登録車庫選択・別の場所・まだ使用中／引渡し／あとで記録を用意し、駐車日時と提出日時を分離。位置履歴に駐車申告の種別・場所・取得方法・関連日報を追加する方針。日報と一括保存、再送の重複防止、古い日報の再提出・訂正、座標なし、複数コース／乗換え、日報なしの単独記録を整理。日報承認・返却予定・打刻・GPS観測から駐車済みを推測しない。
- 記録: `docs/design/daily-report-parking-foundation.md` を追加。旧駐車設計の未作成テーブルとQRの車両ID直書き、地図設計の起票時点、車両セッション設計の駐車先を現行計画へ接続。シフトUI計画・移動通知計画・プレビュー手順も日報起点へ更新した。
- 検証・範囲: 対象コード・migration・既存資料の静的照合、差分の空白チェック。文書のみの変更で、新機能のテスト・実機確認、API／DB／日報UI／プレビューの追加は未実施。本番データ・実通知・公開を変更せず、migration作成・適用、commit／push／デプロイも行っていない。

## 2026-09-01 12:53 シフト表示パネルの速度・契約操作・余白を微調整

- 依頼: 「表示」を押したときの開閉がまだ遅い。独立した契約区分の選択欄は上下余白が大きく、使用頻度も低いため「表示」へまとめたい。以前のラベルによる表示対象切替の状況も確認したい。
- 変更: 本番シフトページの契約区分選択・段分け・判定日を `ShiftDisplayOptions` の2段目へ移し、独立カードを撤去。パネルの上下余白と段間を縮小し、PCの選択・段分けを44pxから36pxへ変更、スマホは44pxを維持した。`ShiftDisplayPanel` は共通 `SmoothCollapse` のquick指定で140msにし、通常の250msは他画面へ残した。
- ラベルの現状: 複数ラベルのOR絞り込みと「ラベル・契約」表示は旧 `/preview/driver-leases` の架空データだけにあり、本番ページへは契約区分だけが移植されていた。会社別ラベルの保存・管理・認可は本番反映計画の段階3で未実装。今回の微修正では架空ラベルを本番用APIへ混ぜず、未移植であることを正本へ明記した。
- 検証: 対象3ファイル6テストに続けてWeb全103ファイル968テスト、TypeScript、3194番の独立プレビュービルド、差分の空白チェックに成功。アプリ内ブラウザのPC1440×1000でパネル115px・契約操作36px・段間8px、スマホ390×844でパネル181px・操作44px・ページ幅／scrollWidth 390pxを確認。契約操作が閉じた状態では表示されず、開いて絞り込み・段分け・表示項目を操作できる。本番DB・API・通知・migrationは変更せず、commit／push／デプロイ未実施。

## 2026-09-01 17:37 日別配車画像を1枚優先にし、行順と日付表示を整理

- 変更: 日別配車PNGは12人まで1枚、超える場合は最小枚数で契約区分／コースの境界を優先し、1人だけの次ページを避ける方式へ変更。画像の日付を中央の `YYYY/MM/DD（曜）` のみにし、「日別配車」と端末内生成・写真保存方法の補足文を撤去した。利用者向けのハイフン日付禁止をAGENTS・design・デザインシステムへ記録した。
- 行順: 「表示」内に共通SortableListを使う「行の並び順」を追加。手動ドラッグに加え、契約区分／選択日のコースでまとめる下書きを用意。シフト専用の `shift_sort_order` と保存API・migration 156を追加し、名簿No.や請求・日報の順序は変えない。半月表・日別一覧・日別PNGで同じ順を使う。
- 検証: 対象5ファイル57テスト、更新した説明・分割の11テスト、Web全105ファイル979テスト、TypeScript、4503モジュールの隔離プレビュービルド、空白チェックに成功。PC1440×1000／スマホ390×844で並べ替え・保存反映・横はみ出しなしを確認。稼働11人は1224×2763px、全員12人は1224×2895pxの各1枚。画像の日付中央・見出し／補足文の撤去、ブラウザ警告・エラーなしを確認した。
- 残課題: migration 156のDB適用、commit／push／デプロイは未実施。13人以上の実画面サンプルとiPhone／Android実機の共有・写真保存は未確認。会社別ラベルの保存・管理・認可と本番シフトのラベル絞り込みは従来どおり未実装。

## 2026-09-01 17:56 車両一覧を登録順にし、新規追加の入力エラーを具体化

- 原因・順序: 車両APIと画面がメーカー→車種→UUIDで並べていたため、同一車種では利用者からランダムに見えていた。APIと画面を登録日時→IDの安定順へ変更し、従来の稼働中→使用不可→廃車の状態分けを維持。画面へ「各登録順」と明記した。
- 入力改善: 新規追加で必須なのは車種だけと明示。空欄保存ではモーダル内の一覧・赤枠・項目下メッセージを表示し、該当タブと項目へ移動する。任意のメーター空欄は0km、交換間隔は3,000kmへ正規化し、DBのNOT NULL違反による500を防止。数値は0以上の整数としてクライアント／API双方で検証し、サーバー失敗の長文と内部エラー詳細は短い再試行案内へ置き換えた。
- プレビュー・検証: 本番車両ページを直接使い、API・DB・3Dモデルを架空データへ差し替える `/preview/vehicles` を追加。IDと登録日時を逆転した同一車種、未入力、正常保存、模擬失敗後の入力保持をPC1440×1000／スマホ390×844で確認。Web全106ファイル985テスト、TypeScript、本番ビルド、車両プレビュー独立ビルド、差分の空白チェックに成功。
- 残課題: commit／push／デプロイは未実施。追加migrationは不要。本番データ・実APIへの保存確認は未実施で、プレビューは `http://127.0.0.1:3195/preview/vehicles` を開いて維持している。

## 2026-09-02 16:50 車両移動・地図作戦盤プレビューをHH5で実装

- 変更: 本番の管理ナビ・Mapbox Standard・ナンバー札・現在／履歴と共通UIを土台に、ログイン不要の `/preview/map-operations` を追加。地図と360pxの車両詳細、移動予定線、要確認絞り込み、通常／担当未設定／駐車未記録、検索、2D／3D、近接表示、駐車記録・移動手配の画面内保存を実装した。設計の流れも画面内へ折りたたみ表示した。
- 3D: 添付アクティHH5 Blockout 70（元SHA-256 `b2d250…c3855c3b`、8,602三角形・149メッシュ・33材質）を、幾何を変えずMapbox向けの頂点配置と描画単位へ変換。29メッシュ・33プリミティブ・約366KB、配置版SHA-256 `91a4d8…3e829` とした。アプリ内ブラウザで判明した自己ホストGLBのCSP拒否を修正し、不要な標準3D建物を初期設定から無効化した。
- 設計記録: 地図はシフト、移動手配、日報・将来QRの情報を見比べる作戦盤とし、予定や駐車実績の正本にはしない。完成した車種から実モデルへ差し替え、制作中のエブリィ等は推測せずフォールバックを使う段階案を `docs/design/map-board.md` とプレビュー手順へ追記。PC／スマホ／記録モーダルの検証画像も保存した。
- 検証: 対象ロジック3件、TypeScript、2075モジュールのMapbox隔離ビルド、差分チェックに成功。アプリ内ブラウザの通常幅1280pxとスマホ390×844で、HH5表示、設計開閉、担当未設定、要確認2台、履歴、駐車記録、移動手配保存、スマホモーダル、横はみ出しなしを確認。最終ブラウザ警告・エラー0件。
- 残課題: 本番API・DB・GPS・日報・QR・通知・権限・同時編集・多数台性能には未接続。HH5の材質別着色アダプター、車種別モデル登録、既存移動手配との永続接続が次段階。追加migration、commit、push、デプロイは未実施。プレビューは `http://127.0.0.1:3196/preview/map-operations` を通常幅で開いて維持している。

## 2026-09-02 16:59 車両地図の表示サイズと車体色を強化

- 変更内容: 車両長を地図幅の9%に保つ緯度・ズーム連動の倍率計算へ変更し、実寸倍率に達した後は等倍で止めた。HH5を車体93KBと窓・タイヤ・灯火等276KBへ形状を変えず分割し、車体だけを強く着色。足元へ白地と細い濃色線のコントラストリングを追加し、地図と重なっても形を読めるようにした。分割用スクリプトと設計記録も追加した。
- 検証結果: 対象ロジック5テスト、TypeScript、2075モジュールのMapbox隔離ビルドに成功。アプリ内ブラウザの通常幅と390×844で約9%の見かけサイズ、青い車体と固定色部品、選択車両への拡大、さらに4段階拡大した実寸到達後の自然な拡大を確認。ブラウザ警告・エラー0件で、PC／スマホ／実寸到達後の検証画像を保存した。
- 残課題: 本番の地図レイヤーには未反映。多数台時のリング密度、白・黒・グレー車体の実運用色、夜間スタイルでのコントラストは次段階で実測する。エブリィ等は完成モデルから同じ車体／固定色分割へ追加する。追加migration、commit、push、デプロイは未実施。プレビューは `http://127.0.0.1:3196/preview/map-operations` を通常幅で開いて維持している。

## 2026-09-02 17:33 SVGナンバーを地図札と3Dプレート面へ共用

- 変更内容: 地図上の簡易テキスト札を実画面の `VehiclePlate` へ置換し、既存SVG字形・実物比率・黒地黄文字を共用した。車体前端に浮かせた画面向きプレート試作は不採用として撤去。HH5の前後プレート34三角形を固定部品から分離し、SVGから生成した512×256画像をUV付きマテリアルへ直接貼る33〜35KBの車両別GLBを4台分生成した。車体・固定部品は共用したまま `model-id` でプレートだけ差し替え、最大ズームを24へ拡張した。
- 検証結果: 生成テクスチャを抽出して「京都480 れ 27-52」のSVG字形を確認。Blender 5.2の合成モデルで前面プレート面への正しい向き・位置を確認した。対象5テスト、TypeScript、2075モジュールのMapbox隔離ビルド、差分チェックに成功。アプリ内ブラウザの通常幅と390×844でSVG札、車体と一緒に傾く素材面プレート、最大近接表示を確認し、警告・エラー0件。検証画像を保存した。
- 残課題: 本番の車両登録・番号変更時にプレートGLBを生成・キャッシュ・削除する仕組み、車種別のプレート材質名とUV規約は未実装。通常倍率でプレート文字が読めないのは物理サイズどおりのため仕様とし、識別は地図上の札を使う。追加migration、commit、push、デプロイは未実施。プレビューは `http://127.0.0.1:3196/preview/map-operations` を近接表示で開いて維持している。

## 2026-09-02 17:56 3Dプレートの文字比率を既存プレート札へ一致

- 変更内容: 共通 `VehiclePlate` は添付の「練馬480 れ51-14」実車写真に近い従来の大きな文字比率（240×120基準で上段29／かな27／一連58）へ復元し、見た目を変更しないまま基準値をJSONへ集約した。3D生成の独自な文字高・上寄せ・ハイフン倍率を廃止し、同じ基準値とSVG可視領域から地名・分類・かな・一連を配置。HH5の車両別プレートGLB4台を36〜38KBで再生成した。
- 検証結果: GLBから512×256テクスチャを抽出し、地図札と同じ大きい文字、等ピッチ、SVG本来のハイフン太さを確認。対象2ファイル11テスト、TypeScript、2076モジュールのMapbox隔離ビルドに成功。アプリ内ブラウザの通常幅・390×844・3D近接と `/preview/plate` の標準／compactを確認し、警告・エラー0件。地図プレビューを3D近接で開いている。
- 残課題: 本番の車両登録・番号変更時のプレートGLB生成・キャッシュ・削除、車種別の材質名・UV規約は未実装。通常倍率では実寸プレートの文字は読めないため地図札で識別する。追加migration、commit、push、デプロイは未実施。
- 最終確認: SVGから4台分を確定スクリプトでもう一度再生成し、各36〜38KBで完了した。

## 2026-09-02 18:10 移動予定を車両より前の空中アーチへ変更

- 変更内容: 地表を這うMapboxの破線レイヤーを撤去し、選択車両の出発地・届け先を画面座標へ投影する白縁付きアンバーの太い空中アーチへ変更。8pxの実線、15pxの白縁、大きな矢尻と始点リングを使い、3Dモデルより前・ナンバー札と操作より後ろに配置した。届け先が画面外でも地図内側28pxに矢尻を残し、ズーム・移動・回転・ピッチ・リサイズへ追従する。履歴では非表示。
- 検証結果: 投影ロジック2件を追加し、対象2ファイル13テスト、TypeScript、2076モジュールのMapbox隔離ビルド、差分チェックに成功。アプリ内ブラウザのPC広域・3D近接・2D、390×844の広域・近接、履歴非表示を確認し、ブラウザ警告・エラー0件。PC／スマホ検証画像を保存した。
- 残課題: 本番の車両移動・受け渡しデータとは未接続。多数の移動予定を同時表示する場合のアーチのずらし方、色分け、選択優先度は本番接続段階で実測する。commit、push、デプロイは未実施。

## 2026-09-02 18:19 地図操作中の追従と京都の到着点を修正

- 変更内容: 車両倍率とナンバー札の高さを操作終了時ではなくMapboxの `move` ごとに更新し、ズーム・移動・回転中の追従を改善。空中アーチは京都が画面外なら線だけを地図外へ抜いて矢尻を隠し、京都が表示範囲へ入ったときだけ実座標へ矢尻を置くよう変更した。画面端を仮の到着点に見せない。
- 検証結果: 画面外の行き先を到着点にしない投影テストへ更新し、対象2ファイル13テスト、TypeScript、2076モジュールのMapbox隔離ビルド、差分チェックに成功。PC・390×844の近接で線が画面外へ続くこと、広域へ縮小して京都が入ると実座標に矢尻が現れること、操作中の車両・札・アーチ追従、警告・エラー0件を確認した。
- 残課題: 本番データとは未接続。非常に速い連続操作時の実機フレームレート、多数の同時移動アーチは本番接続段階で実測する。commit、push、デプロイは未実施。

## 2026-09-02 18:28 車両地図の移動時ちらつきを抑制

- 変更内容: 空中アーチの座標更新をReactの状態更新からSVG要素の直接更新へ分離し、地図操作ごとの親画面再描画を停止。絞り込み車両をメモ化し、ナンバー札は車両IDごとのMapbox Marker・React rootを保持して、選択やデータ変更時だけ内容を更新する方式へ変更した。車両倍率・コントラスト・札位置はパン中に再設定せず、ズーム・リサイズ・移動終了時に必要な差分だけ1フレーム1回更新する。
- 検証結果: 地図計算7テストとTypeScript、差分の空白チェックに成功。アプリ内ブラウザで連続パンとズームを行い、4台のナンバー札維持、車両サイズと空中アーチの追随、ブラウザエラー0件を確認した。
- 残課題: 低性能端末と多数台での実フレームレート計測、本番地図への反映は未実施。commit、push、デプロイは未実施。

## 2026-09-02 18:42 地図のナンバー札を車両追随の常時表示へ変更

- 変更内容: 地図上のナンバー札だけを、共通 `VehiclePlate` の寸法・SVG字形からCanvasへ一度描画する1枚画像へ変更し、移動中に多数のCSS maskを再合成しないようにした。車両IDごとのMarker保持に加え、Mapboxの3D遮蔽opacityを1へ固定し、標準の0.2秒フェードを無効化。画面向きの札を車両座標へ追随させ、表示範囲内では常時表示する。
- 検証結果: 対象2ファイル13テスト、TypeScript、2077モジュールの隔離プレビュービルド、差分チェックに成功。アプリ内ブラウザで選択車両へ寄る移動30サンプルと全体へ戻る移動30サンプルの全てでMarker4個・画像札4枚・opacity 1を維持。手動パンでも4枚を保持し、警告・エラー0件を確認した。
- 残課題: 低性能端末と多数台での実フレームレート、本番地図への反映は未確認。commit、push、デプロイは未実施。

## 2026-09-02 20:55 日報のメーター基準を車両の登録走行距離へ変更

- 変更内容: ドライバーの日報送信（`/submit` = SubmitPageClientV2）で、走行距離の判定基準を「提出日以前で最後に記録された日報のメーター値」から「車両に登録されている走行距離（`vehicles.current_mileage`）」へ変更（ユーザー指定）。日付ごとに基準を取り直す `GET /api/reports/meter-baseline` は未使用となったため削除。文言も「前回」→「現在の登録」「現在: N km」へ更新し、判定ロジック `evaluateMeter` 自体は据え置き（未登録0の車両は初回入力として通す）。`docs/platform-design.md` の未対応項目（meter-baseline のフォールバックで他社車両の走行距離が読める余地）はAPI削除により解消として追記。
- 検証結果: Web 992テスト（107ファイル）と TypeScript に成功。`/preview/submit-meter` を新設し、実画面 SubmitPageClientV2 をそのまま描画して `window.fetch` の `/api/` 宛だけを架空データへ差し替える方式にした（本番の認証・DB・API・通知には接続しない）。dev サーバー（:3001）でSSR 200を確認。
- 残課題: **アプリ内ブラウザが未接続でプレビューの実操作・PC/スマホ幅の確認が未実施**。接続後に、登録値以下の赤字表示と送信ブロック、未登録車両の初回入力、EV車の入力欄非表示、送信後画面までを確認する。承認前の日報は基準に反映されないため、承認が滞ると同じ値を再提出できてしまう点は運用で要観察。commit、push、デプロイは未実施。

## 2026-09-02 23:41 車両移動モードと空中アーチの起点を修正

- 変更内容: 車両地図の表示を「いま／車両移動／履歴」の3モードに整理。「車両移動」では、未完了かつ出発地と届け先が異なる車両だけを表示し、通常の「いま」と履歴では移動アーチを隠す。同じ場所での受け渡しと到着済みは対象外とした。絞り込み前に移動対象外の車両を選んでいても、移動モードへ切り替えた時点で最初の対象車両へ選択を移す。
- 起点修正: アーチの始点を移動手配に入っている車庫の代表座標から、地図に表示している選択車両の最後の記録位置へ変更。4303では豊中車庫の代表点ではなく4303車から吹田車庫へ線が出る。画面外の線分を両方向で切り出し、始点側も地図外まで96px伸ばしてクリップする。実座標が画面外なら始点の丸を隠し、届け先が画面外なら従来どおり矢尻を隠す。
- 検証結果: 地図ロジック9件、Web全107ファイル994件、TypeScriptを含む本番ビルド、2077モジュールの隔離プレビュービルド、差分チェックに成功。アプリ内ブラウザのPC1280pxとスマホ390×844で、通常表示の矢印なし、移動対象2台、非対象2752からのモード切替、1201の両端画面外、4303車と始点の横位置差2.8px、ページ横はみ出しなし、警告・エラー0件を確認した。
- 制限: プレビューの架空データと画面内保存だけを変更。本番API・DB・GPS・日報・QR・通知・本番地図には未接続。commit、push、デプロイは未実施。プレビューは `npm run preview:admin -- map-operations --port 3196 --mapbox` で起動し、`http://127.0.0.1:3196/preview/map-operations` を4303選択の車両移動モードで開いている。

## 2026-09-03 00:46 車両履歴を指定日時の時点再現へ変更

- 変更内容: 履歴の連続スライダーを撤去し、共通の日付・時刻入力と「前の記録」「次の記録」へ置換。架空4台に出どころ付きの時系列記録を持たせ、指定日時以前の最後の記録だけを表示する as-of 解決を追加した。右の詳細も、その時点の場所・記録日時・記録者・出どころに限定し、現在の要確認絞り込み・次回利用・移動手配・編集操作を履歴へ混ぜない。地図凡例は「指定時点の記録」とした。
- 設計判断: 打刻・日報・手動配置のような疎な記録に連続スライダーを使うと、未観測の中間位置まで把握していたように見えるため不採用。記録間の位置は補間しない。3D表示だけをデジタルツインとは呼ばず、同じ車両IDへ実績・予定・時点再現・差分／予測を分離して重ね、本番の記録源と同期して初めて本体になる境界を `docs/design/map-board.md` に明記した。
- 検証結果: 地図モデル10件、Web全107ファイル995件、TypeScriptを含む本番ビルド、4264モジュールの隔離プレビュービルド、差分チェックに成功。アプリ内ブラウザで18:00の打刻記録から20:10の日報記録へ前後操作で切り替わることを確認。通常幅と390×844で日時操作、指定時点の根拠表示、横はみ出しなし、警告・エラー0件を確認した。
- 制限: 固定の架空データとローカル状態のみ。本番API・DB・GPS・日報・QR・シフト・通知には未接続で、連続再生・軌跡・推定区間も未実装。commit、push、デプロイは未実施。プレビューは `http://127.0.0.1:3196/preview/map-operations` を履歴モードで開いている。

## 2026-09-03 01:24 本番地図へ車両移動の手配と履歴日時指定を接続

- 変更内容: 本番 `/admin/map` の履歴を日付・時刻・前後の記録による as-of 表示へ変更。`vehicle_movements` と原子的な保存・完了・取消関数を migration 157 に追加し、会社・車両・車庫・担当者の一致、担当未設定／手配済み／到着／取消、版競合をDBで検証する。`/api/admin/map/movements` から登録車庫、在籍ドライバー、直近14日の実シフトを取得し、本番地図の「車両移動」で未完了かつ別車庫へ移す車両だけを表示。登録・変更・取消・完了UIを追加し、完了時だけ `vehicle_positions` へ実位置を追記する。空中アーチを本番・プレビューの共通部品へ移し、選択車両の最後の記録位置を始点にした。
- 検証結果: Web全108ファイル999テスト、TypeScript、本番ビルド、4266モジュールのMapbox隔離プレビュービルド、差分チェックに成功。アプリ内ブラウザの通常幅と390×844で、移動対象2台への絞り込み、4303車を始点とする吹田向きの矢印、履歴の日付・時刻・前後操作、警告・エラー0件を確認した。
- 残課題: migration 157の本番適用、デプロイ、本番データでの権限・競合・完了記録の確認は未実施。日報と同時の駐車記録、QR・GPS、シフトの車両軸、同じ車庫での受け渡し、通知予約・送信ワーカーも未実装。commit、pushは未実施。プレビューは `http://127.0.0.1:3196/preview/map-operations` を4303選択の車両移動モードで開いている。

## 2026-09-03 02:38 migration 157の場所検証構文を修正

- 変更内容: Supabase SQL Editorで `42601 syntax error at end of input` になった `save_vehicle_movement` の車庫件数チェックを修正。副問い合わせと `CASE` を直接比較する条件をやめ、`SELECT count(*) INTO place_count` で取得してから括弧付きの `CASE` と比較するPL/pgSQLへ変更した。
- 検証結果: 関数3件のドル引用開始・終了、migrationの `BEGIN` / `COMMIT` 対応、差分の空白エラーなしを確認した。ローカルPostgreSQLは起動していないため、実DBでの再実行は未確認。
- 残課題: 最初の実行は明示トランザクション内の構文エラーでロールバックされる想定。修正版migration 157を先頭から再実行し、成功後にテーブルと関数3件が作成されたことを確認する。

## 2026-09-06 14:30 本番ページをシナリオURLで開く管理プレビュー runner

- 変更内容: ChatGPT で整理した「同一コード・fixture・`?scenario=`／`?role=`」方針を既存 runner に実装。`scripts/previews/admin.tsx`（URL振り分け・一覧）、`kernel/`（`@/lib/api`・`swr`・`next/*`・`AdminLayout` の差し替え）、`fixtures/`（ダッシュボード・車両・ドライバー一覧）、`apps/web/src/lib/preview/`（URL解析・役割→capability・fixture ストア）。`AdminPreviewLayout` に役割・会社名・注記の省略可能 props を追加し、閲覧のみ／経理でメニューのロックと書込ボタンが本番と同じ判定で変わる。旧 `vehicles-services.tsx` は fixture へ統合して削除し、`-- vehicles` はエイリアス化。`process.env.NEXT_PUBLIC_COMPANY_CODE` を runner で空文字に固定。
- 検証結果: 新規 17 テスト（scenario／fixtureStore）、プレビュー配下 17 ファイル 161 テスト、Web 全 1016 テスト（LeaseAdminPreview の1件は全体実行時のみ 5.5 秒でタイムアウトする既存の時間依存。単体では変更あり・なしとも 23 件通過）、apps/web と scripts/previews の TypeScript、5096 モジュールの隔離ビルド。アプリ内ブラウザで PC／375 幅、通常・長い名前×閲覧のみ・大量・取得エラー・読み込み中、クイックリンクの pushState 遷移を確認。警告・エラー 0 件。
- 残課題: シフト・報告・売上など他ページの fixture 追加、日付依存ページの `?date=` 固定、車両カードで長い車種名がはみ出す本番側の崩れ（プレビューで発見）。Codex 精査の裏取りと修正順は `docs/roadmap-2026-09.md` に洗い出した。commit／push／デプロイは未実施。プレビューは `http://127.0.0.1:3197/preview/admin`。

## 2026-09-06 15:05 プレビュー runtime を Context 経由に変更

- 変更内容: `scripts/previews/admin.tsx` が描画中に `setPreviewRuntime` で購読者へ通知していた（code-review で検出）。フックは `PreviewRuntimeContext` で受け取り、シングルトンは apiFetch・getStoredDriver・Link の参照専用にした。
- 検証結果: scripts/previews の TypeScript、5096モジュールの隔離ビルド、経理×0件を保ったままダッシュボード→車両の pushState 遷移をアプリ内ブラウザで確認。
- 残課題: commit／push は未実施。プレビューは `http://127.0.0.1:3197/preview/admin`。

## 2026-09-06 15:40 ui-ux-pro-max 導入と車両カードの長い車名の折返し

- 変更内容: ユーザーが `ui-ux-pro-max-cli` を導入し `.claude/skills/` に7スキル（ui-ux-pro-max・design・design-system・ui-styling・brand・banner-design・slides、4.8MB・未コミット）を展開。スキルの検索（`python3 .claude/skills/ui-ux-pro-max/scripts/search.py "…" --domain ux`）で「長いトークンは overflow-wrap anywhere と縮められる flex 子要素で折り返す・本質的な名前を省略しない」を確認し、`admin/(resource)/vehicles/page.tsx` の車種名 span を `shrink-0` から `min-w-0 max-w-full flex-wrap [overflow-wrap:anywhere]` へ変更（空白専用 span も削除）。
- 検証結果: apps/web の TypeScript、車両ページのテスト、隔離ビルド。`/preview/admin/vehicles?scenario=long-name` で PC（本文右端832 < カード右端865）と 375 幅（346 < 363、2行折返し）を計測し、横スクロールなし。
- 残課題: `.claude/skills/` をコミットするか（Codex と共有するなら含める）はユーザー判断。roadmap-2026-09 の A-4 は完了。commit／push は未実施。

## 2026-09-06 15:55 .claude/skills を git 管理外に

- 変更内容: `.claude/skills/` は `uipro init` が展開した一般公開スキル7件のみでハコ虎固有の定義は無いことを確認し、`.gitignore` に追加。固有の役割定義は従来どおり `.claude/agents/`（6役）と `.codex/skills/hakotora-subagents/` を git 管理する。
- 検証結果: `git status` で `.claude/skills/` が未追跡から消えることを確認。
- 残課題: 別マシンでの再導入手順（`npm i -g ui-ux-pro-max-cli && uipro init --ai claude`）を文書に残すかはユーザー判断待ち。

## 2026-09-06 16:40 シフト表に車両の余り・不足と重複割り当ての赤枠を追加

- 変更内容: `apps/web/src/lib/shiftVehicleBalance.ts` に純粋ロジック（使える車両数＝廃車・一時使用不可・その日の貸出中を除く、必要台数＝他社車両を使う人を除いた稼働人数、重複＝2人以上に割り当てた車両）を追加。`admin/(ops)/shifts/page.tsx` は稼働人数の集計を id 集合に変え、PC の両軸ヘッダーとスマホの日別ヘッダーの「稼働 N」の下に「車 余りN台／不足N台／ちょうど」（不足は赤）を表示。コース軸・ドライバー軸・スマホの3箇所のナンバー描画を `DuplicateVehicleFrame` で包み、重複時に赤枠＋説明 title を付ける。`scripts/previews/shifts-services.tsx` に「田中に佐藤と同じ車両を割り当て」の切替を追加。
- 検証結果: 新規4テスト、シフトページ5テスト（重複の赤枠と解消・「車 余り1台」）、apps/web の TypeScript、4505モジュールの隔離ビルド。`http://127.0.0.1:3198/preview/shifts` で PC（稼働10／車 余り2台、赤枠24箇所）と 375 幅を確認。
- 残課題: 台数の定義（貸出中・他社車両の扱い）は上記のとおり置いたので、現場の感覚と違えば調整する。commit／push は未実施。

## 2026-09-06 17:05 日ヘッダーをアイコン＋符号付き数値に変更

- 変更内容: 「稼働 N」「車 余りN台」の2行を `DayHeadcount`（人アイコン＋人数、車アイコン＋ +N 緑／−N 赤／0 灰）の1行にまとめた。PC 両軸ヘッダー・スマホ日別ヘッダー共通。色だけに頼らず符号を付け、hover と読み上げ（aria-label / title）には「稼働 N 人、車 余りN台」の全文を残す。左上の注記を「上段＝稼働人数・車の余り」に更新。
- 検証結果: TypeScript、シフトページ5テスト（`data-vehicle-balance` の +1 と title を検証）、隔離ビルド。PC と 375 幅で表示確認。
- 残課題: commit／push は未実施。プレビューは `http://127.0.0.1:3198/preview/shifts`。

## 2026-09-06 17:20 日ヘッダーの注記を削除し、人・車の行を日付の下へ

- 変更内容: PC 両軸ヘッダー左上の「上段＝稼働人数・車の余り」を削除。`DayHeadcount` を日付の下に移動（余白を mb-1 → mt-1）。スマホの日別ヘッダーは元から日付の下なので変更なし。
- 検証結果: TypeScript、シフトページ5テスト、隔離ビルド、アプリ内ブラウザで表示確認。
- 残課題: commit／push は未実施。

## 2026-09-06 17:35 日ヘッダーの人・車の行を日付より控えめに

- 変更内容: `DayHeadcount` を bold → medium、PC は 10.5px、人数の色を slate-500、アイコンを 70% 濃度にし、日付との余白を mt-1.5、人と車の間隔を gap-2.5 に調整。不足の赤・余りの緑はそのまま。
- 検証結果: TypeScript、シフトページ5テスト、隔離ビルド、PC／375 幅で表示確認。
- 残課題: commit／push は未実施。

## 2026-09-06 17:50 車両の必要台数を「割当済みの車の台数＋未割当の人数」に変更

- 変更内容: ユーザー指摘（時間帯がずれていれば同じ車を2人で回せる）を受け、`computeVehicleBalance` の必要台数を「稼働人数」から「割り当て済み車両の重複排除台数＋車未割当で他社車両でもない稼働人数」へ変更。`vehicleOf` を追加し、シフト画面は `getCurrentVehicleForDriverOnDate` を渡す。
- 検証結果: ロジック5テスト・ページ5テスト（重複時は +2、解消で +1）、TypeScript、隔離ビルド。
- 残課題: 重複割り当ての赤枠は「時間帯が重なるか」を見ていない（便・集合時刻が違えば正しい運用でも赤枠が出る）。必要なら便が異なる組み合わせは枠を出さない判定を足す。commit／push は未実施。

## 2026-09-06 18:10 シフト表の車両過不足・重複表示を本番へ反映

- 変更内容: c9154a4 としてコミットし main へ push。プレビュー基盤・車両カード折返し・roadmap-2026-09 は直前の 8770da2 に同梱済み。重複の赤枠は時間帯を判定せず全て見せる方針で確定。
- 検証結果: Vercel 本番プロジェクト nippo-ace のデプロイが Ready（production）になったことを `vercel inspect --wait` で確認。
- 残課題: 実データで余りの数字が現場の感覚と合うかは運用で確認。次は roadmap-2026-09 の B-1（anon key と DB 権限の棚卸し）。

## 2026-09-06 18:55 migration 157 を一時DBで実行し構文と関数を検証

- 経緯: ユーザーが本番 SQL Editor で受けた `42601 syntax error at end of input (LINE 88: <> CASE ... END THEN)` は 9/3 修正前の内容。現在のファイルは `SELECT count(*) INTO place_count` と括弧付き `CASE` の比較になっている。本番DBを読み取りで確認し `vehicle_movements` は未作成（前回はトランザクション内で失敗しロールバック済み）。
- 検証結果: 別プロジェクトのローカル Supabase の PostgreSQL に一時DB `hakotora_mig157_check` を作り、drivers／vehicles／map_places／vehicle_positions の骨組みを置いて 157 を先頭から実行。COMMIT まで到達しテーブルと関数3件を作成。`save_vehicle_movement`（新規・担当あり）→ `finish_vehicle_movement`（到着、version 2、`vehicle_positions` へ京都の座標と「車両移動の完了記録」を追記）、旧 version の `cancel` は 40001 で拒否、他社 org の保存は P0002 で拒否を確認。一時DBは削除済み。本番には触れていない。
- 残課題: 本番 SQL Editor で現在の 157 を先頭から実行し、`select to_regclass('public.vehicle_movements')` と関数3件の存在を確認してから `/admin/map` の「車両移動」を実データで確認する。

## 2026-09-06 20:10 本番地図へプレビューの見た目を移植し、クリック詳細を作り直し

- 変更内容: 9/2 に検討用プレビューで確定した見た目を本番 `/admin/map` へ移植。純粋ロジックを `lib/map/vehiclePresentation.ts`（車両長＝地図幅の約9%、実寸到達後は等倍、リング半径・札オフセット、差分判定）、SVG札の画像化キャッシュを `lib/map/mapPlateImage.ts`、札の部品を `lib/components/MapPlateLabel.tsx` へ切り出し、プレビューも同じ部品を使うよう変更。`vehicleModels.ts` に車種→2層モデル（車体着色／固定色）の登録表 `VEHICLE_MAP_MODELS` を追加（当面アクティHH5のみ・未登録はHH5へフォールバック・型式は扱わない）。本番地図は旧 `truck-3d`（Meshy由来 every/acty/clipper）を廃止し、`vehicle-contrast`（足元リング）＋`vehicles-3d-tinted`＋`vehicles-3d-fixed` の3層と rAF での倍率更新へ変更。札は Marker を `occludedOpacity: 1`・viewport 追随にし、状態ドット・状態文字・縮退ドットは維持。クリックの詳細は大きなナンバーをやめ、小さな札＋車種・稼働状態・オイル交換の残り（100km未満は赤、300km以下は橙）・走行距離・次回車検・最後の記録に変更し、anchor ごとの offset で札に被らない位置へ出す。地図API `/api/admin/map/vehicles` に走行距離・オイル交換・車検・EV の列を追加。
- プレビュー: admin runner に本番地図ページの fixture（`scripts/previews/fixtures/map.ts`: 拠点3・区画2・配達エリア・車両5／位置なし／40台密集・移動手配1、手動配置・移動の登録／完了・拠点追加を画面内保存）と `@/lib/map/sharedView` のスタブを追加し、`--mapbox` を admin にも許可、CSP に Mapbox 3D建物タイルを追加。起動は `npm run preview:admin -- admin --port 3199 --mapbox` → `/preview/admin/map`。
- 検証結果: 新規テスト（presentationChanged・vehicleMapModelFor）を含む地図系 18 テスト、apps/web・scripts/previews の TypeScript、5166モジュールの Mapbox 隔離ビルド。アプリ内ブラウザで PC 広域／3D近接（青い車体＋固定色部品＋リング＋SVG札＋状態）、車両移動モードの空中アーチ、375幅の40台密集（札34枚が縮退・横スクロールなし）、ポップアップと札の非重なりを座標で確認。全体 1026 テストは 2 件が全体実行時のみタイムアウト（単体では通過する既存の時間依存）。
- 追記（20:40）: ユーザー指摘で詳細を文字中心からアイコン中心へ変更。オイル交換は残量バー（緑／橙／赤）＋残り km、走行距離・車検・最後の記録はアイコン＋数値だけ。札のオイル警告バッジの吹き出しが本文に被ったため、ポップアップ内の札には走行距離を渡さない。座標で札との非重なり・吹き出し非表示を確認。
- 残課題: ステップ2＝ハイゼット19／エブリイ88／アクティ75 の GLB を車体・固定色・**灯火**の3分割で取り込み、夜は灯火層を発光させてヘッドライト・テールランプを点ける（ユーザー要望）。現状の夜の光は従来の足元の円のまま。多数台の実 FPS は本番データで実測する。

## 2026-09-06 20:55 地図の見た目移植を本番へデプロイ

- 変更内容: b144352 を main へ push。Vercel 本番プロジェクト nippo-ace のデプロイが Ready（production）になったことを確認。
- 検証結果: `vercel inspect --wait` で status Ready。実データでの表示・多数台の FPS は運用で確認。
- 残課題: ステップ2（3車種の取り込み＋灯火層の夜間発光、roadmap-2026-09 A-7）。

## 2026-09-07 03:50 地図の車両サイズを短い辺基準へ変更し、重なる車体をまとめ表示

- 変更内容: ユーザーの本番スクリーンショット（幅約2000pxの3D俯瞰で車が巨大化・3台が重なる）を受け、目標の車両長を「地図幅の9%」から「短い辺の14%・40〜120px」へ変更し、ピッチが深いほど目標を下げる補正（`VEHICLE_PITCH_SHRINK`）を追加。`targetVehicleLengthPixels` を切り出し、本番・プレビューとも高さとピッチを渡し `pitch` イベントでも更新。地図コンテナに `data-vehicle-length-px` を出して検証できるようにした。重なり対策として、見かけの車両長の0.9倍以内の車を手前優先で束にし、代表だけ車体を描き（モデルのソースから除外）、吸収された車は状態色ドットへ縮退、代表の札に「N台」バッジ、バッジのクリックで束が離れるまで `fitBounds`。runner の CSP に Mapbox の建物・樹木モデル取得を追加。
- 検証結果: 地図系19テスト（短い辺・ワイド・ピッチ・上下限の期待値を更新）、TypeScript、Mapbox 隔離ビルド。runner で 1726×630 の3Dは車両長 64.8px（旧方式は155px）、1400×760 の「大量」で束バッジ 4台／6台 が出て、バッジクリック後は全て1台・縮退0になることを確認。
- 残課題: 同じ点に重ねて置いた車の横ずらし、束の見た目（代表の色・バッジ位置）の実機での微調整。ブラウザペインが非表示だと最終の見た目が描画されないため、本番デプロイ後に実画面で確認する。

## 2026-09-07 05:20 地図に3車種の実モデルと夜のヘッドライトを接続

- 変更内容: 分割スクリプトを「車体／固定色／灯火」の3分割に拡張（`Headlight Lens`・`Rear Red Lens`・`Light Brake High` を灯火へ。既定でプレート面は固定色に残し `--drop-plates` で旧方式）。ハイゼット19・エブリイ88・アクティ75 を Mapbox 用に整えて `public/models` に9ファイル（約2MB）を追加し、`VEHICLE_MAP_MODELS` に登録。OEM は元車種へ寄せる別名表、既定モデルはエブリイ。`KEI_VANS` のハイゼット・エブリイDA64V・OEM各社に車種キーを付け、`mapModelKeyForVehicle` で `model_key` が空の既存車もメーカー＋車種名から選ぶ。本番地図に `vehicles-3d-lamps` 層を追加し、夜（`isNightJst`）の稼働中の車だけ `lampsOn` で強く発光（履歴では点けない）。runner の配信アセットに新モデルを追加。
- 検証結果: 地図系のテスト（別名・車種名フォールバック・3ファイル）、TypeScript、Mapbox 隔離ビルド。runner（日本時間4時台＝夜）で青いアクティ・赤／白のエブリイ・黒のハイゼットが車種ごとに描かれ、束バッジ、実寸近くの稼働中アクティの前照灯を確認。コンソールに灯火層の式エラーなし。
- 残課題: 灯火の発光量（4）は本番の夜景で微調整。実寸到達前の倍率（最大約4.5倍）は3D建物の隣では大きく見えるので、建物が出るズームでは目標を下げるか検討。同一点に重ねた車の横ずらしは未対応。

## 2026-09-07 05:45 3車種モデルと夜のヘッドライトを本番へデプロイ

- 変更内容: 0ad06ec を main へ push。Vercel 本番プロジェクト nippo-ace のデプロイ Ready（production）を確認。
- 検証結果: `vercel inspect --wait` で Ready。本番の夜景での発光量、3D建物ズームでの倍率は実画面で確認する。
- 残課題: 灯火の発光量（4）の微調整、建物が出るズームでの目標サイズ、同一点の車の横ずらし。

## 2026-09-07 06:20 車両編集の3Dプレビューを新モデルへ切替

- 経緯: ユーザー指摘「車両編集の3Dプレビューがハイゼットにならず旧エブリイのまま」。地図とは別の `VEHICLE_MODEL_URLS`（旧 Meshy の every/acty/clipper.glb）を読んでいたため。あわせて「ライトは点いているのか」→ 現仕様は夜の**稼働中**の車だけ点灯で、写真の 58-54 は退勤済みのため消灯が正しい。
- 変更内容: keivan-3d 版の1ファイルGLB（hijet 19／every 88／acty 75、計約1.9MB）を `public/models` に追加し `VEHICLE_MODEL_URLS` を差替。`modelUrlFor` は地図と同じ別名表（OEM→元車種、既定エブリイ）で解決。`VehicleModelPreview` は塗装材質（Body White 等）だけを車体色で塗り、夜は灯火材質（Headlight Lens／Rear Red Lens／Light Brake High）を発光させ、スプライトは前＝電球色・後＝赤（+X が前）。旧生成モデルの読み込み互換は残した。runner は本物の `VehicleModelPreview` を束ね、1ファイルGLBも配信。
- 検証結果: 地図系10テスト（`modelUrlFor` の別名・既定を追加）、TypeScript、隔離ビルド。runner の `/preview/admin/vehicles` で「ダイハツ ハイゼットカーゴ」の編集モーダルを開き、白いハイゼットのローポリと赤いテールランプ（夜判定）を確認。読み込みエラーなし。
- 残課題: 旧 Meshy モデル群（every/acty/clipper/truck ほか実験用 acty_*）の削除は参照確認のうえ別コミットで。

## 2026-09-07 06:35 3Dプレビュー切替を本番へデプロイ

- 変更内容: 6ff4820 を main へ push。Vercel 本番プロジェクト nippo-ace のデプロイ Ready（production）を確認。
- 検証結果: `vercel inspect --wait` で Ready。
- 残課題: 夜のライトを全車に広げるかはユーザー判断待ち。旧 Meshy モデル群の整理は別コミット。

## 2026-09-07 07:30 地図の詳細ポップアップを札より前に出し、束は全台表示、オイルは進み表示

- 変更内容: ユーザー指摘3点。(1) 詳細ポップアップに他の札が被る → Popup に `vehicle-popup` クラスを付け `globals.css` で z-index 30（札は 5）。(2) オイルのバーは残量ではなく車両一覧と同じ「前回交換からの進み」（右端＝次回交換、色の閾値も一覧と同じ赤／黄／緑）。数値は残り km のまま。(3) 束（N台）の代表をクリックしたら全台の詳細を並べる `VehiclePopupGroup`（開くたびに束の構成で差し替え、最大高さ60vhでスクロール）。あわせて Developer リポジトリの PR #1（Button 44px・TimePicker・和文日付・assets ignore）をマージ。
- 検証結果: TypeScript、隔離ビルド。runner の「大量」で代表の札をクリックし、`9台` の見出し＋各車の詳細、ポップアップの z-index 30、バー幅（アクティ11%・エブリイ73%）を DOM で確認。
- 残課題: 束の詳細が長い場合の見やすさ（車種ごとの折りたたみ等）は実運用で判断。

## 2026-09-07 07:55 地図の詳細ポップアップは常に札の上へ開き、入りきらなければ地図を寄せる

- 変更内容: ユーザー指摘「下に伸びて地図からはみ出るとスクロールしにくい」。Popup の anchor を `bottom` 固定にして常に札の上へ開き、開いた後に枠との差分を測って `map.panBy` で上端・左右に収める（Google マップと同じ自動パン）。束の一覧の最大高さは地図の高さ−96px にして中でスクロール（`overscroll-contain`）。
- 検証結果: TypeScript、隔離ビルド。runner の「大量」で地図上端近く（上から142px）の「6台」の札をクリックし、ポップアップが上に開いて地図が自動で寄り、上端12px〜下端558px（地図高607px）に収まることを DOM で確認。
- 残課題: なし（スマホ幅は地図の高さが小さいので、束が多いときは一覧のスクロールで対応）。

## 2026-09-07 09:40 日報送信時に登録済みの駐車位置を選んで記録する（Phase 1）

- 変更内容: `docs/design/daily-report-parking-foundation.md` の Phase 1（保存基盤＋Web日報の入口）を実装。migration 158 で `vehicle_positions` に申告の列（kind／place_id／slot_id／place_name／driver_id／report_date／client_key）と `source='report'` を追加し、観測は座標必須のまま申告は「車庫か場所名」必須・座標任意。`(org_id, vehicle_id, client_key)` 一意で再送を上書き。`map_places.allow_parking` と `map_latest_positions` の列追加。API は `GET /api/reports/parking-places`（自社の車庫・区画）と `POST /api/reports/v2` の `parking`（`server/reports/parking.ts` で検証→日報保存後に upsert。失敗時は「日報は保存されています」で再送を促す）。Web 日報に `ParkingReportField`（登録車庫→区画、別の場所、まだ使用中／次の人に渡した／あとで記録。いつもの区画がある車庫を先頭に出し自動選択、未回答は送信ブロック）。地図の詳細に申告の場所名、座標なしの申告は `parkedElsewhere` として返す。共有型 `ParkingReport`／`ParkingPlaceOption` を `@repo/core` に追加。admin runner に `/preview/admin/submit`（通常／車庫なし）を登録し、Next 版 `/preview/submit-meter` のモックにも候補を追加。
- 検証結果: 一時DBで migration 158 を先頭から実行し、既存行の移行・再送 upsert が1行のまま・座標なし申告の許可・観測の座標なし拒否・場所なし申告の拒否・RPC の最新返却を確認。`parseParkingReport` 6テスト、日報画面15テスト（未回答ブロック・いつもの区画の自動選択と送信内容・別の場所の名前必須）、TypeScript（web／scripts）、隔離ビルド。アプリ内ブラウザで `/preview/admin/submit` の実操作（未回答→ブロック、豊中→A-1 自動選択、送信内容に place/slot/メモ、送信後画面）を確認。
- 残課題: **本番反映の順番**＝先に SQL Editor で migration 158 を適用してから push（先にコードが出ると駐車の保存が列不足で失敗する）。日報と駐車の同一トランザクション化、`in_use` 等の回答の永続化、シフトの「最後の駐車」表示、地図一覧での `parkedElsewhere` 表示、モバイル、会社別の必須／任意設定、管理者の代理入力・訂正は次段階。commit はローカルのみで push 未実施。

## 2026-09-07 10:05 駐車申告 Phase 1 を本番へデプロイ

- 変更内容: ユーザーが migration 158 を本番へ適用。読み取りで `vehicle_positions.kind／client_key／place_name` と `map_places.allow_parking` の存在を確認してから c1671ba を push。Vercel 本番プロジェクト nippo-ace のデプロイ Ready（production）を確認。
- 検証結果: `vercel inspect --wait` で Ready。実データでの初回送信（車庫の候補が出る・保存される・地図の詳細に場所名が出る）は運用で確認する。
- 残課題: シフトの「最後の駐車」表示、地図一覧の「別の場所」表示、`in_use` 等の回答の永続化、モバイル、会社別の必須／任意設定。

## 2026-09-08 02:50 拠点を日報の置き場所の候補から外す設定を地図の編集パネルに追加

- 変更内容: 地図の拠点編集パネルに「日報の置き場所の候補に出す」チェック（共通の `CheckboxField`）を追加し、保存で `allowParking` を送る。`GET/POST /api/admin/map/places` と `PATCH /api/admin/map/places/:id` で `allow_parking` を返す・受け取る（未指定は出す）。設定の拠点一覧で候補外の拠点に「日報の候補外」を表示。admin runner の地図 fixture に `allow_parking` を追加し、拠点編集が `PUT` しか受けず保存できなかった不一致を `PATCH` に修正。`.claude/launch.json` に admin プレビューの起動設定を追加。設計書にモバイルの方針（候補選択ではなく位置情報＋motion で自動特定）を記録。
- 検証結果: web の tsc、parking／preview kernel の 23 テストに成功。`http://localhost:3197/preview/admin/map?scenario=normal&role=admin` で「吹田 待機」（候補外）を開いてチェック→保存→一覧の「日報の候補外」が消えることを PC 幅で確認、375px 幅でも編集パネルにチェックが収まることを確認。
- 残課題: runner の CSP が Mapbox の meshopt wasm を弾く console エラー（runner のみ・以前から）。編集中の拠点ピンが編集パネルの上に重なる（以前から）。候補の自動特定（モバイル）は未着手。

## 2026-09-08 03:40 モバイルの駐車位置 自動特定（位置情報＋motion）の設計書を起草

- 変更内容: `docs/design/mobile-parking-auto-detect.md` を新規作成。モバイルは候補選択 UI を出さず、業務終了時の1回測位（Phase M1）を確定点にし、停車の保持（M2）・降車検知（M3）は補強に留める。時刻は停車開始（測位時刻）、精度 `accuracy_m` と根拠 `detected_by` を `vehicle_positions` に足す案（migration 159 候補）。スナップはサーバーで行い（`allow_parking=true` の拠点・半径＋精度で判定・精度 25 m 以内だけ区画判定）、当たらなければ座標だけ保存し名前を推測しない。位置の失敗で日報を止めない。
- 検証結果: 文書のみ。地図の実ブラウザ監査（ui-auditor）とモバイルの棚卸し（explorer）を並行で実行中。
- 残課題: 監査結果を待って、Passkey・モバイルログイン・セキュリティ（roadmap-2026-09 B-1〜B-8）・モバイル画面・地図作戦盤の残タスクを1枚の Web ページに整理する。点の拠点の既定半径 60 m、区画判定の精度上限 25 m、[違う] の操作方式は要判断。

## 2026-09-08 05:10 残タスク作戦盤（6トラック）と地図の使い勝手 再設計を整理

- 変更内容: 地図の実ブラウザ監査（Playwright・normal／large・1440px／375px）とモバイルの棚卸しを実施し、`docs/design/map-board-usability-2026-09.md`（監査 P1〜P3 の原因を行単位で特定・重なり順の規約・段階1〜4・受け入れ基準）を新規作成。Passkey／モバイルログイン／セキュリティ B-1〜B-8／モバイル画面／駐車位置／地図作戦盤の残タスクを 6 トラックに束ねた Web ページ（Claude アーティファクト）を公開し、`docs/roadmap-2026-09.md` に §D として索引と着手順を追記。B-7 は challenge token が 5 分の JWT で一回限りの消費が無いことをコードで確定。`feat/mobile-capture-flow`（VisionCamera 版・4 コミット）が未マージ・未 push であることを確認。
- 検証結果: 文書とページのみ。コード変更なし。
- 残課題: 判断待ち J-1〜J-7（札の直接ドラッグ、広域でモデル非表示、VisionCamera ブランチの取り込み、駐車スナップの閾値、夜のライト、テスト環境、PIN 閉鎖日）。着手は S-1（DB 権限の棚卸し）と地図 段階1 から。

## 2026-09-08 06:20 CLI から DB を読む・migration を適用する経路を用意（S-1 着手）

- 変更内容: `scripts/db/db.sh` を新規作成。`q`／`qcsv`／`qf`／`psql` は `default_transaction_read_only=on` の読み取り専用セッションで走り、書き込みは `apply <file> --confirm=<ファイル名>`（1トランザクション＋`_migrations` 台帳へ記録）と `dryrun`（BEGIN…ROLLBACK）と `write --confirm=write` だけに限定。接続先は `apps/web/.env.dbadmin`（gitignore 済・空欄テンプレを作成）の `DB_URL_PROD` / `DB_URL_DEV` で切り替える。S-1 の棚卸し SQL `scripts/db/audit/s1-anon-grants.sql`（スキーマ権限・テーブルごとの GRANT と RLS と policy 数・集計・関数 EXECUTE・シーケンス・DEFAULT PRIVILEGES・storage/auth）と手順書 `docs/development/db-access.md` を追加。
- 検証結果: 本番へは読み取りのみ（書き込みなし）。`apps/web/.env.local` の `SUPABASE_DB_URL` は別プロジェクト `wdbifbzwxivgefyxpzbi`（ap-south-1 pooler）を指したままで接続タイムアウト＝migration 118 以降適用できなかった原因を特定。本番 anon key（ref `ooirajiizydcynyglvuv`）で REST を叩くと 97 テーブル中 89 が 200（明示 REVOKE のある `org_record*` 4 件のみ 401）だが返る行数は 0（service_role では `roles` 8 行）。migration には RLS も policy も一切なし。dev Supabase（`wdbifbzwxivgefyxpzbi` / hakotora-dev）は REST も DB も生きており、Vercel 側のパスワードが古いだけ（`password authentication failed`）＝「dev は存在しない」という従来のメモは誤り。
- 残課題: `apps/web/.env.dbadmin` の `DB_URL_PROD` が未記入のため棚卸し SQL は未実行。埋まり次第 `scripts/db/db.sh qf scripts/db/audit/s1-anon-grants.sql` を流し、anon が 0 行なのが RLS によるものか別要因かを確定して `docs/security/` に保存 → REVOKE の migration を書く。本番 Vercel（nippo-ace）の env に `SUPABASE_DB_URL` があるかの確認は権限の分類器にブロックされた。commit・push・デプロイは未実施。

## 2026-09-08 11:40 S-1 anon 権限の棚卸しを本番で実施し REVOKE migration 159 を起草

- 変更内容: `docs/security/2026-09-08-s1-anon-grants.md`（棚卸し結果）と `supabase/migrations/159_revoke_anon_authenticated_grants.sql` を新規作成。159 は public の全テーブル・シーケンス・ルーチンから anon / authenticated を REVOKE し、関数は PUBLIC 経由の EXECUTE も剥がす（public に拡張は無く extensions スキーマに分かれているため巻き込み無し）。DEFAULT PRIVILEGES（public・storage）も取り消して今後の新規テーブルに権限が付かないようにし、storage は所有者の都合で失敗しうるため DO ブロックで握る。service_role の権限と public の USAGE は維持。駐車の migration は 160 へずらす前提。
- 検証結果: 本番 `ooirajiizydcynyglvuv`（PG 17.6）へ読み取り専用で接続し棚卸しを実行。public 94 テーブルのうち **90 で anon / authenticated が SELECT/INSERT/UPDATE/DELETE/TRUNCATE を保持**、一方で 90 すべて RLS 有効・policy 0 本。RLS を有効化しているのは migration ではなくイベントトリガ `rls_auto_enable`（public への CREATE TABLE で自動 enable）。RLS を迂回するビューは 0（94 個すべて通常テーブル）。Storage は 7 バケットすべて非公開・policy 0 本だが anon に全 DML 権限。関数 6 本が anon から EXECUTE 可能（5 本は SECURITY INVOKER）。dev（`wdbifbzwxivgefyxpzbi`・81 テーブル）へも接続確認。
- 残課題: 159 は **dev・本番とも未適用**（dev への適用コマンドが権限の分類器にブロックされたため、ユーザーの許可待ち）。適用後は棚卸し SQL の集計で anon_can_select 90→0・anon_blocked 4→94 を確認し、日報送信・シフト表・地図・請求書を本番で一巡する。Vercel の `SUPABASE_DB_URL`（hakotora-dev）は実行時未使用だが古い値が残っている。Vercel の Needs Attention は Sensitive 保存への移行提案で、`JWT_SECRET` のローテートは全ユーザー即ログアウトになるため値を変えずに再登録する方針。legacy anon/service_role キーから新形式（sb_publishable / sb_secret）への移行は別タスク。commit・push は未実施。

## 2026-09-08 12:10 migration 159（anon 権限の剥奪）を dev で適用・検証

- 変更内容: `159_revoke_anon_authenticated_grants.sql` に `SET LOCAL lock_timeout = '3s'`（REVOKE の ACCESS EXCLUSIVE ロックが長いクエリと衝突したら待たずに中止）と、supabase_admin 由来の DEFAULT PRIVILEGES は postgres では取り消せないという既知の残りをコメントで追記。`docs/security/2026-09-08-s1-anon-grants.md` に dev の適用前後の比較表を追加。
- 検証結果: dev（`wdbifbzwxivgefyxpzbi`・81 テーブル）へ適用。anon が SELECT できるテーブル 77→**0**、anon が EXECUTE できる public 関数 3→**0**、postgres 由来の anon 付き DEFAULT PRIVILEGES 6→**0**。`set role anon; select count(*) from drivers` は **permission denied** になり、`set role service_role` では従来どおり 5 行を返す（アプリ経路は無影響）。public スキーマの USAGE は維持。storage は `buckets_analytics` の一部列で `no privileges could be revoked` の WARNING が出るが `objects` / `buckets` の REVOKE は成功。`ALTER DEFAULT PRIVILEGES FOR ROLE supabase_admin` は `permission denied to change default privileges` で不可（dev・本番の両方に存在）。
- 残課題: **本番への適用はユーザー確認待ち**（適用時間帯の選択を含む）。適用後は棚卸し SQL で anon_can_select 90→0・anon_blocked 4→94 を確認し、日報送信・シフト表・地図・請求書を一巡する。テーブルを追加する migration のあとは棚卸し SQL の再実行を運用に入れる。commit・push は未実施。

## 2026-09-08 12:25 migration 159 を本番へ適用（S-1 完了）

- 変更内容: 本番 Supabase `ooirajiizydcynyglvuv` へ `159_revoke_anon_authenticated_grants.sql` を1トランザクションで適用（`lock_timeout 3s`）。`_migrations` 台帳を本番に新規作成し 159 を記録。`docs/security/2026-09-08-s1-anon-grants.md` に本番の適用結果と回帰確認を追記。
- 検証結果: anon が SELECT できる public テーブル 90→**0**（95 テーブル全て遮断）、anon が EXECUTE できる関数 6→**0**、postgres 由来の anon 付き DEFAULT PRIVILEGES 6→**0**。anon の public スキーマ USAGE と service_role の SELECT/INSERT は維持。REST 実測で anon キーは `401 permission denied for table drivers`（適用前は `200 []`）、service_role は drivers 34 / daily_reports 614 / vehicles 17 行を従来どおり返す。本番アプリは `/login` `/join` が 200、`POST /api/auth/login` に存在しないドライバーコードを送ると 401 と「見つかりませんでした」＝service_role 経由の DB 照会が正常。書き込みは一切していない。
- 残課題: `supabase_admin` の DEFAULT PRIVILEGES は postgres では取り消せないため、テーブル追加 migration の後に棚卸し SQL を流す運用にする。legacy anon/service_role キーから publishable/secret への移行は別タスク。作戦盤アーティファクトの S-1 を完了に更新していない。commit・push は未実施。次は着手順 2 の地図 段階1（K-1〜K-6）。

## 2026-09-08 15:40 S-1 をコミットし、作戦盤ページを「次の一手」中心に作り直し

- 変更内容: S-1 の成果（migration 159・`scripts/db/db.sh`・棚卸し SQL・`docs/security/2026-09-08-s1-anon-grants.md`・`docs/development/db-access.md`・作業ログ）を main へコミット（eb41e42）。作戦盤アーティファクト（同 URL に republish）を再構成し、冒頭に「いま、次にやること」パネル（地図 段階1・最初の一手 4 つ・終わったと言える条件・直前に終わったこと）を新設。8 ステップの一覧は「済／いま／次／待ち」を左端に出す段組みに変更し、判断待ち 7 件を「決めれば動く」としてトラックより前へ移動。S-1 を完了、駐車の migration を 160 に、X-1/X-4 の記述を CLI から DB を読める前提へ更新。
- 検証結果: スタイル崩れの原因を特定して修正。凡例の `<span class="chip design">` が設計カード用の `.design{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr))}` と衝突し、チップが 280px のグリッドコンテナになっていた。カード側のクラスを `.notes` に改名し、状態表示は角丸ピルをやめて 9px の角丸スクエア＋テキスト（`.st.s-*`）に変更して幅で崩れない形にした。3 状態（light / dark / 未指定）のトークンを `:root` に全定義。
- 残課題: 作戦盤の見た目は実ブラウザ未確認（ユーザー側で確認）。次は着手順 2 の地図 段階1（K-1 → K-3 → K-4 → K-2 → K-5 → K-6）。K-3 は J-2 の判断が前提。

## 2026-09-08 16:10 島本さん7月請求書のマイナス（−3,173円）の原因調査

- 変更内容: 調査のみ。本番DBは読み取り専用セッションのみで、コード・データとも一切変更していない。
- 検証結果: 原因は **1便/2便（cycle）化と日報の cycle_no の不一致**。2026-08-22 に「Amazon　上鳥羽吉祥院」「豊中 Amazon」を uses_cycles にした際、`course_fixed_rates` が cycle_no=1/2 の行だけになり、**cycle_no=0 の行が消えた**。分割前の日報はすべて cycle_no=0 のため、`driverPayout.ts` の `fixedByCourse` が `course:cycle` → `course:0` の順で引いても両方ヒットせず、その日の報酬が 0 になる。島本さんの7月は6日のうち5日が旧コース（0円）で、残り1日が旧「Amazon上鳥羽吉祥院1便」コース（cycle 0 の単価が残っている）の 5,909 円のみ計上され、控除 9,082 円（事務手数料 4,000＋オイル交換 5,082）を引いて −3,173 円になっていた。分割前の1日単価は `course_rate_versions`（上鳥羽 2026-02-27 版・豊中 2026-03-15 版）と現在のセット単価 `course_fixed_rate_bundles` がいずれも税抜 11,818 円/売上 15,454 円で一致するため、正しくは 5日×11,818＝59,090 円が加算され、島本さんの7月は **55,917 円** が見込み。影響は `rate_snapshot` が無い 2026-02〜2026-07 の 176 日・9名で、報酬 2,079,968 円・売上 2,719,933 円が現時点の再計算で欠落する。2026-08 以降は日報に rate_snapshot が入っている（260 件中 259 件）ため影響なし。既発行の6月以前の請求書は 8/22 より前に作られており金額は正常（島本さん6月 145,087 円 approved）。**危ないのはこれから作る7月分**で、9名が該当する。
- 残課題: 直し方の判断待ち。案A＝`course_fixed_rates` に cycle_no=0 の行（11,818/15,454.55）を足す。案B＝`driverPayout.ts` のフォールバックを「cycle 0 の単価が無ければセット単価を使う」に変える（単価編集UIの保存で消えないぶん堅い）。案C＝過去日報に rate_snapshot を後埋め（根本的だが大量書き込み）。決定後に7月の請求書を作り直す。派生の課題として、2026-07 以前の承認済み日報 1,617 件すべてに rate_snapshot が無く、過去月の請求書が常に「現在の単価」で再計算される構造も残っている。

## 2026-09-08 19:20 便別コース化で 0 円になっていた分割前日報の報酬を全日単価で補う

- 変更内容: `apps/web/src/server/billing/driverPayout.ts` に、サイクル導入前（`cycle_no=0`）の日報がどの固定単価とも突合できなかったときの受け皿を追加。`data.fixedRateBundles` の全日（セット）単価を 1 日分として計上し、明細には「コース名（固定・全日）」の行を出す。適用条件は `requiredCycleNos` が 2 便以上・`fixedPayout` が非 0・支払方式が固定を含むこと。スナップショット側には同じ補完（`rateSnapshot.fixedBundle`）が既にあり、集計エンジン `compute.ts` の `fullDayUnits` も cycle_no=0 を全日 1 回として数えていたため、非スナップショット経路だけが欠けていた。
- 検証結果: 本番データに対する parity スクリプト（SELECT のみ）`npx tsx src/scripts/check-driver-payout.ts` で、修正前の 2026-07 は **11 pass / 9 fail**（吉田 ¥0 対 ¥141,816、島本 ¥5,909 対 ¥64,999 など）、修正後は **20 pass / 0 fail**。2026-02〜06・08・09 もすべて 0 fail（10/12/11/13/17/19/17 pass）。島本さんの 7 月は報酬 ¥64,999 となり、控除 ¥9,082 を引いて請求書は **¥55,917** になる見込み。`tsc --noEmit` 通過、`src/server` の 32 ファイル 300 テスト通過。
- 残課題: **未 push**（push すると本番へデプロイされる）。デプロイ後に 7 月の請求書を作り直す必要がある（該当 9 名。島本さんは −3,173 円の下書きが 4 本残っている）。`computeDriverAutoPayout` 自体の単体テストは無く、回帰は parity スクリプトで担保している。2026-07 以前の承認済み日報 1,617 件に `rate_snapshot` が無く、過去月が常に現在の単価で再計算される構造は未解決（案C）。

## 2026-09-08 19:35 報酬計算の修正を本番へデプロイ

- 変更内容: `f899b01` を main へ push。Vercel nippo-ace の本番デプロイ `dpl_4xY5Ad53CgKdCtJeKfYj7Suiwdnu` が READY になり、hakotora.jp に反映済み（ビルド約 1 分40 秒・turbopack）。
- 検証結果: `/login` `/join` とも 200、`POST /api/auth/login` に存在しないドライバーコードを送ると 401 と「見つかりませんでした」でサーバー経路も正常。
- 残課題: 7 月の請求書の再作成はユーザー側で実施（該当 9 名。島本さんは −3,173 円の下書きが 4 本残っている）。6 月以前の既発行分は作り直すと現在の単価で再計算されるため触らない。`computeDriverAutoPayout` の単体テストと、2026-07 以前の日報 1,617 件への `rate_snapshot` 後埋めは未着手。

## 2026-09-08 20:35 作戦盤の折返し崩れを修正し、単価スナップショット後埋めを X-9 として追加

- 変更内容: 作戦盤アーティファクト（同 URL に republish）。「最初の一手」の説明文が 1 文字ずつ縦に折り返していた不具合を修正 — `.steps li` が 22px＋1fr の 2 列グリッドなのに子要素が 3 つ（`::before` の連番・`<b>` 見出し・`<span>` 説明）あり、3 つ目の説明が次の行の 22px カラムへ回り込んでいた。`.steps li > b, .steps li > span{grid-column:2}` で明示的に 2 列目へ置き、説明に `max-width:52ch` を付けた。あわせて X トラックに **X-9「過去日報への単価スナップショット後埋め」**（2026-07 以前の承認済み日報 1,617 件に `rate_snapshot` が無く、過去月が常に現在の単価で再計算される。9/8 の請求書マイナス事故の根本原因）を追加し、「直前に終わったこと」に報酬計算の修正とデプロイ（f899b01）を追記。レールの X の件数を 8 → 9 に更新。
- 検証結果: アーティファクトの publish は成功。見た目はユーザー側で確認。
- 残課題: 7 月の請求書の再作成はユーザーが実施中（該当 9 名）。次は着手順 2 の地図 段階1（K-1 → K-3 → K-4 → K-2 → K-5 → K-6）。K-3 は J-2 の判断が前提。

## 2026-09-08 21:05 駐車自動特定の設計書の migration 番号を 160 に修正

- 変更内容: 別セッションが S-1 で migration 159 を使ったため、`docs/design/mobile-parking-auto-detect.md` の「migration 159・候補」を 160 に改め、`docs/roadmap-2026-09.md` §D の着手順に S-1 完了を反映。作戦盤ページ（アーティファクト）は別セッション側の版（S-1 完了・X-9 追加・次の一手＝地図 段階1）を正とし、このセッションからは再公開しない。
- 検証結果: 文書のみ。
- 残課題: 地図 段階1（K-1〜K-6）から着手。判断待ち J-1〜J-7。

## 2026-09-08 21:20 地図 段階1「見える」（K-1〜K-6）を実装

- 変更内容: 重なり順の規約を `apps/web/src/lib/map/zIndex.ts`（canvas 0 / 拠点 1 / ドット 2 / 札 5 / つまみ 6 / popup 30 / 操作UI 40 / モーダル 50）に集約し、マーカーと操作UIの z を全てここから引くようにした（K-1）。操作UIのラッパを `pointer-events-none` にし、押せる部品（button・input・白い面を持つ塊）だけ `pointer-events-auto` にして帯の余白を地図へ抜いた（P2-5）。「このエリアを再検索」を絶対配置から検索窓と同じ列へ移し、375px でツールバーを覆う問題を消した（P2-4）。375px は地図の +/− コントロールを避けるため列に `pr-9` を入れ、「共有」が下段へ折り返るようにした。`declutterPlates` の衝突判定を矩形リスト方式に変え、札（104×92 相当）と縮退ドット（直径12＋余白）の両方を置いた位置として突き合わせるようにし、ドットは `MAP_Z.vehicleDot` で札より下の層へ（K-2）。台数バッジは札の内側の左上へ移した。`vehiclePresentation.ts` に `modelVisibleAtZoom` / `targetLengthForZoom` を追加し、**z13 未満は車体モデルと足元リングを描かない**（J-2 の判断）。z13 で 40px、z15 以上で目標いっぱい、その間は線形（K-3）。拠点ピンを既定表示にし（K-4）、編集対象のマーカーだけ後から `setDraggable` と popup を切り替える effect を足した（P2-3）。モーダルの Esc・初期フォーカス・focus trap を `apps/web/src/lib/ui/dialog.ts` の `useModalKeys` に共通化して設定・移動登録へ適用し、車両 popup も Esc で閉じるようにし、車両マーカーに `aria-label`（例「480 り 10-00 稼働中」）を付けた（K-5）。検索ヒットの hover は CSS クラス（`.search-hit.is-active`）の切替に変え、hover のたびに全マーカーを作り直していたのをやめた（K-6）。プレビュー `MapOperationsPreview` にも同じズーム下限を反映。検証用に地図コンテナへ `data-map-zoom` / `data-vehicle-model-visible` を追加。
- 検証結果: 隔離プレビュー（`npm run preview:admin -- admin --port 3197 --mapbox` → `/preview/admin/map?scenario=large&role=admin`）を Playwright で実操作。**ズーム下限**: z13=40px・z14=64.1px・z15/16=88.2px でモデル表示あり、z12 以下は 0px でモデル非表示。**受け入れ基準4件**: large で札の中心クリック→束の popup が開く／検索窓のクリックで最前面が INPUT でフォーカスが入る／z11 で市名（箕面市・池田市・宝塚市・伊丹市・尼崎市・大阪市）が読める／375px の帯の空白の最前面が canvas。**縮退**: z11 で札1・ドット39・札z=5・ドットz=2・札とドットの重なり0件・「40台」バッジは札の内側で札からのはみ出し0。**モーダル**: 設定モーダルが `role="dialog"` で開きフォーカスが中に入り Esc で閉じる、車両 popup も Esc で閉じる。**拠点ピン**: 既定で3件表示。tsc 通過、地図関連 26 テスト通過、本番ビルド成功。
- 残課題: 束ね距離は広域で 83px 固定にした（車体を描かないと見かけの車両長が 0 になり束が作れないため）。「このエリアを再検索」は PC でも中央ではなく検索窓と同じ列（左寄せ）になった。実際に検索してボタンを出した状態での重なり確認はプレビューのジオコーディングが返らず未実施（構造上ツールバーとは別の行に入る）。runner の console に出る Mapbox の meshopt wasm と CSP のエラーは以前からのもの（X-7）。全体テストを並列で流すと `LeaseAdminPreview` と shifts の重い DOM テストが 5 秒タイムアウトで落ちることがあるが、単独では通る（今回の変更とは無関係）。commit のみで push・デプロイは未実施。

## 2026-09-08 21:35 地図 段階1 を本番へデプロイ

- 変更内容: `2530e07` を main へ push。Vercel nippo-ace の本番デプロイ `dpl_8bnwmbcaMw8g1JGXbgpdLjvtpq81` が READY になり hakotora.jp へ反映。
- 検証結果: `/login` `/join` `/admin/map` がいずれも 200。`POST /api/auth/login` に存在しないドライバーコードを送ると 401 と「見つかりませんでした」でサーバー経路も正常。
- 残課題: 実運用での見え方（実データ・実機幅）の確認はユーザー側。「このエリアを再検索」が PC でも検索窓と同じ列（左寄せ）になった点は、中央に戻すかどうかの判断待ち。次は着手順 3 の S-7（Passkey 応答の一回性）。地図は段階2「探せる」（K-7）が続き。

## 2026-09-08 22:45 地図 段階2「探せる」（K-7）: 検索窓で車両・ドライバーも引く

- 変更内容: `apps/web/src/lib/map/vehicleSearch.ts` を新規作成し、車番・地名・かな・車種・ドライバー名の照合を純粋関数に切り出した（全角と区切りを無視、4桁の完全一致→前方一致→部分一致→文字の前方一致→部分一致の順で並べ、同じ強さなら位置がある車を先に出す）。分類番号（480 等）は完全一致のときだけ拾う。地図の検索窓は1つのまま、上段「車両・ドライバー」（プレート＋メーカー車種＋状態/ドライバー名、位置なしは「位置なし — 選ぶと地図に置けます」）と下段「住所・施設」（既存の Mapbox）の2系統を返す。車両を選ぶと z16 まで寄せて移動完了後に詳細 popup を開き、位置が無い車は既存の `pendingPlaceVehicle`（地図をクリックして置く）へ入る。種別のショートカット（ガソリン等）は検索窓にフォーカスがある間だけ出すようにして、常設の帯をやめた。プレースホルダを「車番・ドライバー・住所で探す」に変更。
- 検証結果: 照合の単体テスト 9 件を追加（`vehicleSearch.test.ts`）。隔離プレビュー（`--mapbox`）を Playwright で 1440px と 375px の両方で操作し、フォーカス前はショートカットが出ないこと、「1000」で該当車1件、「大阪」で6件、選択で z14→z16 に寄って詳細が開き入力欄が空になること、`scenario=empty` で位置なし車を選ぶと「地図をクリックすると 480 り 12-01 をそこに置きます」が出ることを確認。分類番号の前方一致で「48」が 480 の車を全部拾う問題をプレビューで見つけ、完全一致のみに絞った。tsc 通過、地図関連 35 テスト通過、本番ビルド成功。
- 残課題: 車両ヒットの上限は 6 件（それ以上は絞り込みを促す表示を出していない）。ドライバー名は位置情報に紐づく `driverName` のみで、シフト上の担当者からは引けない。段階3（札の直接ドラッグ・J-1）は未着手。commit のみで push・デプロイは未実施。

## 2026-09-08 23:05 ナンバープレートの長押しで車両の詳細を出す（運営の全ページ）

- 変更内容: 地図の吹き出し専用だった詳細を `apps/web/src/lib/components/VehicleDetailCard.tsx` に切り出し、地図はそれを使う薄い包みに変更（未使用になった整形関数とアイコンの import を削除）。`VehicleDetailSheet.tsx` に `VehicleDetailProvider` を新規作成し、`(admin)/layout.tsx` に設置。ドライバー側のレイアウトには置かないので、同じ `VehiclePlate` を使っていても長押しは効かない（見せる範囲を配置で決める）。長押しは document で `pointerdown` を capture し、`pointerType === "touch"` のときだけ祖先の `[data-vehicle-plate-id]` を辿る方式（500ms・10px 動いたら中止・直後の click は飲む・対応端末では 15ms の触覚）。PC のマウスは対象外（ユーザー判断 2026-09-08）。`VehiclePlate` のルートに `data-vehicle-plate-id` を付け、`pointer-events-none` を掛けているシフト表の2箇所は囲いの方に属性を置いた。車両1台分の詳細は新規 API `GET /api/admin/vehicles/[id]/detail`（`can_view_vehicles`・他社 ID は 404）。プレビューのシェル（`scripts/previews/admin.tsx`）にも同じ Provider を置き、車両 fixture に detail のルートを追加。
- 検証結果: 隔離プレビュー `/preview/admin/vehicles?scenario=normal&role=admin` を Playwright のタッチ端末（390×844・hasTouch）で操作。長押し対象のプレート 4 件を検出し、650ms の長押しでシートが開いて「ダイハツ ハイゼットカーゴ／佐藤 翔太／残り 2,662 km／149,030 km／2026年11月／9/8 20:02」を表示。150ms の短いタップでは開かない。pageerror 0 件。tsc 通過、本番ビルド成功。全体テストは 1052 件中 1050 件通過（落ちた 2 件は `LeaseAdminPreview` と shifts の 5 秒タイムアウトで、単独実行では 94 件すべて通過。並列時のみの既存の不安定さ）。
- 残課題: 実際に長押しを確認したのは車両一覧のみ（シフト表・日報・記録報告の fixture がまだ無い）。仕組みは属性＋Provider で共通なので同じはずだが、実機での確認は要る。ドライバー側へ広げるときは整備情報だけに絞る（ユーザー判断）。PC の操作は未実装。commit のみで push・デプロイは未実施。

## 2026-09-09 01:10 地図 段階3「動かせる」（K-8・K-9）: 札を直接ドラッグして確認シートで確定

- 変更内容: 通常表示のナンバー札をそのまま掴んで動かせるようにした（J-1「変える」で合意）。タッチは長押し 350ms、PC はドラッグ開始で掴む。掴んでいる間は掴んだ札を前へ出し、他の札を薄くし（`.vl-dragging`）、地図のパンを止める。動きと離しは `document` の capture で受ける（`setPointerCapture` は合成イベントや端末によって例外になり、ハンドラごと止まるため使わない）。離した位置は `apps/web/src/lib/map/dropSnap.ts`（新規・純粋関数）で拠点・区画へスナップし、確認シートに「豊中センター（A-2）に置きます」と出す。ボタンは「今ここにある」（`source=manual` で追記）と「ここへ運ぶ手配にする」（車は動かさず、車両と届け先を決めた状態で移動フォームを開く。`openMovementForm` に preset を追加）と「やめる」（掴む前の位置へ戻す）。拠点の外は「登録外の場所」で運ぶ手配は出さない。保存後は「元に戻す」のトーストを出し、直前の位置をもう1行追記して戻す（K-9）。手動配置の札には破線縁の「手動」バッジを出す。位置修正モードは「位置なし車両を置く」専用に縮小し、位置がある車のつまみの一斉表示を撤去（監査 P3-3・大量時に格子状に重なって選べなかった）。使われなくなった `map-drag-handle` の CSS・`plateShort`・`markersRef`・`popupRootsRef` を削除。履歴モードと閲覧のみでは掴めない。
- 検証結果: `dropSnap` の単体テスト 8 件を追加（既定半径 120m・円で登録した拠点はその半径・区画は 15m・他拠点の区画は拾わない・近い方を選ぶ）。隔離プレビュー（`--mapbox`）を Playwright で操作: PC 1440px で札をドラッグすると `.vl-grabbed` 1件・`.vl-dragging` が付き、拠点ピンの上で離すと「豊中センター に置きます」＋3ボタン、拠点の外なら「登録外の場所 に置きます（拠点の外）」＋2ボタン。「今ここにある」で保存され「480 り 43-03 を動かしました／元に戻す」のトーストが出て、押すとトーストが消える。保存後の札に「手動」バッジが1件付く。タッチ端末（390×844）では 450ms の長押しで掴めて確認シートが出て、150ms のタップでは何も起きない。役割を viewer にすると掴めずシートも出ない。pageerror 0 件。tsc 通過、地図関連 43 テスト通過、本番ビルド成功。
- 残課題: 長押しの秒数は 350ms 固定（設定は持たせていない）。ドロップ先の確認を省略する設定は持たない方針のまま。共有ビューへの手動配置の broadcast は後段。段階4（3Dモデルの面に実ナンバー・同一点の横ずらし）は未着手。commit のみで push・デプロイは未実施。

## 2026-09-09 09:10 地図 段階4の前半（K-11 同一点の横ずらし）と、K-10 の実現方法の見極め

- 変更内容: `apps/web/src/lib/map/spreadVehicles.ts` を新規作成（純粋関数・テスト7件）。同じ座標に重なった車を、記録は動かさず描画位置だけ 1.2 台分（4.1m）ずつ横へ並べる。ずらす向きは車の向き（区画の軸）に直角で、並び順は id 順に固定（再描画で入れ替わらない）。中心は元の座標に残す。地図の3Dモデルとナンバー札の両方に同じ表示座標を使う。プレビューの地図 fixture の normal シナリオに「同じ拠点の代表点に重なる2台（77-88 / 77-99）」を追加した。
- 検証結果: 単体テスト7件（離れている車はそのまま・3台を等間隔・向き0度で東西/90度で南北・id順で安定・しきい値外は別扱い・台数が増えても中心が動かない）。隔離プレビューで、重なっていた2台の札が同じ画面座標にならないことを確認（適用前は完全一致、適用後は 54px 離れる）。tsc 通過、地図関連 50 テスト通過、本番ビルド成功。
- 残課題: **見た目の効きは限定的**。車体は「地図の短辺の14%」という一定のピクセルサイズで描くため、4.1m の実距離はズーム z21〜22 まで寄らないと分離して見えない（それ以下では既存の束表示「N台」＋ドットが受け持つ）。設計 §3 の「区画があれば区画へ」は未実装で、実装すると「この車は A-2 にいる」と事実として読める表示になるためユーザー判断待ち。K-10（3Dモデルの面に実ナンバー）は生成方法の判断待ち（下記）。commit のみで push・デプロイは未実施。

## 2026-09-09 09:50 地図 段階4（K-10）: 3Dの車の面に実ナンバーを出す。車両登録時にサーバーが自動生成

- 変更内容: 車両ごとのナンバープレート GLB をサーバーで作り、**非公開の Supabase Storage バケット** `vehicle-plate-models` へ置く仕組みを追加した（実在のナンバーが入るファイルなので、public リポジトリの `apps/web/public/models` には置かない）。(1) `scripts/build-plate-blank.mjs` を新規作成し、車種モデルからプレート面だけを抜いた「無地の型」を 3 車種ぶん `apps/web/src/server/vehicles/plate-blanks/` に用意（21〜37KB。元の車種モデルは 474〜754KB あり関数へ同梱できないため）。材質名は車種で違う（アクティ「License Plate Front」／エブリイ・ハイゼット「Front Plate」）ので `/plate/i` で拾う。(2) `apps/web/src/server/vehicles/plateModel.ts` に番号テクスチャの描画（sharp・既存の SVG 字形と `vehiclePlateLayout.json` を流用）と型への貼り付け（gltf-transform）を実装。(3) `plateModelStorage.ts` に保存・削除・署名URLの一括発行。(4) 車両の作成（POST）と更新（PUT）で `after()` を使い、**応答を待たせずに裏で生成**する。更新は保存後の行を読み直して作るので、どの項目が変わったかを追う必要がない。(5) 地図 API が `plateModelUrls`（1時間の署名URL）を返し、地図は `vehicles-3d-plate` レイヤーを足して車ごとに `addModel` する。未生成・番号未入力の車は `model-id` が空になり従来どおり。(6) 既存車のための一括生成 `apps/web/src/scripts/backfill-plate-models.ts`（既定は確認のみ・`--apply` で実行）。`sharp` と `@gltf-transform/*` を apps/web の依存に追加し、字形と型が関数へ入るよう `next.config.mjs` に `outputFileTracingIncludes` を設定。
- 検証結果: サーバー側で「大阪 480 り 12-01」のテクスチャ（29KB）と 3 車種ぶんの GLB（51〜66KB）が生成できることを確認。本番へ backfill を適用し、**非公開バケットを作成して 14 台ぶん（計 898KB）を保存**。バケットは `public=false` で、公開URLへの直リンクは 400。service_role の署名URLからは 200 で取得でき、先頭 4 バイトが `glTF` で正しい GLB であることを確認。tsc 通過、`src/lib/map` と `src/server` の 339 テスト通過、本番ビルド成功。DB は読み取りのみで書き換えていない。
- 残課題: **実ブラウザでの見た目は未確認**（プレビューは署名URLを持たないため、本番デプロイ後に実機で確認する）。車両を削除したときの GLB の後始末（`removePlateModel`）は用意したが呼び出していない。近接（z17 以上）で札を消してモデルのナンバーだけにするかは実機で判断（設計 §3 段階4）。commit のみで push・デプロイは未実施。

## 2026-09-09 10:15 本番の地図APIを500にして復旧（sharp のネイティブ依存）

- 変更内容: 前項（段階4・K-10）のデプロイで `/api/admin/map/vehicles` が 500 になった。`plateModelStorage.ts` が生成側の `plateModel.ts`（sharp）を**静的 import** していたため、署名URLを配るだけの読み取り経路でも sharp が読み込まれ、Vercel の linux-x64 で `libvips-cpp.so.8.18.3: cannot open shared object file` に失敗していた。修正: 生成は `syncPlateModel` の中で動的 import に変え、読み取り経路は sharp に触らない。あわせて `next.config.mjs` の `serverExternalPackages` に `sharp` と `@gltf-transform/*` を指定し、ネイティブ依存を関数から参照できるようにした（`106c611`）。
- 検証結果: 修正のデプロイ後、`/api/admin/map/vehicles` は **500 → 401**（未認証の正しい応答）へ回復。`/login` `/join` `/admin/map` はいずれも 200。直近1時間のランタイムエラーは 0 件。生成が失敗しても `after()` の中で握って `{ok:false}` を返すため、車両の登録・更新自体は止まらない（プレートが既定のまま出るだけ）の設計であることも確認した。あわせて、本番の Storage にある**実物の GLB を2台ぶん署名URLで取得し、three.js で前後から描画**して番号の向きを検証（Playwright・スクラッチパッドの静的サーバー）。「京都 481 り 39-67」「京都 481 り 42-24」がいずれも前からも後ろからも正しく読めた（奥に見える反転した札は裏面が透けているだけで、地図では車体が隠す）。
- 残課題: **本番の地図画面でプレートが実際にどう見えるかは未確認**（ログインが要るため）。実機で開いて、車体越しに裏の札が透けないか・近接時の見え方を確認する。原因の教訓として、ネイティブ依存を持つモジュールは「使う関数の中で動的に読む」を既定にする。車両を登録したときに Vercel 上で実際に生成できるか（serverExternalPackages が効いているか）は、次に車両を登録したときにログで確認する。

## 2026-09-09 10:50 車両の登録・編集で車種（3Dモデル）と色を直せるようにする

- 変更内容: 車種の選択を**編集でも変更できる**ようにした。2026-08-10 に「実車の車種は変わらない」として登録後は表示のみにしていたが、登録時の入力ちがい（本番の 5972 はメーカー「三菱」・車種名「EV」で登録されており、ミニキャブなのに地図が標準モデルで描いていた）を直せなかったため、ユーザー依頼（2026-09-09）で戻した。カタログに無いメーカーで登録された車は、編集を開いた時点で「その他（自由入力）」を選んだ状態にする（選択肢に無いと空欄に見え、そのまま保存すると車種が消えるため）。あわせて `apps/web/src/lib/vehicleModels.ts` に `VEHICLE_MAP_MODEL_LABELS` と `mapModelLabelFor` を追加し、**どの3Dモデルで描かれるか**を名前で示すようにした（OEM は「エブリイ（クリッパー と同型）」のように元車種を添える）。当てはまるモデルが無いときは「標準の軽バン（エブリイ）で表示されます」と注意色（amber）で出す。車体色の選択と3Dプレビューは以前から編集でも使える。更新 API は元から manufacturer / brand / modelKey / modelCode を受け取るため、サーバー側の変更は不要だった。
- 検証結果: 隔離プレビュー `/preview/admin/vehicles?scenario=normal&role=admin` を Playwright で操作。編集を開くと「メーカー／車種／型式」の3つのセレクトがすべて操作可能（disabled でない）で、「登録後は変更できません」の文言が消え、「地図・アプリでは ハイゼット の3Dモデルで表示されます」が出る。メーカーを「その他（自由入力）」に変えると「この車種・世代の3Dモデルはまだ無いため、標準の軽バン（エブリイ）で表示されます」が注意色で出ることも確認。pageerror 0 件。tsc 通過、`src/lib` と `src/app/(admin)` の 335 テスト通過、本番ビルド成功。
- 残課題: 本番データの直し（5972=ミニキャブ、6318=ホンダなのに車種名がエブリィ、6186=車種名なし、3967/5601/5873/5991/6520 は model_key 未設定）は**画面から運営が直す想定**で、こちらでは触っていない。車種を直すと `after()` でプレート GLB も作り直される。ミニキャブ（U6 系）とアトレーの3Dモデルは keivan-3d で製作中のため、いまは標準モデルに落ちる。commit のみで push・デプロイは未実施。

## 2026-09-09 17:50 固定控除に「終わりの月」を持たせ、リース代の二重計上に気づけるようにする（A+B の A）

- 変更内容: 調査で、固定控除（`driver_fixed_expenses`）とリース契約（`driver_leases`）の両方から同じリース代が引かれ、月額リースの5名で **199,000円/月** が二重になっていることを確認した（本番データ）。原因は、経費ダイアログが固定控除を必ず `valid_to: null`（ずっと引く）で作り、終わりの月を指定する入口が無かったこと。API 側は元から `valid_to` を受け取れる。(1) 新規行の「期間」を「ずっと／終わりを決める」の選択＋月入力にし、保存時に月末日へ直して `valid_to` に入れる。(2) 既存の固定控除の期間セルを月入力にして、その場で終わりの月を決める・「ずっとに戻す」ができるようにした（PATCH `/api/admin/driver-expenses/[id]`）。(3) 名前に「リース」を含む固定控除があり、その人にリース契約もある場合、新規入力と既存行の両方に赤字で警告を出す。(4) 月末日の計算と月の判定を `apps/web/src/lib/expenses/fixedExpensePeriod.ts` に純粋関数として切り出した。プレースホルダの例示から「リース代」を外し「事務手数料」にした。
- 検証結果: `endOfMonthDate` などの単体テスト4件（31日・30日・2月・うるう年・不正な形は null）。tsc 通過、本番ビルド成功。**画面自体はプレビュー未確認**（payments の fixture がまだ無い・X-5）。データ整理は dry-run のみ実施し、書き込みはしていない: 固定「リース代」12件のうち10件はリース契約の開始前月で締められる（勝政・坂田・梶原・廣瀬は 〜2026-07、日笠は 〜2026-06、日額リースの5名は 〜2026-07）。木下・萩原はリース契約が無く保留。
- 残課題: **本番データの整理は未実施**（ユーザー確認待ち）。日笠は `valid_from` 2026-07-01 に対して締めが 2026-06 になり期間が逆転するため、行の削除が妥当。木下・萩原はリース契約を作るのか固定控除のままにするのかの判断が要る。日額リースの人が持つ固定「リース代」4,000円が何の費用かも未確認。payments 画面の fixture を作って実ブラウザで確認するのも残り。commit のみで push・デプロイは未実施。

## 2026-09-09 18:20 固定控除の期間UIを本番へデプロイ。日額リースの単価設定は保留

- 変更内容: `839dba5` を main へ push。Vercel の本番デプロイが READY になり hakotora.jp へ反映。
- 検証結果: `/login` `/admin/payments` が 200、地図API は 401（未認証の正しい応答）。直近30分のランタイムエラー 0 件。あわせて、発行済み請求書は `invoice_documents.payload` に控除明細ごと保存されていることを確認（日笠 2026-07 は「リース代 32,000／事務手数料手数料 4,000／ラジエーター 6,600／オイル交換　電球 5,808」）。固定控除の行を消しても発行済みの請求書は変わらない。木下・萩原はいずれも status=inactive（稼働終了 2026-08 / 2025-08）でユーザー判断により放置。日額リースの5名（吉田・川人・桑野・磯江・辻村）は**事務手数料の固定控除を持たず**「リース代 4,000円」1本だけで、コースの `daily_lease` は全38本が 0 円だった。
- 残課題: **本番データの整理は未実施**（ユーザー確認待ち）。日笠の固定「リース代」削除と、勝政・坂田・梶原・廣瀬の 〜2026-07 締めで月額リースの二重 199,000円/月 が止まる。日額リースの単価（2,000円/日）をコースへ設定する案は**ユーザー判断で保留**（日当がリース代を引いた額で決まっていること、C1だけ・C2だけのときに半額にする扱いが未整理のため、社内で運用を確認する）。payments 画面の fixture が無く実ブラウザ未確認（X-5）。

## 2026-09-09 18:35 経費ダイアログを直す: 月ピッカーの統一・名前と金額の編集・payments のプレビュー追加

- 変更内容: 前回の改修をユーザーが実画面で見て「ネイティブの月入力が英語表記で浮いている」「『ずっとに戻す』の意味が分からない」「名前の打ち間違い（事務手数料手数料）や金額を直せない」と指摘。(1) 期間を共通の `MonthYearPicker` に統一し、開始月・終了月ともその場で選べるようにした。終了月の枠には未選択時「ずっと」と出す。(2) `MonthYearPicker` に `onClear` / `size="sm"` / `className` を追加し、値の右に「×」を出して選択を消せるようにした（「ずっとに戻す」という別ボタンをやめた）。(3) 固定控除の**名前と金額をその場で編集**できるようにした（フォーカスを外したときだけ PATCH。金額は桁区切りで表示し、編集中だけカンマを外す。符号は変えない）。(4) 表の最小幅を 560px → 720px にし、狭い幅では横に送って読ませる。(5) `scripts/previews/fixtures/payments.ts` を新規作成し、支払い画面をプレビューに登録（固定控除の期間・名前・金額の編集と削除、臨時の追加を fixture 側で受ける）。
- 検証結果: 隔離プレビュー `/preview/admin/payments?scenario=normal&role=admin` を Playwright で PC(1280) とスマホ(390) の両方で操作。ネイティブの月入力は 0 個になり、期間は「2026年3月 〜 ずっと」の日本語表記。名前は「事務手数料手数料」を入力欄として編集でき、金額は「4,000」「35,000」と桁区切りで出る。リース契約と重なる行には赤字の警告が出る。390px では表が 836px あり、外枠が `overflow-x: auto` で横に送れることを実測（見えている幅 316px）。pageerror 0 件。tsc 通過、`src/lib` の 231 テスト通過、本番ビルド成功。
- 残課題: 390px では期間・金額・削除が初期表示で見えず、横に送る必要がある（表の作りは従来どおり。運営はPCが主なので当面このまま）。本番データの整理（日笠の削除＋4名の 〜2026-07 締め）は未実施でユーザー確認待ち。日額リースの単価設定は保留のまま。

## 2026-09-10 00:05 シフト表のプレートが消えた不具合と、長押し時の文字選択を修正

- 変更内容: 長押しの対象を示す `data-vehicle-plate-id` を付けるために、シフト表のプレートを `<span className="block">` で包んでいた。この span に幅が無いため flex アイテムとして潰れ、**スマホの運営画面でナンバープレートが消えていた**（すぐ上のコメントが「w-full が無いと幅が決まらずプレートが潰れる」と警告していたのと同じ踏み方）。包む要素をやめ、既にある囲い（`data-mobile-export-plate-*` を持つ span）へ属性を移した。長押し導入前との差分は属性の追加だけになり、DOM の構造は元どおり。あわせて `globals.css` に `[data-vehicle-plate-id]` と `.vehicle-label` の `-webkit-touch-callout: none` / `user-select: none` を追加し、**長押ししたときに iOS の文字選択とコピーのメニューが割り込まない**ようにした（プレートの文字は SVG 字形なので選択できなくても困らない）。
- 検証結果: 長押し導入前（05115ca）からのシフト表の差分が属性2箇所の追加のみであることを確認。隔離プレビュー（車両一覧・390px・タッチ端末）で `user-select: none` が効き、プレートが 166×83 で潰れずに出ることを確認。tsc 通過、シフトと `src/lib` の 236 テスト通過、本番ビルド成功。
- 残課題: **シフト表そのものはプレビューの fixture が無く実ブラウザで確認できていない**（X-5）。DOM の構造を元に戻しただけなので直っているはずだが、実機での確認をお願いする。`-webkit-touch-callout` は headless Chromium の getComputedStyle に出ないため、iOS 実機での確認が要る。commit のみで push・デプロイは未実施。

## 2026-09-10 00:20 長押しで開いた詳細シートの文字選択を止める（実機の指摘）

- 変更内容: 前項では長押しの対象（プレート・地図の札）だけ選択を止めていたが、実機では**開いたシートの見出し「車両の詳細」が選択され、コピーのメニューが被る**ことが分かった。長押しの指が残ったままシートが出るため、そこから選択が始まっていた。シートのラッパに `.vehicle-detail-sheet` を付けて `-webkit-touch-callout: none` / `user-select: none` を適用し、開く直前に `window.getSelection()?.removeAllRanges()` で選択を解くようにした。
- 検証結果: 隔離プレビュー（車両一覧・390px・タッチ端末）で 650ms の長押しからシートを開き、シートと見出しの `user-select` が `none`、開いた直後の選択文字が空であることを確認。tsc 通過、本番ビルド成功。
- 残課題: iOS 実機での確認は要る（`-webkit-touch-callout` は headless の getComputedStyle に出ない）。commit のみで push・デプロイは未実施。

## 2026-09-10 02:30 シフト表に「この予定ならこの日にオイル交換」の見込みを出す

- 変更内容: まず実データで前提を確かめた。日報のメーター入力は記録の 88% に入っており、メーターが減った記録・極端な飛びは 3% 未満、車両の `current_mileage` も日報と同期していて、コース別の1日あたり走行距離は 45〜160km と3倍の開きがある（＝全車平均では外れる）。これを踏まえて実装した。(1) `apps/web/src/lib/oil/oilForecast.ts` に純粋ロジックを追加。予定を日付順に積み、残り距離（前回交換 + 交換間隔 − 現在の走行距離）へ届いた最初の日を返す。同じ日に2便入っていればその日ぶんを足す。**予定が入っている範囲だけを見て、先へは伸ばさない**。既に残りが 0 以下の車は「予測」ではなく既存の警告の領分なので何も返さない。(2) `GET /api/admin/oil-forecast`（`can_view_shifts`）が、直近56日の日報のメーター差から**コース別・車別・全体**の1日あたり走行距離の中央値を返す。1日ぶんとして数えるのは前の記録の翌日だけ、0以下と 600km 超は捨てる、5件未満のコース・車は使わない。取得は `fetchAllRows` に `ORDER BY vehicle_id, report_date` を付けて行う。失敗しても空を返し、シフト表は動かす。(3) シフト表のセルに、届く見込みの日だけ**破線の輪郭の「オイル見込み」**を1回出す。既存の「もう交換時期」は塗りつぶしの丸のままで、**事実と見込みを見た目で分ける**（2026-09-10 ユーザー合意）。ホバーで「あと何稼働日 / 約何km」を出す。
- 検証結果: 単体テスト12件（コース別の距離を使う・2便の合算・日付が前後しても日付順・予定の範囲で届かなければ何も出さない・超過済みは出さない・EV/設定なしは null）。本番データ（読み取りのみ）で試算し、これからのシフト128件・車両17台に対して**印が出るのは1台だけ**（り6520・残り325km → 9月12日・あと3稼働日/約414km）、予定のある13台のうち11台は予定の範囲では届かず何も出ない、既に超過している車は0台であることを確認した。印が乱発されないことを実データで確かめた。プレートの潰れ（前回の不具合）を繰り返さないよう、Playwright で同じクラス構成を再現して実測し、変更前・変更後・枠つきのいずれも 88×44 で変わらないことを確認。tsc 通過、248 テスト通過、本番ビルド成功。
- 残課題: シフト表は fixture が無く本番ページのプレビューでは未確認（X-5）。表示している月の予定だけを見るため、届く日が翌月になる車は月を送るまで印が出ない。日報の配車画像（`captureDispatchImage`）にはこの印も入る（既存のオイル警告と同じ扱い）。

## 2026-09-10 02:45 未 push の3件をまとめて本番へ反映

- 変更内容: `f545017`（シフト表のプレート消え・長押しの文字選択）・`1f529ec`（詳細シートの文字選択）・`dc53f00`（オイル交換の見込み）を main へ push。
- 検証結果: 本番で `/api/admin/oil-forecast` が 404 → **401**（未認証の正しい応答）に変わり、新しいビルドの反映を確認。`/login` は 200。
- 残課題: スマホ実機で「シフト表のプレートが出ること」「長押しでコピーのメニューが出ないこと」の確認をお願いする。

## 2026-09-10 22:40 ペイメントはその月に稼働していた人だけ出す

- 変更内容: 支払い一覧は `drivers.status`（＝いまの状態）で絞っていたため、過去の月を見ても現在の稼働者が全員並び、逆に当時の在籍者かどうかは分からなかった。稼働開始月〜終了月（`active_from_month` / `active_until_month`・migration 100）で絞るように変更（請求書一覧が既に使っている考え方に揃えた）。(1) `apps/web/src/lib/drivers/activePeriod.ts` に `isActiveInMonth` を純粋関数として追加。開始月なし＝昔から在籍、終了月なし＝いまも在籍として扱う（どちらも未入力の人が実在するため、未入力を「稼働していない」と読まない）。(2) `/api/admin/payments` がこの判定で行を絞る。**ただし稼働期間の外でも報酬・控除が残っている人は消さずに出し、`outsideActivePeriod` の印を付ける**（稼働期間の入力漏れで支払いを見落とさないため）。(3) 画面の名前の右に小さく「稼働期間外」を出す（PC・スマホとも）。(4) 対象がいない月の文言を「ドライバーが登録されていません」→「この月に稼働していたドライバーはいません」に直した。(5) プレビューの fixture が `name` を返していて**氏名が空欄で表示されていた**のを `driverName` / `displayName` に直し、稼働期間外の行を1件足した。
- 検証結果: `isActiveInMonth` の単体テスト5件（境界の月・年またぎ・未入力・壊れた値）。本番データで試算（読み取りのみ）: 33人中、表示は 2026-07 が28人・08 が30人・09 が30人。**稼働期間の外なのに金額が残っている人が実在し**、印つきで残る（2026-07: 山本〈月額リース〉・萩原〈固定4,000〉／2026-08: 萩原・西脇〈報酬〉／2026-09: 木下〈固定35,000+4,000〉・磯江・猪上・萩原〈各4,000〉）。とくに**西脇は稼働開始月が 2026-09 なのに 2026-08 の日報が12件**あり、期間だけで切ると8月の支払いが消えていた。隔離プレビュー `/preview/admin/payments?scenario=normal&role=admin` を PC(1280)・スマホ(390) で確認し、印が名前の右に出ること・pageerror 0 を確認。tsc 通過、248 テスト通過、本番ビルド成功。
- 残課題: **稼働期間の入力を直す必要がある**。西脇の開始月（2026-08 が正しいはず）、終了月が空のまま inactive になっている3名（江前・榮・上手）は毎月出続ける。木下・磯江・猪上・萩原に残っている固定控除は、前回からの持ち越しでユーザー確認待ちの整理対象。

## 2026-09-10 23:45 シフトメモ: 臨時の非稼働・希望休の反映・文字札の削除・日付選択の解除

- 変更内容: (1) **担当枠×日の「この日だけ休み／稼働」**を追加。曜日の設定（`activeWeekdays`）に対する例外として持ち、右パネルの担当枠ごとの「この日を休みに／この日を稼働に」で切り替える。盤では臨時の指定を破線の囲みで示し、右上の文字が「非稼働」（曜日どおり）と「臨時休」（その日だけ）で分かれる。曜日の設定に戻る操作では例外ごと捨てる。判定は `apps/web/src/lib/shiftMemo/board.ts` に純粋関数として置いた。(2) **希望休をシフトメモへ反映**（これまで未反映だった）。運営シフト画面が既に読んでいる `shift_requests` を盤へ渡し、全休の希望を出している人は名前札に赤い「休」を付け、その日の配置に置いてある札にも印を出す。右パネルに「この日の希望休」の一覧を出す。便指定の休み希望は正式シフト表と同じく印にしない。置くこと自体は止めない（メモは下書きのため）。(3) **自分で作った文字札を削除**できるようにした（札の右の×。置いてある札はそのまま残す）。(4) **表の外を押すと日付の選択が外れる**ようにした（選択中の列の青い枠が残らない）。選択が無いときは右パネルに名前札だけを出す。あわせて**書き出した画像に青い枠が写らない**ようにした（選択中のセル・見出しの色を書き出し用の複製から外す）。(5) プレビュー `/preview/shift-memo` は盤を丸ごと複製した1,508行のモックだったので、**本番のコンポーネントを架空データで動かす薄いページ**に置き換えた（`storageNamespace` で保存先を分け、実際の下書きは汚さない）。`previewLogic.ts` とそのテストは複製専用だったため削除。(6) `npm run preview:admin -- shifts` のモックに希望休を数件足した。
- 検証結果: `board.ts` の単体テスト10件（例外の重ね方・元に戻す・曜日設定を変えたあとの残り・全休だけを拾う）。本番シフト画面のプレビュー（`preview:admin -- shifts`）を Playwright で PC(1440)・スマホ(390) の両方で操作し、臨時休・臨時稼働・希望休の印と一覧・文字札の追加と削除・表外クリックでの選択解除がすべて動くこと、pageerror 0、スマホで横スクロールが出ないことを確認。臨時稼働の札が右上の「あと5」と重なったため、札をやめてセルの破線囲みに直した（画像で確認）。tsc 通過、**全1,086テスト通過**、本番ビルド成功。
- あわせて修正: 車両の作成・更新で `after()` を直接呼んでいたため、リクエストの文脈が無い単体テストで**保存自体が 500 になっていた**（2026-09-09 のプレート GLB 自動生成で入り込んだ。本番の実リクエストでは動く）。`src/server/afterSafely.ts` で受け止め、後片付けの登録に失敗しても保存の成否を変えないようにした。これで既存の2件の失敗テストが通る。
- 残課題: シフトメモの臨時休は**この端末の下書きだけ**で、正式シフトには反映しない（メモの位置づけどおり）。希望休は印を出すだけで、置くこと自体は止めていない。シフト自動作成との関係は未着手（下記の提案をユーザー判断待ち）。

## 2026-09-11 00:45 シフトメモ: 休みの切り替えをセル上へ、コース単位の並び替えと非表示

- 変更内容: 前項の「この日だけ休み」は**日付を選んでから右パネルで担当枠を選ぶ**順序だったが、現場の考え方は**コースありき→その日を休みにする**なので、盤のセルの上で直接切り替えられるようにした。(1) セルの左上に「休み」／「稼働」の小さなボタンを置き、押すとその担当枠のその日だけを切り替える（日付の選択は変えない）。ホバーで出し、ホバーの無い端末では薄く常時出す（見えない当たり判定を作らない）。書き出した画像には入れない。右パネルの切り替えはタッチでの操作用に残した。(2) **コース単位の並び替えと非表示**を追加。コースの見出しに「一番上へ」と「このコースを非表示」を置いた。運用しているのが一部のコースだけなので、よく使うコースを押して上に積んでいけば並べ替えられる（狭い表でのドラッグを避けた）。非表示にしたコースは既存の「非表示 N件」バーに札で並び、押すと戻る。「すべて表示へ戻す」はコースも戻す。並び順・非表示はこの端末に保存する。
- 検証結果: 本番シフト画面のプレビュー（`preview:admin -- shifts`）を Playwright で確認。PC(1440): セルのボタンは通常 opacity 0 → ホバーで 1、押すと臨時休になり右パネルの見出し（＝選択中の日付）は変わらない、戻すと消える。コースは「北大阪を一番上へ」で並びが `北大阪・豊中・吹田` になり、「吹田を非表示」で盤から消えて非表示バーが 1件になり、札を押すと戻る。**再読み込みしても並び順と非表示が残る**ことを確認。スマホ(390): セルのボタンは opacity 0.6 で 32×20、タップで臨時休になる。pageerror 0。tsc 通過、全1,086テスト通過、本番ビルド成功。
- 残課題: コースの並びは「一番上へ」の積み上げのみ（1つ下げる・任意位置への移動は無し）。非表示を戻すのはバーを開いて札を押す2手。担当枠（町名）単位の並べ替えは従来どおりドラッグのまま。

## 2026-09-12 04:50 認証修正の本番DB更新と公開準備・次の着手順を整理

- 変更内容: Passkey再送防止と所属変更時の失効を実装し、本番へmigration 161→162を適用。ユーザー承認済みのWeb公開を準備。公開用には検証済みコードと技術記録だけをまとめ、個人別の監査メモとその履歴を含めない。
- 検証結果: Web全1,134テスト・Web/プレビュー型検査・本番向けビルド成功。開発DBで競合と期限境界・権限・世代変更を検証。本番では構造・権限・既存世代0を読み取り確認。コードは検証済みの作業ブランチと一致する。
- 残課題: Webの反映確認。公開先が既存の本番Git連携であることを確認し、送信内容を絞って公開操作を再審査する。次の順序は他社データ→非公開ファイル→請求書のHTML表示→強い本人確認→モバイル認証とPIN廃止。詳細は `docs/roadmap-2026-09.md` の冒頭と `docs/security/2026-09-12-auth-release.md`。

## 2026-09-12 07:45 他社データ・非公開ファイル・請求書住所を保護し、UI出力監査を標準化

- 変更: B-3/B-4/B-5を作業ブランチ`codex/tenant-files-invoices`へ実装（`a8e0484`）。他社IDの承認・却下と関連更新を拒否し、当日の正式な貸与車は維持。報告種別の会社分離migration 163、新規会社の初期種別、保存/署名/削除のファイル範囲検査、請求書住所の文字列描画と社印参照の統一を追加。
- プレビュー・監査: 請求書編集/印刷と報告種別の本番ページを隔離fixtureに追加。PC/768/390/320px、特殊文字、長文、取得失敗、保存失敗と復帰、報告種別の対象消失・削除・閲覧権限を確認。UI・PDF・画像の継続監査をAGENTSと開発手順へ追加し、ロードマップを更新。
- 検証: 全125ファイル・1,177テスト、TypeScript、本番ビルド、48テーブルのAST所属検査（指摘0）、差分検査を通過。全体テスト初回に既存UIテスト1件が時間超過したため並列数を2にして再実行し全通過。通常のcheck:tenantはsandboxのIPC制約があったため同じスクリプトをnode --import tsxで実行。
- DB・PDF: migration 163は開発DBで既存設定の複製・既定会社のID維持・会社内一意性・他社更新0件・再実行を確認後、全変更をROLLBACK。架空請求書のPDF4種類（計7ページ、約278〜305KB）を新規一時ブラウザで生成し、全ページを画像で確認。住所改行・特殊文字・明細1〜45の欠落なし。
- 残課題: 本番DB163・Web公開は未実施。npm auditの31パッケージ（critical 1/high 11/moderate 19）を確認し、Next.js等の依存更新を次の優先作業に設定。45行請求書の大きな余白・合計欄反復、閲覧役の編集UI、保存失敗時の再試行/印刷制御を監査項目化。QRの共通圧縮・待機処理、受託者控、シフトメモ/配車の補正・後片付けは次に検証する。過去の支払額・単価・実データは変更していない。


## 2026-09-12 15:34 Next.js・sharpの重大指摘を修正し、DB163とWeb公開の準備を完了

- 変更内容: Next.js 16.3.5、sharp 0.35.4（libheif 1.23.2）へ更新し、旧sharpの別コピーを解消。コミットb009d45。CLI送信前検査で独自名のDB接続ファイルの除外漏れを発見し、.vercelignoreで全環境ファイル・原本素材・エージェント設定を除外。秘密ファイルの転送前に修正済み。
- 検証結果: 全125ファイル・1,177テスト、本番ビルド/型、48テーブルの所属検査が成功。ログイン200、未認証管理API401、PNG最適化、Next.js画像処理関数で架空AVIF→WebP、番号PNGとGLB生成を確認。npm監査はcritical 0、Next.js/sharp指摘0（残りhigh 9/moderate 19）。
- DBと公開準備: 本番は4種別/2社・163未適用を読み取り確認。設定を権限600のローカル一時ファイルへ退避。開発DBで163の会社別複製・既定ID保持・他社更新0件・一意性・再実行を再検証しROLLBACK。対象APIの一時停止→300秒待機→DB→待機Web公開→停止解除の手順をdocs/deployment/tenant-scope-163.mdへ記録。
- 公開状況: Vercelの最初のソース転送がHTTP 500で失敗。再送は自動承認審査が「具体的なソース内容とVercel宛先への明示承認がない」として拒否。送信一覧1,199件・約58MB、コミットとnippo-ace/hakotora.jpを指定し追加確認を依頼。迂回送信していない。本番DB163/Firewall変更/Web公開/GitHub pushは未実施。
- 残課題: 追加承認後の本番切替・検証、残る依存関係の指摘整理、請求書の改ページ/権限/保存UI→QR等のPDF・画像処理統一。アプリ内ブラウザの請求書プレビュー表示を確認し維持（架空データ、実API/DBから隔離）。


## 2026-09-14 20:16 週次レポートを照合し、DB163とb009d45のWeb本番公開を完了

- 変更内容: ユーザーの具体的なVercel送信承認を受け、前回と完全一致の1,199ファイル（秘密ファイル除外済み）をnippo-aceへアーカイブ送信。Next.js 16.3.5・sharp 0.35.4を含むb009d45の待機デプロイを作成した。
- 本番DB: PCから5432番接続がタイムアウトするため、公式Supabase Management APIと既存CLI認証を使用。DB163をBEGIN/COMMIT・タイムアウト・台帳記録付きで適用し、各社4件/計8件、既定ID/全設定/日時保持、所属必須、一意性、一般権限0を読み取り検証。
- 本番公開: 対象5経路の一時停止→300秒以上の旧処理待機→DB163→ReadyのWeb昇格→一時停止解除を実施。デプロイdpl_3dNWNnNa6k7D3NUvdpkAeYfUwxzb、hakotora.jpのSHAがb009d45で一致。ログイン200、対象APIの通常401、画像最適化200を確認。一時ルール/下書きは残さず、診断が生成した保護用トークン1件も失効済み。
- 検証結果: 前回の125ファイル/1,177テスト・型・本番ビルドを通過したコードと一致し、今回Vercelの本番ビルドも成功。9月14日npm監査はcritical 0、Next.js/sharp指摘0（残りhigh 9/moderate 19）。実利用者の業務更新を試験していない。請求書プレビューをアプリ内ブラウザで確認・保持（架空データ/DB非接続）。
- 週次レポート: 32コミット・mobile差分なし・期間内作成PR/Issue0を確認。Actionsなしは誤りでPages公開処理あり（アプリCIとは別）。DB157も台帳未記録だが16列/4索引/3関数が存在し、本体はmigrationと一致。未適用と断定しないよう照合記録とロードマップを更新。会計の人数/金額は過去調査値であり、今回再計算・更新していない。
- 残課題: GitHub mainは72842f1のため公開内容の同期とアプリCI、残依存の指摘、請求書の改ページ/権限/保存UI→QR等のPDF・画像処理監査、Passkey追加削除時の本人確認、会計の最新照合。運用の記録はdocs/deployment/tenant-scope-163.md、週次照合はdocs/development/weekly-report-review-2026-09-14.md。

## 2026-09-14 20:50 型式別GLBと車検証写真/PDF登録の設計案を整理

- 依頼: GLBが増えてきたため型式登録の必要性と、車検証写真から車両登録する案を相談。
- 確認結果: 既存画面/APIに任意のmodel_codeがあるが、DA17V/DA64V等は同じ車種キーに解決され、地図の車両解決は型式を受け取らない。制作元keivan-3dの現行台帳は20仕様（全てin-progress）、ハコ虎の接続は3車種。世代別OEM・型式未確定の写真仕様も確認。
- 変更内容: docs/design/vehicle-registration-and-models-2026-09.mdへ設計案を追加。型式任意、原表記と照合値の分離、外観候補の確認、写真/記録事項PDF→確認修正→登録、既存キーとプレート再生成の扱いを整理。ロードマップにV-1〜V-4、索引に1行追加。既存の未コミット変更を保持。
- 根拠・検証: 車両ページ/fixture/API/モデル解決/プレート生成と制作台帳・manifestを読み取り照合。国交省・軽自動車検査協会の公式資料で電子券面に満了日が無いこと、記録事項PDFと構造化データの出力、車名欄と通称車種の違いを確認。文書差分・参照先を確認。文書のみのためアプリテストは実施せず。
- 残課題: 型式別GLBの取り込み、写真登録の隔離プレビュー、OCR選定・実書類検証、必要な保存欄と原本管理、既存車への反映は未実装。UI変更・画面プレビュー・DB変更・本番公開・コミットは行っていない。

## 2026-09-14 22:33 型式別GLB・車検証読取・部位別の外観設定を実装

- ユーザー指定の型式別GLB接続→登録プレビュー→写真読取を実装。制作元の版とSHAを固定し、20仕様を全体／塗装／固定／灯火／ボンネット／前後バンパー／プレート雛形の160 GLBへ取り込んだ。制作元は全件in-progressのままで変更なし。型式任意・接頭辞保持・未知型式の代替表示を維持し、手動外観と自動照合を区別した。
- 既存車両ページの実装とfixtureを再利用し、車体を大きく、色一覧を小さく整理。色は共通の明度／色相順。専用の外観詳細画面でボンネット・前後バンパーの別色と外観候補を選択でき、個別色解除・保存失敗後の再保存も対応した。
- 3Dの中心と回転を整理し、灯火材質の発光に統一。全体GLBとプレート雛形のUVを共通化し、編集プレビューの前後プレートにも入力番号を描く。地図側も同じ型式解決・分割部位を使い、表示対象だけ取得する。番号／モデル版ごとにStorageパスを分け、旧番号・旧型式のプレートを返さない。
- 写真／PDFはブラウザ内Tesseract.js・PDF.jsで実読取。原本と候補8項目を比較し、選択項目だけ既存フォームへ反映。自社ナンバーの重複確認を追加。原本・全文は外部OCR、API、Storage、ログへ送らない。表の位置順復元とDPI指定で、Canvas化した罫線写真の読み落としを修正した。
- 部位色用migration164とRPC許可フィールド更新を準備。共有の外部開発DB試験は自動承認レビューに拒否されたため、ネットワーク・マウント・実データのない一時ローカルPostgreSQLの最小スキーマで実SQLを検証し、ROLLBACKとコンテナ削除を完了。作成・更新・解除・色保持・他社拒否・不正色拒否・RPC権限・再適用が成功。開発／本番DBへは未適用。
- 検証：129ファイル1,197テスト、最終本番用ビルド、TypeScript、tenant-scope、差分検査、20仕様160GLB幾何検査が成功。アプリ内ブラウザでPC／スマホ390px、6状態、3役割、部位色とナンバー、写真／PDF、選択解除、新規登録、重複、保存・読取失敗復帰を確認。地図は車体表示を確認したが検索／一部タイルに一時的な通信失敗あり。
- 起動は `npm run preview:admin -- admin --port 3197 --mapbox`、`/preview/admin/vehicles?scenario=normal&role=admin`。仕様・読取サンプル・監査結果・制限・公開前の164適用と既存プレート再生成は `docs/development/vehicle-registration-preview.md` に記録。本番公開、既存車の再生成、車台番号／初度検査年月／燃料の取込、実書類の撮影条件別精度評価は未実施。

## 2026-09-14 22:38 シフトメモの日別必要人数を設計検討

- ユーザーの「必要人数を日によって例外的に変えたい。UIを複雑にせず検討から」の依頼に対し、現行の担当枠人数・日別休み／稼働・不足集計・端末内保存と、9月11日のコースから直接操作する方針を調査した。
- 現行シフトメモを3193の隔離プレビューで別タブに開き、PC1280px／スマホ390pxでセル・人数・右パネルを実見。76pxの列に常設＋／−を追加せず、既存の人数表示からその日だけの人数を編集する案を第一候補とした。例外日の補足、通常へ戻す、人数減でも配置札を消さない、休み／稼働との独立を整理。
- `docs/design/shift-memo-required-count-exceptions-2026-09.md` に3案の比較、保存・集計・画像出力への影響、0人の扱いとスマホ操作領域の検討事項を記録。今回は調査と設計案のみで、人数例外のUI・保存・集計・DB変更・出力検証・公開は未実施。車両プレビュー3197と既存の作業差分は保持。

## 2026-09-14 23:14 シフトメモの日別必要人数を実装

- ユーザーが設計案を承認し、担当枠×1日の必要人数を0〜10人で変更できるようにした。セルの「あと1」「2/2」と日別配置カードから同じパネルを開き、変更日だけ「必要3人」と表示する。
- `requiredCountOverrides` を従来の個人メモへ追加。通常人数・休み指定とは独立し、旧保存を読める。通常復帰はその日の人数だけを解除し、配置済みの名前札を減らさない。セル、日付別不足合計、不足一覧、日別配置、出力の計算を共通化した。
- 保存失敗時の内容維持と再試行、子ボタンのSpace操作で名前札を誤配置しない制御、人数パネルのフォーカス循環・Escape・復帰を追加／確認。
- 本番画面を再利用する `scripts/previews/shifts-services.tsx` に日別人数サンプルを追加。サンプル差替え時の画面重複は再読込で解消。PC1280px／スマホ390・320pxで人数変更、通常復帰、通常設定との独立性、半月切替、再読込、カードの44px操作、横はみ出しなしを確認。
- 全130ファイル・1,204テストと本番ビルド成功。最終変更後に関連17テストと型チェックも成功。PNG2660×3238pxとPDF1ページ・679,495バイトを実保存し、PDF描画とPNGで人数・不足数・名前札の一致を確認。`pngToPdf` を継続利用。
- 設計メモとプレビュー手順を更新。`http://127.0.0.1:3193/preview/shifts` を開いた状態で完了。スマホ実機タッチ・最小列幅56pxの実ドラッグ・実ブラウザの容量不足は未確認（保存失敗は自動テスト）。DB変更、正式シフトへの反映、本番公開は行っていない。

## 2026-09-14 23:23 シフトメモの日別必要人数を本番公開

- ユーザーの公開承認に基づき、公開済みb009d45から専用コピーを作り、シフトメモ変更をaa40e2d（`codex/shift-memo-daily-count-release`）へ記録。車両GLB・車検証読取・部位別色・DB164は未公開作業として元の作業ツリーに保持した。
- 公開用コードで126ファイル・1,184テスト成功。送信対象1,190ファイル・57,624,548バイトを検査し、環境値・接続設定・docs・請求書保管庫・mobile・他の未公開差分を除外。ローカル接続ツール設定 `.mcp.json` も送信除外へ追加した。
- 本番nippo-aceをCLIで明示し、ドメイン未切替の本番デプロイを作成。Vercelの依存インストール・Next.js 16.3.5ビルド・型検査が成功。旧本番が準備中に変わっていないことを確認後、promoteを実行した。
- hakotora.jpが `dpl_6qB7oq1YfBLLhNKSkJ6MArrHrt2Y`（Ready/production）を指すことを照合。ログイン/シフトページ200、未認証のシフト/車両API401、本番配信JSの人数設定・通常復帰・保存キーを確認した。DB変更・本番業務データの編集テスト・GitHub pushは行っていない。
- 記録は `docs/deployment/shift-memo-daily-count-2026-09-14.md`。以後の公開基点はaa40e2d。設計メモ・プレビュー手順を公開済みに更新した。

## 2026-09-15 00:47 豊中9月後半の照合とシフトメモからの正式反映・人数の即時更新

- 依頼: 添付PDFと本番豊中シフトの照合、メモから正式シフトへ反映。追加指定で日別人数の見出し・適用ボタンを撤去し、日付・コース名を大きく、＋／−即時更新と「通常:○人」・リセットへ。
- 照合: ユーザーが確定したACE4名だけを対象（吉田智＝吉田ト、島袋は対象外で島本とは別人）。9/16〜30の25人日、C1/C2各25件・計50件を本番DBの読み取り専用比較で確認し、差分0。添付題名の「前半」と表の日付「16〜30日」は区別した。氏名入り照合資料はfixtureに持ち込んでいない。
- 実装: メモから担当枠・期間・便を選び、追加／入れ替えの差分を見て確定。空白日は既定除外、未登録名は明示対応、同便同人は集約。メモは保持。保存後のシフト再取得失敗も区別する。APIは会社・実行者・権限を認証から決定。
- DB: migration 165で確認／確定RPCを追加。対象・在籍・便の検証、revisionと書込ロック、配置とログの同一トランザクション。変更なしの車両・時刻は保持、解除で配車情報もクリア。ログ失敗時は全体取消。同時に確定しても一方だけ保存する。本番への適用・業務データ更新・Web公開は未実施。
- プレビュー: 本番shiftsページを標準admin runnerでも直接再利用し、架空fixtureと通信スタブを追加。3215番 `/preview/admin/shifts`。PC1280・768・390・320px、通常／空／長い名前／多数／反映失敗／競合／名前対応／loading／error、管理者・経理・閲覧を必要箇所で監査。日別人数の即時更新とリセット、取消、保存、失敗復帰を確認。
- 検証: 全体133ファイル・1,217テスト、Web／preview型検査、tenant静的検査成功。network=noneの専用PostgreSQL 17.6でmigration・整合性・途中失敗全体取消・2接続競合を確認。ブラウザ警告・エラー0。生成PNGに必要人数・最終日・最終行が含まれることを実見。IABのダウンロード結果を取得できなかったため、PNG/PDF端末保存とPDF実ファイルの再検証は未完了。
- 手順・制限: `docs/design/shift-memo-reflect-2026-09.md`、`docs/development/preview-workflow.md`、日別人数設計書に記録。別作業の車両差分は保持し、今回分を公開済みと混同しない。

## 2026-09-15 00:51 シフト入力ミスを当日前に発見する仕組みを最優先残タスクへ追加

- 依頼: シフト入力の誤りを当日まで発見できない構造を残タスクにし、ほかの重大リスクも整理する。
- 変更: `docs/design/operational-risk-detection-2026-09.md` を追加し、ロードマップ冒頭へ記録。原本の版と照合、共有する日別必要人数、予定の版に対する本人確認、期限前の未解決一覧を完了条件付きで整理。
- 調査: 通知前は変更検知の基準がないこと、通知設定OFFで検知も止まること、通知作成/配信/既読と本人確認の違い、通常編集の後勝ち保存と履歴の保証不足、車両の同日重複許可、旧日報・現在設定に依存する過去金額再計算をコードから確認。単価履歴・snapshotは実装済みであり、古い資料の「履歴なし」を現状と誤認しない。
- 検証: 根拠コードと既存の修正・公開記録を照合。文書のみの変更につきアプリテストは再実行せず。現在の事故・誤支払・本番設定は再調査していない。
- 残課題: 確認期限・責任者・原本更新担当・例外運用を設計時に決め、独立照合・未確認の検知と各リスクの実装/回帰検証を進める。今回、新機能・業務データ・本番設定・通知送信は変更していない。

## 2026-09-17 01:39 iPadOS・ハコ虎 Baseを残タスクへ追加

- 依頼: ChatGPT会話「ハコ虎機能整理」を踏まえ、iPadOSの開発も残タスクに入れる。
- 変更: `docs/roadmap-2026-09.md` にBASE-1〜BASE-5を未着手として追加。現場運営用の別アプリ「ハコ虎 Base」、iPad推奨・スマホ対応、金銭処理/条件変更はWebという方針を記録し、業務範囲・構成/配布・隔離プレビュー・API/認可・実機検証へ分解。モバイル移行ロードマップから参照を追加。
- 優先順位: シフト入力ミスの事前検知と既存の公開・基盤の着手順を維持。Baseの初回範囲と着手時期は未確定。会話中の機能案や端末機能は一律の実装要件とせず、設計・端末確認で採否を決める。
- 検証: 参照会話の最新10ターンと残タスク文書を照合し、差分・リンク・ログ件数を確認。文書のみの変更につきアプリテストは実行せず。アプリ実装・プレビュー・本番変更・配布は未実施。

## 2026-09-17 01:50 公開済みコードのmain同期とアプリCIを準備

- 依頼: 提示された公開・基盤の順序に従い、まずGitHub main同期とアプリCIを進める。
- 照合: GitHub mainは72842f1、Vercel nippo-aceのreleaseCommitはaa40e2dでReady。mainと既存Git連携しているため、公開済みaa40e2dから専用作業ツリーとcodex/main-sync-ci-20260917を作成。
- 変更: 68e62bcでApp CI、Web/プレビュー/モバイル型と所属検査のnpm scripts、手順・同期文書を追加。公式Actionsをコミット固定、読み取りトークン、本番秘密値を使わないNode.js 24で実行。主作業ツリーにもCI設定・npm scripts・記録を反映し、未公開の車両/正式シフト反映は保持。
- 検証: 専用作業ツリーのlockfileからnpm ci。全126ファイル・1,184テスト、Web/プレビュー/モバイル型、48テーブルの会社境界、Node.js 24.21.0のWebビルド成功。workflow YAMLとdiffを確認。125ファイルの送信差分を列挙し、追加行の一般的な秘密値パターン検出0件。
- 未完: 公開GitHub TTH15/nippoへのpushが「具体的payloadと公開先への明示承認がない」と自動承認審査で拒否。ブランチ送信・GitHub上のCI・main同期・Vercelの再公開確認は未実施。承認後に68e62bcの送信から再開する。詳細は `docs/deployment/main-sync-ci-2026-09-17.md`。

## 2026-09-17 02:26 招待登録のPasskeyを半必須化し、新規ユーザーのPIN作成経路を閉じる

- 変更: Webの招待登録・途中再開・SMSログイン後はPasskey登録を主操作にし、設定できない場合は説明を開いてSMSで続行。成功・失敗・再試行・登録済みの分岐を共通化。ログインはPasskey/SMSを先頭、旧PINは補助導線へ移動。
- 変更: 招待ユーザーのマイページからPIN変更を非表示にし、本人PIN変更APIも既存ハッシュがある行だけ更新して新規発行を拒否。PIN未発行エラーをUIが一般エラーへ置換していた問題も修正。認証方針・ロードマップ・プレビュー手順を更新。
- 検証: 全138ファイル・1,239テスト（追加22件）、Web/プレビュー/モバイル型、48テーブル所属検査、Node.js 24の隔離した本番ビルド成功。全体の初回実行は既存UI2件が時間切れ、並列2で再実行して通過。PC1280・iPad幅768・スマホ390/320で新規・再開・非対応・失敗/再試行・SMS続行・PIN有無を確認。
- 残課題: 本番未公開。実SMS・OSのPasskey・実DBの認証は未検証。既存PIN認証/手動作成時の初期PIN/旧モバイル、Passkey追加・削除の直近本人確認は残る。GitHub同期の送信承認待ちとiPadOSの残タスクも維持。プレビュー: http://127.0.0.1:3199/preview/admin/onboarding?scenario=resumed 。詳細は docs/design/onboarding-passkey-2026-09.md。

## 2026-09-17 11:00 B-1〜B-8の残実装を検証し、日報画像提出・駐車検知の設計を追加

- 変更: B-1〜B-5を再確認。B-6のWeb/モバイルPIN UI・認証/変更API・新規発行を閉じる候補、HTTPS/ATS設定と移行ガード167、B-7の直近本人確認・鍵管理・原子的な本人通知RPC166・電話登録の迂回防止を実装。B-8はExpo57/React19を維持して依存更新、npm監査を26件から0件へ。
- 検証: 全1,256テスト成功を確認。最終並行実行の既存UI 5件のタイムアウトは該当2ファイル28件の単独再実行で成功。Web/プレビュー/モバイル型・48テーブルの所属検査・隔離Web本番ビルド・iOS/Androidエクスポート成功。専用開発DB/一時Postgresで権限・最後の鍵・同時削除・通知失敗の全体取消・PIN移行ガードを検証し、一時コンテナを撤去。
- UI: 本番ページを再利用した隔離プレビューでPC/スマホ/iPad幅、模擬SMS誤入力/再試行、鍵追加/削除、最後の鍵、確認手段なし、読込中/エラーを確認。管理CSPのnonce一致と画面起動を確認。操作と重なる常設説明を削除。
- 追加要望: 日報のスクショ原本・作成日時の取得元・受領日時・ハッシュ・既知形式OCR・本人確認/訂正履歴をRIMG-1〜5として設計。駐車は位置送信を先行し、モバイルで候補→通知→確認/写真へ。配送途中の通知抑制、区画後の左右撮影を強制しない点検分担、GPS区画の過信防止をPARK-M1〜5へ反映。Expo57の前景motion APIを公式資料/インストール済みコードで再確認。
- 残課題: 本番未公開・166/167未適用。active DRIVER 17所属中11所属に確認済みSMS/Passkeyがなく、先に本人の電話確認と旧モバイル移行が必要。EAS内部配布・実SMS/実端末Passkey・通知到達は未検証。CSP下の新PDF実保存と全管理画面の追加確認を残す。公開GitHub同期の送信承認待ちは継続。日報画像提出/駐車検知は設計のみで実装未着手。iPadOS/Baseの残タスクは維持。

## 2026-09-17 11:27 日報にSMS確認・Passkey登録の移行案内を追加

- 変更内容: 本番の日報 `/submit` に未完了の本人向け案内を追加。勤務予定・稼働実績で除外せず、その場でSMS確認→Passkey登録。入力を維持して日報提出を妨げず、設定済みは非表示。電話番号なし・取得/登録失敗の対処と再試行を用意。個別の案内は送信しない。
- 認証: 本人の状態を返すAPIはDBエラーを成功扱いせず、電話は下4桁のみ返却。SMSの番号省略時は保存済み本人番号に限定し、OTP検証と条件付き保存の成功後だけ同一セッション・5分の証明を返す。既存の再確認/登録処理を再利用し、証明を永続保存しない。
- 調査: 本番を読み取り専用で照合し、使用済み招待記録と所属を照合できたDRIVER 7名はSMS確認済み7・Passkey登録0（承認待ち1名を含む）。使用記録のない再参加や共有コードの全利用者を含む集計ではない。実名・電話番号・秘密情報をリポジトリに転記しない。
- 検証結果: 関連7ファイル42テスト、Web/隔離プレビューの型検査、48テーブル所属検査、プレビュービルド、差分の空白検査が成功。本番コードを再利用しPC1280/スマホ390・320pxでSMS誤入力→成功、鍵登録失敗→再試行、完了/番号なし/勤務なし/取得失敗→復帰を確認。個数42・距離50123の保持と日報提出を確認、320px横はみ出しなし・操作高44px・ブラウザ警告/エラーなし。
- 残課題: 本番未公開。RPC166とB-7/日報の案内を先に公開して本人の登録を確認し、PIN停止はその後とする。実SMS到達・OSのPasskey登録は未検証。プレビューは架空データ・通信隔離で `http://127.0.0.1:3221/preview/admin/submit?scenario=normal` に残した。起動/制限は docs/development/preview-workflow.md。

## 2026-09-17 18:36 残タスクを公開順に整理

- 変更内容: `docs/roadmap-2026-09.md` 冒頭に「直近の進行順」を追加。公開済みコードのmain同期、B-7/B-8/日報移行案内、B-6のPIN停止、シフトメモ正式反映、車両モデル/車検証読取、設計のみの業務事故検知/日報画像/駐車/Baseを分けた。
- 判断: 現在の作業ツリーには複数の未公開差分が混在しているため、dirty tree全体をそのまま本番へ出さない。まず送信承認待ちのmain同期、次にB-6を除いた認証強化の選別公開へ進む。
- 追加整理: [B-7/B-8・日報移行案内の先行公開計画](../deployment/b7-b8-login-setup-release-plan-2026-09-17.md) を追加。RPC166、reauth、Passkey管理、日報案内、依存更新を含め、SQL167、ログインPIN停止、mobile、車両/シフトを除外する境界を記録した。
- 残課題: 本番DB・GitHub・Vercel・EASへ影響する操作は未実施。次は公開基点からB-6を含まない差分を切り出し、対象テスト・型・tenant・ビルド・strict CSP下の帳票/地図/OCRを再確認する。

## 2026-09-17 21:57 業務事故の事前検知・日報の原本提出・座標だけの駐車申告を実装し、Baseの設計を起こす

- 業務事故の事前検知（O-1）: 日×コース×便の必要人数を配置と独立に持つ表（曜日の基準＋日付の指定）、本人の予定確認（版つき・追記ログ）、取り込み原本の抽出結果を migration 168 で追加。空白を「未入力／休み／人数未確定」に分け、未確定を 0 で埋めない。配置が1件も無い日でも不足が出る。判定は純粋関数 `server/shifts/readiness.ts` に置き、9種類（不足・休みの日に配置・人数未確定・基準未設定・対応不可・要再確認・未確認・原本と不一致・配車未完了）を状態から毎回導く。閉じた印を持たないので解決するまで消えない。
- 同（画面・API）: シフト表の上に未解決一覧 `ShiftReadinessPanel`、設定モーダルに「必要人数」タブ、本人のシフト確認タブに確認・対応不可を追加。API は `/api/admin/shifts/readiness`、`/api/admin/shifts/requirements`、`/api/me/shift-confirmations`。予定の版は日付・コース・便・集合場所・集合/着車/終業時刻から作り、車両は入れない（配車の入れ替えで再確認が形骸化するため）。古い画面からの確認は 409 で拒否。AI取り込みの適用時に読み取り全件を `extracted` へ保存し、原本照合が動くようにした。
- 日報の原本提出（RIMG-2）: migration 169 で `report_source_images` と読み取り・確認の履歴、非公開バケット `report-source-images` を追加。受領／画像の作成／ファイルの更新／対象営業日の4つの日時を別々に持ち、取得元が不明な作成日時は NULL のまま（CHECK で現在時刻の混入を防ぐ）。SHA-256 はサーバー計算で、同じ提出の再送は行を増やさず元の提出を返す。別日・別人への同じ原本は要確認にするだけで拒否しない。同じ提出枠で中身の違う画像を出し直した場合は新しい行を足して前を指す。API は `POST|GET /api/reports/source-images`。
- 駐車（PARK-M1のサーバー側）: migration 170 で `accuracy_m` / `detected_by` を追加し、申告の CHECK を「登録車庫・場所名・座標のいずれか」に緩めた。座標→拠点のスナップは誤差円が1つの拠点に収まるときだけ確定し、境界またぎ・複数該当・150m超の粗い測位・精度不明では確定しない。区画は座標から決めない。点の拠点の既定半径は運営の地図と同じ 120m に揃えた（設計の初期案60mからの変更）。
- ハコ虎 Base: BASE-1の分担案と BASE-2 の構成調査を `docs/design/hakotora-base-2026-09.md` に起こした。境界は「その場で起きた事実を記録・確認するのが Base、事実から金額や条件を決めるのが Web」。初回範囲は車両と鍵の受渡し1本を提案。`apps/base` は workspaces に自動で載るが、metro / babel / tsconfig / root の typecheck / `.vercelignore` への追記が要る。**アプリ実装・配布は未着手**で、初回範囲・識別子・共有端末の利用者切替・EAS・RNのプレビュー方式はユーザーの確定待ち。
- 検証: 全152ファイル・1,356テスト（追加48件）、Web/プレビュー/モバイルの型検査、54テーブルの所属検査、隔離プレビューのビルドが成功。**使い捨ての一時Postgresコンテナ**で migration 168/169/170 を2回連続で適用し、冪等性と CHECK の効き（working に人数必須・closed に人数を持たせない・曜日の基準に未確定を置けない・取得元unknownで作成日時を持てない・座標なしのdetected_by拒否・場所も座標も無い申告の拒否）を実測した。コンテナは撤去済み。
- UI監査: 隔離プレビュー `http://127.0.0.1:3197/preview/admin/shifts?scenario=readiness` を実ブラウザでPC1280/768/390/320pxと `prefers-reduced-motion` で2巡確認。1巡目は折り返し時に期限が単独行になる／PC幅で期限が本文から離れる／「要対応 0件」と出る／期限文字のコントラスト4.33:1／当日期限が他と同じ見た目、を修正。未再現だった状態は fixture にシナリオ（`readiness-light`＝期限切れなし、`readiness-many`＝40件・長いコース名）を足した。2巡目は必要人数タブで「未設定」の文字欠け、内部スクロールの手がかりなし、横スクロールで行見出しが消える、未保存の変更が確認なしで捨てられる、「休み」と人数が同じ見た目、Escape/role=dialog/tablist/scope の不足を修正した。3巡目で自分が入れた退行（`pr-4` を足したせいで1280pxでも「未設定」が欠ける。ネイティブの `select` の矢印は content box に重なって描かれ padding では避けられない）を見つけ、`appearance-none` ＋背景画像の矢印に変えた。あわせて確認ダイアログ内の Escape が効かない（document→window の二重処理）、フェードが初回しか計算されない、ドライバーの「確認」「対応できない」が取り消せない、確認ダイアログ表示中も背後がモーダル扱い、`ErrorDialog` に role が無い、を直した。`role=viewer` の必要人数タブは select を出さず値のテキストだけにして、説明文を足さずに状態を伝えるようにした。4巡目で Escape の対策が効いていない機序（React 19 が discrete event 内の setState を同期に流すため、document バブルで確認が閉じた直後に window リスナーが同じイベントの伝播中に張り直される）を突き止めた。capture 段階へ移す案は逆向きの往復（親が確認を開き、その確認が同じ Escape のバブルで自分を閉じる）を生んで**未保存時に Escape が完全に無効になる退行**を作ったため、層を変えて**内側のダイアログが Escape を消費する**形（`ConfirmDialog` / `ErrorDialog` で `stopPropagation`、親は window のバブルのまま）に直した。`ErrorDialog` は Escape で閉じられるようにもした。併せて、`lib/ui/dialog.ts` の `useModalKeys` が document の **capture** で `stopPropagation` するため上に載せた確認・エラーのダイアログへ Escape が届かない既存の食い違い（地図画面の設定パネル・車両移動フォームが該当）を、上位にダイアログがあるときは Escape と Tab を譲る形で直した。選び直し中の行に元の回答を薄字で残して「やめる」を追加し、`ErrorDialog` に初期フォーカスと復帰を入れ、select の文字を11pxにして余白を確保した。
- プレビューの見かた: `npm run preview:admin -- admin --port 3197` で `/preview/admin/shifts?scenario=<normal|readiness|readiness-light|readiness-many|empty>&role=<admin|viewer>`。ドライバー側の「予定の確認」は `/preview/admin/my-shifts?scenario=<normal|none|done|changed|save-error>` を新規に追加した（本番の `(user)/shifts` をそのまま再利用し、ナビ・本文幅も揃えた）。いずれも架空データ・通信隔離で、本番の認証・DB・API・通知には一切つながらない。`readiness` 系のシナリオはサーバーの重要度順ソートを再現していないため、グループ内の並びは本番と異なる。一覧は `docs/development/preview-workflow.md` にも追記した。
- 差分監査の反映: 1000行の切り詰め（ORDER BYなしのページング）、migration 170 未適用時に測位付き申告が日報の完了を止める、バケット作成漏れ、`extracted` を書く側が無い、運用初日に未確認が全員ぶん出る、削除の1件ずつ往復、同じ提出枠での出し直しが一意制約に当たる、を指摘どおり直した。
- 残課題: **migration 168/169/170 はいずれも本番未適用**で、本番の動作は変わっていない。適用前でも各APIは表が読めなければ `unavailable` を返す。解消期限（人数3日前・本人確認2日前・配車前日）は仮値で、実運用の締切・未確認時の連絡担当・正当な例外の記録方法は未決定。日付ごとの必要人数の編集画面、個人メモ盤のlocalStorageからの移行、RIMG-1（ヤマト配完個数表の実サンプル照合）・OCR・管理確認、駐車の検知/通知/撮影とモバイル送信は未着手。O-2の配信追跡とO-3の通常編集の競合検知も手を付けていない。

## 2026-09-18 02:35 ハコ虎 Base の入れ物を作り、未解決の解消期限を会社ごとの設定にする

- Base: `apps/base/` を新設。`apps/mobile` から metro / babel / tsconfig / NativeWind / safe-area の型拡張を写し、起動だけする空のアプリにした。bundleId `jp.hakotora.base`（ドライバー用 `jp.hakotora.app` とは別アプリなので同じ端末に両方入り、ログインも別々になる）、iPad の横向き・分割表示のため `orientation: "default"`、scheme `hakotora-base`。`.vercelignore` に `apps/base` を追加。業務機能は入れていない。
- Base の未了: workspace へ取り込むにはルートで一度 `npm install` が要る（`package-lock.json` が書き換わるため実行していない）。ルート `package.json` の `typecheck` への追加と `eas init` も未実施。理由と手順は `apps/base/README.md` に書いた。
- 解消期限: ユーザー判断により、仮値をそのまま既定値にして会社ごとに変更できるようにした。migration 171 で `shift_readiness_settings`（人数・原本／本人の確認／配車 の3区分 + 先読み日数）。種類ごとに9つ設定させず、手を打つ相手が同じものをまとめた。**先読みが一番長い期限より短いと期限が来る前に一覧へ出ない日ができる**ので、DBのCHECKと入力検証の両方で拒否する。設定が無い会社・171 未適用の環境は既定値（3/2/1日前・14日先）で動く。
- 画面: 「シフト提出の設定」に4つ目のタブ「未解決の期限」を追加。タブが4つになったので狭い幅では横スクロールにし、未保存の判定を必要人数タブと共有して、どちらかが未保存なら閉じる前に確認を出すようにした。
- 本番DBの確認（読み取りのみ）: ユーザーが migration 167（ドライバーのPIN全消去）を実行し、安全装置 `PIN retirement blocked` で中断した件を検証。**PINは消えていない**（active DRIVER 17名中13名に `drivers.pin_hash` が残存、`identities.pin_hash` は11名）。ブロックの理由は active DRIVER 17名のうち SMS確認済み6名・Passkey登録0名で、11名がどちらも持たないため。書き込みは一切していない。
- 検証: 全153ファイル・1,368テスト（追加12件）、Web/プレビュー/モバイルの型検査、55テーブルの所属検査が成功。使い捨ての一時Postgresで migration 171 を2回連続適用し、冪等性と CHECK（先読み < 期限を拒否・期限31日前を拒否・既定値は通る）を実測。コンテナは撤去済み。
- 残課題: **migration 168/169/170/171 はいずれも本番未適用**。167 は SMS確認/Passkey が全 active DRIVER に行き渡り、かつ SMS/Passkey ログインのアプリが本番に出るまで適用しない。未確認時の連絡担当・代替手段・正当な例外の記録方法は未決定。Base の初回機能（車両と鍵の受渡しを提案）・共有端末の利用者切替・EAS・RNのプレビュー方式もユーザー確定待ち。新しい設定タブの実ブラウザ監査は未実施。

## 2026-09-18 03:15 業務事故の事前検知を O-2 / O-3 まで広げ、日報の原本提出を画面までつなぐ

- O-3（通常編集の後勝ち）: `/api/admin/shifts` の POST を RPC `assign_shift_driver`（migration 172）へ移した。画面で見ていた値と DB の現在値を `FOR UPDATE` で照合し、違えば 409。変更ログを同じトランザクションで残し、便番号も記録する（従来は `void logShiftChange(...)` の投げっぱなしで、便を使うコースのログは全部 0 だった）。migration 未適用・EXECUTE 権限なしの環境は従来の無条件 upsert へ落ちる。
- O-3 の副作用対応: 同じセルへの保存を直列化（「外す→別の人を入れる」の到達順が入れ替わると誤 409 が出ていた）。D&D の連日コピーを便対応にし、有効な便が1つだけならそこへ、複数なら勝手に決めずスキップ。ドライバー解除時に個別時刻・取込元も消して 165 の空枠再利用に合わせた。**変更ログをベストエフォートから同一トランザクションへ方針反転した点は設計書に明記**。
- O-2（通知が届いたか）: 受信者ごとの記録に直した。`sendWebPush` の結果を**通知ごと**に返すようにし（受信者キーだと同じ人宛の複数通が1件に潰れ、1通しか送っていないのに全部「送信済み」になっていた）、LINE 未連携・会社で未設定・端末なしを `skipped` + 理由で残す。配信ログはチャネルごとに分けて insert する。`GET|POST /api/admin/notifications/undelivered` と通知画面の「届いていない」タブを追加。**再送するのは「送信に失敗」だけ**で、記録の無い過去の通知は再送しない（一括再送で古い通知が連打されるのを防ぐ）。
- リポジトリと本番の食い違いを1件修正: migration 108 の CHECK は `('line','push')` だが本番には `web_push` が追加済みで、新環境に 001〜を流すと配信ログの保存だけが静かに失敗する状態だった（migration 173 で本番に合わせた）。
- O-1 の残り: 未解決の解消期限を会社ごとの設定に（migration 171・既定値は従来の固定値と同じ）。設定モーダルに「未解決の期限」タブ、「必要人数」タブに「特定の日だけ変える」（人数未確定はここでしか作れない）を追加。
- RIMG-2: 日報に「配完表の画像」を追加し、原本をそのまま（圧縮・回転・切り抜きなしで）送れるようにした。管理側から署名URLで見る `GET /api/admin/reports/source-images` も追加。OCR（RIMG-3）と件数の確認（RIMG-4）は未着手。
- Base: `apps/base/` を新設（起動するだけの入れ物）。bundleId `jp.hakotora.base`、iPad 用に横向き許可。**ルートで `npm install` が必要なため未実行**（`package-lock.json` が書き換わる）。
- 検証: 155ファイル・1,383テスト（`--no-file-parallelism` で全成功。並列時に落ちる2件は既存の5秒タイムアウトで、単独実行では通る）、Web/プレビュー/モバイルの型検査、55テーブルの所属検査。使い捨ての一時Postgresで migration 168〜173 の適用・冪等性と、競合検知・制約・解除時のクリアを実測。**本番DBは読み取りのみ**（167 が PIN を消していないこと、配信ログの実態、shifts の実スキーマの確認）。
- UI監査・差分監査の反映: 実ブラウザ監査で7件、差分監査で深刻4件・中9件の指摘を受けて修正した。4タブすべてで未保存の確認、タブの折り返しと WAI-ARIA tab パターン、フォーカストラップ、数値入力の途中状態、viewer の readOnly 化など。
- 残課題: **migration 168〜173 はいずれも本番未適用**。167 は SMS確認/Passkey が全 active DRIVER に行き渡るまで適用しない（現状 17名中 SMS確認6名・Passkey 0名）。配車・時刻・取込の競合検知、RIMG-1（実サンプル照合）とOCR、駐車の検知/通知/撮影、Base の初回機能は未着手。再監査の結果待ち。

## 2026-09-18 03:25 差分監査の指摘を反映（自分の修正が作った退行5件を含む）

- 監査で、前の修正が作った新しい壊れ方が5件見つかった。(1) 未着一覧の取得上限を200→500へ上げたことで `.in()` がリポジトリ自身の安全上限（200件）を超え、通知の多い会社でタブごと失敗する。(2) D&Dコピーを「有効な便が1つだけなら複製」にしたため、便が2つ以上ある通常のコースでは**事実上コピーが消えていた**（しかもドラッグ元の便の情報は手元にあったのに捨てていた）。(3) 保存の直列化でキュー待ち中に「保存中」が出ず、未送信のまま離脱しうる。(4) 先行の保存が失敗すると後続が古い期待値のまま発火して二重にエラーが出る。(5) `import_batch_id` をクリアしたため「取り込みの取り消し」が手編集した行を消せなくなる。
- 直した内容: 配信ログ・ドライバー名の `.in()` を200分割＋`fetchAllRows`＋一意な order に。`dragSource` を `frames: {courseId, cycleNo}[]` に変えてドラッグ元の便へ複製。`setAutoSaving` の増分を enqueue 時へ移動。セルごとの generation で失敗後の待機分を破棄。`import_batch_id` のクリアを撤回し、「取込由来の空枠は165の再利用対象にならない」ことを設計書に明記。
- 配信経路の同じ罠も塞いだ: `push_subscriptions` と `identities` の `.in()` を200分割＋ページング。ここが切り詰められると、取得できなかった人が「通知の許可なし」「未連携」という**嘘の理由**で記録される。
- 再送の安全側: POST は送信直前に配信ログを見直し、`isResendable` を満たすものだけに絞る（古い一覧からの再送で既に届いた通知を送り直さない）。`refresh()` を `finally` へ移し、途中失敗でも送れたぶんは一覧から消す。`truncated` を件数ありでも表示。
- 検証: `--no-file-parallelism` で155ファイル・1,383テスト全成功、全ワークスペースの型検査、55テーブルの所属検査。使い捨ての一時Postgresで 171/172/173 の適用と、解除時に「個別時刻は消える・取込元は残る」ことを実測。本番DBは読み取りのみ。
- 残課題: **migration 168〜173 は本番未適用**。42501 フォールバック時に競合検知が無言で無効化される件（監視の当て先が未定）と、409 判別がサーバーの日本語メッセージの部分一致になっている件（`apiFetch` がボディを捨てる制約）は未対応。配車・時刻・取込の競合検知、RIMG-1とOCR、駐車の検知/通知/撮影、Base の初回機能も未着手。UI監査・差分監査とも再確認の結果待ち。

## 2026-09-18 03:32 差分監査の2巡目・3巡目を反映（配信経路の取りこぼしと偽の越境アラート）

- 2巡目で見つかった退行5件を直したあと、3巡目で新規4件。いずれも「取りこぼしが別の意味に化ける」種類だった。
  - `assertSameOrg` の `.in()` だけ200分割が抜けていた。ここで取りこぼすと「自社の人が見つからない」→**他社データが無いのに越境検出で全員への配信が止まる**。バッチごとの結果を合算してから判定するようにした。
  - 失効した購読の削除が重複込み・未分割・エラー未確認だった。機種変時期に200件を超えると削除がまるごと失敗し、しかも無言で毎回失敗し続ける。重複除去＋200分割＋エラーのログに。
  - 通知の upsert の `.select()` が `db-max-rows` で切り詰められ、1000人超の一斉配信で「保存はされたのに配信も配信ログも走らない通知」ができる状態だった。500件ずつに分割。
  - 破棄した保存が再取得を予約しておらず、楽観表示の巻き戻しが先行の `load()` 頼みだった。破棄側でも `scheduleRevalidate()` を呼ぶようにした。
- 表示の正直さ: `truncated` の文言が未着件数を出していたので、サーバーが `examined`（調べた件数）を返すように変更。再送で全件が対象外だったときに無反応にならないよう `skipped` を読んで伝える。D&Dコピーの完了文言を「N件コピーしました」→「N件を反映しています」に（保存は投げっぱなしで、言えるのは送った件数まで）。
- 設計書に「知っていて残していること」を追記: RPC の EXECUTE 権限が失われると競合検知が無言で無効化される点と、409 の判別がサーバーの日本語メッセージの部分一致になっている点（`apiFetch` が応答ボディを捨てるため、文言が実質 API 契約）。
- 検証: `--no-file-parallelism` で155ファイル・1,383テスト全成功、全ワークスペースの型検査、55テーブルの所属検査。
- 残課題: **migration 168〜173 は本番未適用**。配車・時刻・取込の競合検知、RIMG-1とOCR、駐車の検知/通知/撮影、Base の初回機能は未着手。UI監査の再確認と差分監査の最終確認は結果待ち。

## 2026-09-18 03:38 UI監査の指摘を反映（共通フックのフォーカス奪取と未保存の消失）

- 差分監査（O-2 / O-3）は**指摘なしで完了**。方針として残す2点（RPCのEXECUTE権限が失われると競合検知が無言で無効化される件、409判別がサーバーの日本語メッセージの部分一致になっている件）を設計書の「知っていて残していること」に追記した。
- UI監査で、自分の修正が **P1 を2件**作っていた。
  - **編集の2文字目が捨てられる**: フォーカストラップを `useModalKeys` に載せ替えた副作用。`onClose` を deps に入れていたため、`requestClose` が `dirty` で作り直されるたびに effect が破棄・再実行され、「閉じる前の要素へ戻す」→「中の先頭へ移す」でフォーカスが入力から奪われていた。飛び先がタブのボタンなので、反射的な Space でタブが切り替わる二次被害もあった。`onClose` を ref へ退避し deps を `[open, panelRef]` に。**共通フックなので地図画面等にも効く**。
  - **「特定の日だけ変える」の操作で曜日グリッドの未保存編集が消える**: 保存後の再取得で初期化が走っていた。未保存バッジも同時に消えるため失ったことに気づけない。初期化を1回だけにし、再取得では保存済みの基準だけ更新するようにした。
- そのほか: `FOCUSABLE` から `tabindex="-1"` を除外（roving tabindex と衝突して Shift+Tab がモーダル外へ抜けていた）。共有 `DatePicker` のポップオーバーに `max-w-[calc(100vw-1rem)]` と `collisionPadding`（320pxで土曜と翌月送りが画面外だった・**共有部品なので他画面にも効く**）。例外一覧を狭い幅で2行に。日付表記を `M月d日(E)` にそろえ、翌年以降は年を付ける。`toDate` で取得範囲より先を選べないように。削除に確認ダイアログ。日付ボタンを44pxに。
- 検証の穴を1つ塞いだ: プレビューに `/api/admin/shift-deadlines` と `/api/admin/shift-slots` の fixture を追加。これまで両タブは取得失敗状態でしか開けず、**4タブの未保存確認を実データで検証できていなかった**。
- 検証: `--no-file-parallelism` で155ファイル・1,383テスト全成功、全ワークスペースの型検査、55テーブルの所属検査。
- 残課題: **migration 168〜173 は本番未適用**。数値入力の丸めを blur まで遅らせている都合で、フォーカス中は表示と保存値が食い違い、空欄のままでも保存が押せる（対処方針は監査の意見待ち）。同じ日・同じコース便の再追加が警告なく上書きする点も未対応。配車・時刻・取込の競合検知、RIMG-1とOCR、駐車の検知/通知/撮影、Base の初回機能は未着手。

## 2026-09-18 04:00 設定モーダルの保存直後の誤確認と、320pxのカレンダーを直す

- **P1: 保存が成功した直後に「保存していない変更」の確認が出ていた**（提出締切・便タブ）。親の `requestClose` が `dirty` をクロージャで見ており、子が `onDirtyChange(false)` を呼んだ直後に `onClose()` を呼ぶため state 未反映で true のままだった。提示される2択のどちらも「保存した」に当てはまらない状態。未保存の判定を ref へ移し、`markDirty` で ref を先に更新するようにした。**プレビューに fixture を足して初めて見えた不具合**で、それまで該当タブは取得失敗状態でしか開けなかった。
- 保存後の挙動を4タブでそろえた: 提出締切・便は `embedded` のときモーダルを閉じない（単独モーダルとして開く従来の経路は変えていない）。
- 320px のカレンダー: ポップオーバーは収まるようにしたが中身が336px固定のままで、土曜と月送りが画面外だった。`max-[359px]` の幅だけ日セル `size-12→size-10`・曜日 `w-12→w-10`・余白 `p-4→p-2` に（**共有の Calendar なので他画面の320px表示にも効く**）。
- react-day-picker v9 は `fromDate`/`toDate` では**月送りを止めない**ため、`startMonth`/`endMonth` も渡すようにした（選べない月を延々めくれていた）。日付表記の括弧を全角にそろえた。
- 監査の助言を採用: 入力中で未確定（空欄・範囲外・小数）のあいだは保存を disabled にして、表示と保存値の食い違う窓を閉じた。既に同じ日・同じコース便の指定があるときはボタンを「追加」→「変更」に。
- 検証: `--no-file-parallelism` で155ファイル・1,383テスト全成功、全ワークスペースの型検査。前回の再監査では P1-1（フォーカス奪取）・P1-2（未保存の消失）・P2-1（Shift+Tab）・P2-3・P2-4・P3-2〜P3-4 が直っていることを実ブラウザで確認済み。
- 残課題: **migration 168〜173 は本番未適用**。提出締切・便タブのタップ領域が13〜34px（有効チェックボックス13px・25要素）は既存コードの広範囲に及ぶため未対応。共有 `DatePicker`/`Calendar` への変更は他画面で未実測。配車・時刻・取込の競合検知、RIMG-1とOCR、駐車の検知/通知/撮影、Base の初回機能は未着手。UI監査の再確認は結果待ち。

## 2026-09-18 04:15 配車の後勝ちも止め、カレンダーの縮小範囲を実測の境界にそろえる

- 両監査が完了: 差分監査は「無し」、UI監査も最終的に「無し」。UI監査の最後の指摘（カレンダーの縮小が `max-[359px]` 止まりで 360〜387px では土曜が切れる。360はAndroid・375はiPhone SE/miniの幅）を受け、境界を **`max-[387px]`** に広げた。9幅（320〜1280）で土曜と月送りが初期表示で見え、内側の横スクロールが0であることを実測で確認。
- **配車（`/api/admin/shifts/vehicle`）にも競合検知を入れた**。こちらは migration 不要で、`UPDATE ... WHERE vehicle_id = <画面が見ていた値>` の条件で足りる。0行更新のとき、枠が無いのか他の人が変えたのかを読み直して見分ける。画面側は割当と同じくセルごとに直列化し、直前に自分が保存した値を覚えて期待値にする（再取得を待たずに続けて操作しても誤検知しない）。配車の変更ログにも便番号が入るようにした。
- 検証: `--no-file-parallelism` で155ファイル・1,383テスト全成功、全ワークスペースの型検査、55テーブルの所属検査。
- 残課題: **migration 168〜173 は本番未適用**。O-3 の残りは時刻（`shifts/times`）と取込（`import/apply`）。配車の競合検知は**実ブラウザでの動作確認をしていない**（サーバー側の条件付き UPDATE と画面側の直列化はコードレベルのみ）。提出締切・便タブのタップ領域13〜34px、共有 `Calendar`/`DatePicker` の他画面への波及も未検証。RIMG-1とOCR、駐車の検知/通知/撮影、Base の初回機能は未着手。

## 2026-09-18 04:47 割当・配車の保存経路にAPIテストを足す（ブラウザ無しの検証）

- プレビューサーバーがメモリ不足で停止したため、ブラウザを使わない検証へ切り替えた。既存の `auth/login/route.test.ts` と同じ hoisted モックの型で、**本番の中心的な書き込み経路2本にAPIテストを追加**（計15件）。
- `api/admin/shifts/vehicle`（6件）: 期待値が一致すれば保存し、**期待値が UPDATE の条件に入っていること**を実際に検査。期待値 null は「車両なし」を条件にする。他の人が先に変えたら409（枠はある）、枠が無ければ404、期待値を送らない古い呼び出しは条件を付けずに従来どおり、変更ログに便番号が入る。
- `api/admin/shifts`（9件）: RPC 成功は従来経路を使わない／40001→409／P0002→404／P0001→403／PGRST202・42501 は従来経路へ落ちる／**PGRST203 では落とさない**（引数を増やして古い定義が残ると返るコード。文面で判定していると無条件 upsert へ落ちて後勝ちが復活するため、ここを固定した）／従来経路でも便番号をログへ／期待値が不正なら400。
- 検証: `--no-file-parallelism` で157ファイル・1,398テスト全成功、全ワークスペースの型検査。使い捨てコンテナの残留なし。プレビューは再起動して `http://127.0.0.1:3197`（`/preview/admin/shifts` と `/preview/admin/my-shifts`）で起動中。
- 残課題: **migration 168〜173 は本番未適用**。配車の競合検知は API テストで固定したが**実ブラウザでの操作確認は未実施**。O-3 の残りは時刻（`shifts/times`）と取込（`import/apply`）。提出締切・便タブのタップ領域13〜34px、共有 `Calendar`/`DatePicker` の他画面への波及、RIMG-1とOCR、駐車の検知/通知/撮影、Base の初回機能は未着手。

## 2026-09-18 05:15 シフト系3ルートのテナント境界の抜けを塞ぐ

- O-3 でシフト系APIを読んでいて、**他社のデータに届く経路を3本見つけた**。`shifts` / `shift_requests` / `shift_request_logs` は `org_id` 列を持たない（コース・ドライバー経由で会社が決まる）ため、`check:tenant` の自動検査に載っておらず、呼び出し側の確認も抜けていた。
  - `POST /api/admin/shifts/times` — **他社のシフトの集合・着車・終業時刻を書き換えられる**
  - `DELETE /api/admin/shifts/requests/[id]` — **他社の希望休を削除できる**
  - `GET /api/admin/shifts/requests/history` — **他社の希望休の変更履歴を読める**
- いずれも「どこかの会社で該当権限を持つ」＋「対象のUUIDを知っている」が条件。UUIDは推測しにくいが、複数社に所属した経験のある運営なら分かるので、秘匿性に依存した状態だった。**本番で悪用された形跡を確認したわけではない**（アクセスログは見ていない）。
- 3本とも既存の `belongsToOrg` で自社のコース・ドライバーを確かめてから DB を触るようにし、他社のIDは 404「見つかりません」で返す（存在の有無を漏らさない）。`requests/[id]` は対象行が無い場合も 404 に（削除の空振りを成功として返していた）。記録は `docs/security/2026-09-18-shift-tenant-gaps.md`。
- 再発防止として `src/test/security/shiftTenantRoutes.test.ts` に6件。**他社のIDでは DB を触る前に止まること**（`supabase.from` が呼ばれない・削除が走らない）まで検査する。`check:tenant` は org_id 列を持つテーブルしか見ないので、この種のテーブルを触るルートは自分で `belongsToOrg` を呼ぶ必要がある、と記録に残した。
- あわせて、割当・配車の保存経路にAPIテストを追加（計15件）。特に **PGRST203 では従来経路へ落とさない**こと（落とすと競合検知が無言で無効になる）を固定した。
- 検証: `--no-file-parallelism` で158ファイル・1,404テスト全成功、全ワークスペースの型検査、55テーブルの所属検査。
- 残課題: **migration 168〜173 は本番未適用**。この抜けの修正も本番未公開。他の領域（車両・日報・請求）に同じ形の抜けが無いかは未調査。プレビューはメモリ不足で停止中（起動は `npm run preview:admin -- admin --port 3197`）。配車の競合検知の実ブラウザ確認、O-3 の残り（時刻・取込の競合検知）、RIMG-1とOCR、駐車の検知/通知/撮影、Base の初回機能は未着手。

## 2026-09-18 08:20 希望休まわりに org_id を足し、テナント検査の穴そのものを埋め始める

- 05:15 の3件は「見つけたから直した」だけで、**`check:tenant` が org_id 列を持つ表しか見ない**という穴は残っていた。本番の103表のうち**49表に org 列が無く**、検査の外にある。そこで列を足して検査対象に載せる方針へ切り替えた（「見つけたら直す」→「書き忘れたら検査で落ちる」）。第1弾が希望休まわりの2表。
- `supabase/migrations/174_shift_requests_org_id.sql`: `shift_requests` / `shift_request_logs` に **NULL 許容の** `org_id` を足し、`drivers` から backfill。複合外部キー `(org_id, driver_id) → drivers(org_id, id)` で、行の org とドライバーの所属が食い違えないようにした。
  - **NOT NULL は入れない**。全書き込み経路が org_id を入れていると確認してから別 migration で入れる（先に入れると入れ忘れた経路で希望休の提出が落ちる）。複合FKは MATCH SIMPLE なので org_id が NULL の行では検査されず、移行中も安全。
  - 使い捨て Postgres での検証中に**複合FKが所属変更を塞ぐ**ことに気づき、`ON UPDATE CASCADE ON DELETE CASCADE` を付けた。今のコードに所属変更の経路は無いが、塞いだままにしない。削除は driver_id 側の既存 `ON DELETE CASCADE` が先に効いて通ることも実測で確認した。
- 列が増えて `check:tenant` の対象が 57 表になり、この2表を触る**12箇所**が検出された。全て実体を読んで仕分けた。
  - 書き込み（提出2箇所・履歴記録）は `org_id` を入れる。`ShiftLogRow.org_id` を**型で必須**にしたので、呼び出し側の入れ忘れは型検査で止まる（実際に2箇所が検出された）。
  - 読み取り・削除9箇所は `.eq("org_id")` を足さず、既存の絞りを `// tenant-scope-ok:` で明示した。**本人の `driver_id` 固定は org 絞りより狭い**ので、置き換えると防御が弱くなる。残りは `belongsToOrg` 通過後のID、または自社 drivers から作った `driverIds`（両方とも `.eq("org_id", orgId)` 由来であることを確認した）。
- **適用順に依存しないようにした**。174 を本番へ適用する前にコードが出ても希望休の提出が落ちないよう、`server/shiftRequests/orgColumn.ts` に退避経路を置いた。`org_id` 列が無いことだけが原因のエラー（42703 / PGRST204）なら org_id を落として書き込み直す。vehicles の `part_colors` と同じ扱い。174 が本番に入りきったら消してよい。
- 検証: 使い捨て Postgres で174を2回適用（冪等）、backfill・他社orgの拒否・所属変更の追随・ドライバー削除の4挙動を実測。コンテナは削除済み。`--no-file-parallelism` で**159ファイル・1,408テスト全成功**、型検査、`check:tenant` 57表で違反0。
- 残課題: **migration 168〜174 は本番未適用**（174 は他と違い単独で先に適用してよい。コード側は未適用でも動く）。**残り約46表**は同じ手順で順に潰す（`shifts` は41箇所絡むので単独で1回分）。174 適用後に `org_id IS NULL` が増えていなければ NOT NULL（175）を入れられる。O-3 の残り（時刻・取込の競合検知）、RIMG-1とOCR、駐車の検知/通知/撮影、Base の初回機能は未着手。

## 2026-09-18 09:05 shifts と経費4表を検査対象に載せ、金額に届く抜けを塞ぐ

- 08:20 の続き。org 列が無い表に `org_id` を足して `check:tenant` に載せる作業を、`shifts`（migration 175）と経費まわり4表（176）まで進めた。**検出された76箇所を1件ずつ読んだ結果、実害のある抜けが6ルート見つかった**。記録は `docs/security/2026-09-18-org-column-rollout.md`。
- **`DELETE /api/admin/courses/[id]`** — 関連レコード（シフト・担当コース・単価）を `course_id` だけを条件に消していた。他社のコースIDを渡すと**他社のシフト・単価が消える**。`courses` 自体は `.eq("org_id")` で守られるので、他社のコースは残ったまま中身だけ消えて `ok: true` が返る。削除前に `belongsToOrg` → 404 にした。
- **経費まわりの4ルート**（`driver-ad-hoc-expenses` と `driver-expenses` の一覧・作成・更新・削除）が **org を一切参照していなかった**（`requirePermission` の戻り値が権限判定のあと未使用）。他社の経費の金額を読む・足す・書き換える・消すが全部できた。`driver-rewards` も `driver_id` が未検証で経費3表が素通りだった。金額がそのままペイメント・請求書に効くため、5ルートに所属確認を入れて 404 にした。
- 実害には至らないが直したもの: 日報の日別・期間集計、要対応バッジ、取込の既存割当チェック、請求先の推定が **シフトを org で絞らず全件読んで**いた。応答は正しいが1000行上限で黙って切られると自社の行が欠ける（2026-08-02 の「休み」誤表示と同型）。自社の driver / course 集合で絞った。代理入力は所属確認より前に他社のシフトを読んでいたので順序を入れ替えた。
- migration 175（shifts）は **他社のドライバーをシフトに割り当てることをDBレベルで不可能**にする。本番で違反行0件を確認済み。既存の `shifts.driver_id` が `ON DELETE SET NULL` なので、複合FKは PG15 以降の**列指定形** `ON DELETE SET NULL (driver_id)` にした（列を指定しないと `org_id` まで NULL になる）。本番は PostgreSQL 17。
- 読み取りに `.eq("org_id")` を足していない箇所がある。本人の `driver_id` 固定・`belongsToOrg` 通過後のID・自社集合からの `.in()` は **org 絞りより狭い**ので、置き換えると防御が弱くなる。`// tenant-scope-ok: 理由` で実際の絞りを明示した。
- 検証: 使い捨て Postgres 17 で 175 / 176 を2回ずつ適用（冪等）、backfill・他社orgの拒否・所属変更の追随・ドライバー削除時の挙動を実測。コンテナは削除済み。`--no-file-parallelism` で**160ファイル・1,417テスト全成功**、型検査、`check:tenant` 62表で違反0。セキュリティテストを9件追加（他社IDでは **DB を触る前に止まる**ことまで検査）。
- 残課題: **migration 168〜176 は本番未適用**。org 列が無い表は残り約36表（日報明細・コース単価・車両まわり・通知・チーム戦・締切ルール・units）。174〜176 適用後に `org_id IS NULL` が増えていなければ NOT NULL を入れられる。O-3 の残り、RIMG-1とOCR、駐車の検知/通知/撮影、Base の初回機能は未着手。

## 2026-09-18 09:15 日報明細・コース単価・担当まで検査対象を広げ、全社の単価が漏れる経路を塞ぐ

- 09:05 の続き。migration 177（`report_entries` / `daily_reports`）と 178（コース子表6つ＋ドライバー子表3つ）を足し、`check:tenant` の対象を **57表 → 73表**にした。検出された74箇所を1件ずつ読み、さらに3件の抜けを見つけた。
- **`GET /api/admin/course-rates`** — 絞りが一切なく、**全社のコース単価（売上・支払・利益）をそのまま返していた**。条件は「どこかの会社で単価の閲覧権限を持つ」だけで、**他社のUUIDを知る必要すらない**。今回見つかった中で最も条件が緩い。自社のコース集合で絞るようにした。`PATCH` も `course_id` 未検証で他社の単価を書き換えられたので `belongsToOrg` を追加。
- **`PUT /api/admin/shift-slots`** — 便マスタ `shift_request_slots` は org 列を持たない**完全な共有表**（本番3件）。保存処理が「一覧に無い便を全部消す」「割当を全部消す」を**全社横断**でやっており、A社が便設定を保存するとB社の便・割当（＋CASCADEでB社の希望休）が消える。**稼働中の会社が1社のみのため実害は出ていない**が、2社目で必ず壊れる。便の削除は他社が使っていないものに限り、割当の削除・追加は自社のドライバーに限るようにした。**便を会社ごとに持つかは要判断**として記録。
- `admin/monthly` が旧日報を org 無しで全件読んでいたのを自社ドライバー絞りに。`aggregation/load.ts` は単価3表を全件ページングしていたのを、自社のコース集合を先に確定してから絞る形に変えた（往復回数は同じ）。`legacyShape` の便一覧も `courses!inner` で絞った。
- migration 178 の `driver_courses` は親が2つ（drivers と courses）。両方に複合外部キーを張ったので、**他社のコースを自社ドライバーの担当にすること**もDBレベルで不可能になる。使い捨て Postgres 17 で実測確認。
- 検証: 177 / 178 を2回ずつ適用（冪等）、backfill・他社orgの拒否・親削除時のCASCADEを実測。コンテナは削除済み。`--no-file-parallelism` で**161ファイル・1,420テスト全成功**、型検査、`check:tenant` 73表で違反0。セキュリティテストを6件追加（単価の一覧が自社コースで絞られること、便の保存が他社の行を消さないこと）。
- 残課題: **migration 168〜178 は本番未適用**。org 列が無い表は残り約28表（車両まわり5・通知3・チーム戦3・締切ルール3・units 系ほか）。`identities` / `driver_identities` / `passkey_credentials` / `organizations` は設計上テナント横断なので対象外。**`shift_request_slots` を会社ごとにするかは判断待ち**。

## 2026-09-19 00:55 日報の便ラベル欠落を直し、便マスタに持ち主を持たせる

- **日報の日別画面で、同じコースの行が2つ並ぶ理由の調査**（ユーザー指摘）。C1/C2 を作ったコースはシフトが便ごとの枠になり、両方に割り当てられたドライバーは**1回の提出で日報が2件**できる（`cycle_no` 1 と 2・提出時刻は同一ミリ秒）。単価も便ごとの固定（7,727円×2）なので、2件あること自体は金額モデル上は正しい。**金額はズレていない**（固定単価なので個数は請求に使われない）。
- ただし画面で見分けられないのは不具合だった。`CycleBadge` も `legacyShape` の `cycle_label` も揃っているのに、**日別タブが使う `day-summary-range` だけが途中で捨てて**いた（`course_name` は詰めているのに `cycle_label` を詰め忘れ）。2行足して直した。`/api/admin/daily/all` は元から出ていた。
- **便（`shift_request_slots`）の持ち主を決めた**（migration 179）。元請⇄下請で設定が伝わる構造を保ちたいというユーザー判断により、**便は共有のまま**にし、変えるのは「**直す・消すは作った会社だけ**」の一点。
  - 名前・時刻・削除は持ち主の会社だけ。他社の便・持ち主不明の便は画面から消しても残す。新規作成時は自社を持ち主にする。割り当ての削除・追加は自社のドライバーだけ。
  - `owner_org_id` は割当先ドライバーの所属から backfill（1社に定まるときだけ）。本番3件はすべて ACE に決まることを使い捨て Postgres で確認。複数社が使う便・孤児便は NULL のまま＝誰も編集できない。
  - 179 未適用の環境では持ち主が分からないので、退避として「他社のドライバーが使っていない便のみ削除可」へ落ちる。画面は止まらない。
  - 管理画面では他社の便を読み取り専用（グレー背景＋一言）で並べる。
- 便が `check:tenant` の対象（74表）に入ったため、共有マスタである理由を5箇所へ明示した。
- 検証: 179 を2回適用（冪等）＋backfill の分岐4パターンを実測、コンテナ削除済み。型検査、`check:tenant` 違反0、`--no-file-parallelism` で**162ファイル・1,429テスト全成功**（便の所有権テスト9件に差し替え）。
- 残課題: **migration 168〜179 は本番未適用**。「承認された関係の範囲だけに便を見せる」には `organizations` に会社間の関係が必要で、これは新規設計（単価・車両貸出も同じ土俵）。**シフト表のPC出力の作り直しは未着手**（方針は確定: 日付範囲・含める人・含めるコースを画面上のドラッグと除外グレーアウトで選び、プレートは実物、PNG を作って `pngToPdf` で PDF 化）。

## 2026-09-19 01:15 シフト表のPC出力を作り直す（プレビュー確認まで）

- PC のシフト表出力を、手描き（`shiftPdf.ts` で A4横1枚に座標を置く）から**画面をそのまま画像にする方式**へ作り直した。方針はユーザー確定: 見た目はシフトメモ、画像化の手順は日別配車、PNG を作って `pngToPdf` で PDF 化、対象は画面上で選ぶ。
- 新規: `lib/shiftExport/selection.ts`（日付のドラッグ選択・除外・要約／13テスト）、`lib/shiftExport/data.ts`（出力素データと週境界の改ページ／5テスト）、`lib/captureShiftImage.ts`（複製→プレート画像差し替え→html2canvas）、`ShiftExportBoard.tsx`（出力盤面）、`ShiftExportDialog.tsx`（選択＋プレビュー＋保存）。
- **選択は表そのものをドラッグする**（Excel と同じ感覚・ユーザー指摘で作り直し）。最初はチップを並べて日付を選ぶ形にしたが、求められていたのは**シフトメモと同じ矩形選択**だった。表の上をドラッグすると日付×人の矩形が決まり、範囲の外はグレーで残る（消さないので、どこを外したかが見えて戻しやすい）。範囲の中でも日付・名前の見出しを押せば個別に出し入れできる＝範囲に穴を開けられる。選んだ矩形には Excel と同じ青い枠を出す（罫線ではなく内側の影で描き、セルの大きさを変えない）。
- 選ぶ表と画像にする盤面は**別物**にした。選ぶ表は全体を出してグレーで示し、画像化するのは選んだぶんだけを描いた画面外の盤面。こうしないとグレーの行が画像に写る。
- **ナンバープレートが実物になった**（旧出力は `formatPlateOneLine` の文字列）。日別配車と同じく、複製した DOM の CSS mask プレートを `renderPlateImage` の画像へ差し替える。
- A4横1枚に圧縮する制約をやめ、**日付7日ぶんで自動改ページ**（週の途中で割れにくいコスト設計）。15日なら3枚。
- プレビューで見つけて直したもの:
  - **`未割当` の判定が便（cycle_no）を見ていなかった** — `getCurrentDriverId(date, courseId, slot)` が cycleNo 既定0のままで、C1/C2 のコースでは誰も拾えず**稼働中の人まで「未割当」に出て**いた。旧エクスポートも同じ表を使っていたので、同じ誤りが出ていたはず。`assignedDriverIdsOnDate` に切り出し、有効な便を全部見るようにした。
  - **コース名が C1/C2 を区別していなかった** — 同じ名前のチップが2行並ぶ。`courseCycleLabel(course, cycleNo)` に変えた。
  - **画像でだけコースの枠線が消える** — `outline` を使っていたが html2canvas は outline を描かない。`border` に変えた。画面と画像の不一致は、実ファイルを出して初めて分かった。
- 検証: 隔離プレビュー（`npm run preview:admin -- admin --port 3197`）の `/preview/admin/shifts?scenario=normal&role=admin` で、1440 / 768 / 390px を実ブラウザで操作。PNG と PDF を実際にダウンロードして中身を目視。console エラーなし。型検査、`--no-file-parallelism` で**164ファイル・1,447テスト全成功**。
- fixture に架空の車両4台（黒・黄・白・緑）を足し、日付とレーンで回して4色のプレートが混ざる状態にした。実在の番号はコピーしていない。
- 残課題: **`lib/shiftPdf.ts` が未使用になった**（確認後に削除する）。コース単位の除外は未実装（日付と人のみ）。スマホは従来どおり日別配車の画像へ直行する（PC向けの作り直しのため）。**migration 168〜179 は本番未適用**。

## 2026-09-19 01:25 シフト表の出力を「Excel 感覚の矩形ドラッグ」に作り直す

- 01:15 の出力ダイアログは、日付チップを並べて選ぶ形にしていた。ユーザー指摘「シフトメモのように、Excel感覚でドラッグで範囲を選択する。日付のチップで選択はしません」を受けて選択モデルから作り直した。
- `lib/shiftExport/selection.ts` を矩形ベースへ全面的に書き換え（17テスト）。`range: {firstDay,lastDay,firstRow,lastRow} | null` と、範囲の中で個別に外す `excludedDates` / `excludedDriverIds` の2層。表全体を覆う矩形は `null`（＝全部）へ畳んで状態を1つに保つ。範囲の外にある見出しを押したときは、そこまで範囲を広げてから戻す。
- 盤面（`ShiftExportBoard`）に `interaction` を足し、セルの `pointerdown` / `pointerenter` でドラッグ、見出しのクリックで個別の出し入れ。`pointerup` は window で拾い、どこで離しても終わる。`select-none` でドラッグ中の文字選択を止める。
- 選んだ矩形には Excel と同じ青い枠を出す。罫線を足すとセルの大きさが変わるので `box-shadow: inset` の辺で描く（`marqueeShadow`）。
- **選ぶ表と画像化する盤面を分けた**。選ぶ表は全体を出して範囲外をグレーにする。画像にするのは画面外に置いた「選んだぶんだけ」の盤面。同じ要素を使い回すとグレーの行が画像に写る。
- 検証: 隔離プレビューで実際にドラッグ（9/17 田中 → 9/19 高橋）し、範囲内の 鈴木 を見出しクリックで外して PNG を保存。狙いどおり 3日×2人 の画像になった。console エラーなし。型検査、`--no-file-parallelism` で**164ファイル・1,451テスト全成功**。

## 2026-09-19 01:35 シフト表の出力の選択を Excel の作法にそろえる

- 01:25 の矩形ドラッグに対し、ユーザー要望「Shift 押しながらで複数選択、名前クリックで行全体、細かい仕様も Excel に近づけたい」を反映。
- 選択モデルを **矩形1つ** から **「出す列の集合 × 出す行の集合」** へ作り直した（`days: number[] | null` / `rows: number[] | null`、`null` = 全部）。出力が「どの日付 × どの人」の表である以上この形が正本で、矩形ドラッグも飛び飛びの選択も同じ形に落ちる。全部そろったら `null` へ畳み、状態を1つに保つ。
- 対応した操作（25テスト）:

  | 操作 | 結果 |
  |---|---|
  | セルをドラッグ／クリック | その矩形（日付の範囲 × 人の範囲） |
  | Shift + セルクリック | 起点から今のセルまで伸ばす |
  | 名前をクリック | その行全体（日付は全部に戻る） |
  | Shift + 名前クリック | 起点の行から今の行まで |
  | Cmd/Ctrl + 名前クリック | その行を足す／外す |
  | 名前の列をドラッグ | 連続した行 |
  | 日付の見出し | 同じ操作の列版 |
  | 左上「ドライバー」 / Cmd+A | 全選択 |

- 最後の1行・1列は Cmd+クリックでも外せないようにした（空の画像を作らせない）。
- 選択枠は **隣が選ばれていない辺にだけ**引く（`cellEdges`）。飛び飛びの選択でもまとまりごとに Excel と同じ枠になる。`days`/`rows` が `null`（＝全部）のとき範囲外の添字まで「選択済み」と判定して表の縁に枠が出ない不具合をテストで捕まえ、境界を渡すようにした。
- 検証: 隔離プレビューで7通りの操作を実ブラウザで順に実行し、副題の日数・人数で結果を確認（行クリック=1人 → Shift=4人 → Cmd=5人 → 列 Shift=3日 → Cmd+A=全部 → セルドラッグ=2日×2人 → Shift+セル=4日×3人）。飛び飛びの行選択で枠が2つに分かれることも目視。console エラーなし。型検査、`--no-file-parallelism` で**164ファイル・1,459テスト全成功**。
- 残課題: コース単位の除外は未着手。`lib/shiftPdf.ts` が未使用のまま。スマホは従来どおり日別配車の画像へ直行。

## 2026-09-19 12:35 シフト表の出力: 端スクロール・名前セル全面クリック・列幅の整理

- ユーザー指摘3点に対応。
- **ドラッグで画面外の列を選べなかった**（右側の隠れている日付に届かない）。端までポインタを持っていったら表をスクロールして追従するようにした（シフトメモと同じ `exportEdgeVelocity` を再利用）。
  - 位置からセルを引くのは `elementFromPoint`。スクロール後の実位置で引き直せる。**端では枠の余白やスクロールバーに乗ってセルに当たらない**ので、当たるまで内側へ14pxずつ寄せて探す。
  - rAF ループが**最初のフレームで止まっていた**（押した直後はポインタ未移動で `pointer.current` が null）。ドラッグが続く限り回し続けるように直した。これが無いと「押してから端へ持っていく」が一度も効かない。
- **名前のセルは「どこを押しても」行が選べる**ようにした。ボタンを `flex` でセルに敷いても**行の高さまでは伸びず**、上下の余白が押せなかった（実測で確認）。`th` を `relative` にしてボタンを `absolute inset-0` で全面に重ねる形へ。日付の見出しと左上も同じにそろえた。
- **ドライバー名の列が広すぎ・余白過多**を整理。
  - 出力盤面: 132px → 104px、余白を詰め、名前を太字・中央寄せに。
  - 本体のシフト表: 1列目を `min-w-[9rem]` → `min-w-[7rem]`、`px-3` → `px-2`、ドライバー名を `font-medium` → `font-semibold`。
- あわせて**選ぶときだけ見出し行と名前列を固定**した（Excel のウィンドウ枠固定）。右まで引いたときに誰の行か・いつの列かを見失わない。画像側には効かせない（`interaction` が無いときは sticky を付けない）。
- 検証: 隔離プレビューで実測。名前セルの端をクリック→1人、右端で保持→**15日ぶん全部**（以前は7日で止まっていた）。スクロール後の sticky 位置を DOM で実測（スクロール枠 x=64/幅1152、名前列 x=80 に固定）。Cmd+クリックで田中だけ外して保存した画像が5人になることも確認。console エラーなし。型検査、`--no-file-parallelism` で**164ファイル・1,459テスト全成功**。
- 残課題: コース単位の除外は未着手。`lib/shiftPdf.ts` が未使用のまま。

## 2026-09-19 13:05 シフト表の出力: 選んだ範囲を全部出す・未割当を既定で外す・プレート幅の不一致を直す

- ユーザー指摘4点に対応。うち1件は**共有部品の不具合**だった。
- **「9/30 まで選んだのに 9/20 までしか出ない」** — 保存が**表示中のページだけ**を撮っていた。選んだ範囲の全ページを出すようにした。PNG は1枚ずつ（枚数をボタンに出す）、PDF は `pngsToPdf` で**1ファイルにまとめる**（`pdfExport.ts` に追加・5テスト）。画面外の盤面をページぶん並べて順に撮る。
- **スクロール追従時に名前列の左へ中身が溢れる** — `sticky left-0` は**スクロール枠の padding 辺**に貼り付くので、枠に `p-4` があるとその16pxの帯に中身が流れ込む。枠の余白をやめて解決。
- **未割当の行は既定で入れない**ようにした（チェックで入れられる）。選ぶ表も同じトグルに従わせ、画像に出ないものをプレビューに出さない。
- **プレビューと画像でコース名チップとナンバープレートの幅が合わない** — 原因は共有の `renderPlateImage`。`measurePlate` が `VehiclePlate` を `className="w-full"` だけで測っており、**compact の既定 `max-w-[100px]` で頭打ち**になっていた。幅を引数で受け取る関数なのに100pxを超えられず、セル幅が100pxを超える表では画像のプレートだけ細くなる。`!max-w-none` を足して渡した幅どおりに描くようにした。日別配車の画像化はセル幅が100px未満なので影響しない（DOM実測で確認: チップ・スロット・プレート枠とも134.9pxで一致）。
- あわせて、画像化する盤面の包みを `w-max` にした（親に合わせると右に余白が残る）。
- 検証: 隔離プレビューで PNG 3枚・PDF 1ファイル（3ページ）を実際にダウンロードし、3枚目に 9/30 が入ること・未割当が無いこと・チップとプレートの幅が揃うことを目視。横スクロール後の見た目も確認。console エラーなし。型検査、`--no-file-parallelism` で**165ファイル・1,464テスト全成功**。
- 残課題: コース単位の除外は未着手。`lib/shiftPdf.ts` が未使用のまま。

## 2026-09-19 15:35 シフト表も日別配車も「1枚」に収める

- ユーザー指定「横長になってもいい／縦長になってもいいので1枚に入れたい」。両方のページ分割をやめた。
- 枚数で割らないぶん画像が大きくなるので、倍率の決め方を `lib/imageCaptureScale.ts` に切り出した（6テスト）。これまでは「400万画素以内」の固定式で、1ヶ月ぶんの横長だと**等倍を下回って文字が読めなくなる**。
  - 1辺 12,000px・面積 30M px を上限にし、収まるなら3倍まで上げる。**等倍を下回るのは、等倍でも上限を超えるときだけ**。
  - 実測: 48人×15日 → 5856×5122（30.0M px・倍率2.5）。31日でも倍率1.8前後で等倍を割らない。
- シフト表: ページ送りのUIと `planShiftExportPages` / `sliceExportPage` を撤去。画面外の盤面も1つに戻した。PDF は1ページ（`pngsToPdf` は複数ページ対応のまま残す）。
- 日別配車: `captureDispatchImage` から 12行ごとの分割を外し、`options.page` / `pageCount` も削除。`ImageExportDialog` には `pageCount={1}` を渡す（`pageCount > 1` でしかページ送りを出さないので表示は消える）。
  - 実測: 48人 → 1224×6846（8.4M px）を1枚で出力。副題の「1/4枚目」も消えた。
- `lib/dispatchImagePages.ts` は driver-leases のプレビューがまだ使っているので残す。
- 検証: 隔離プレビュー（`large` シナリオ＝48人）で両方を実際にダウンロードし、画素数と中身を確認。console エラーなし。型検査、`--no-file-parallelism` で**165ファイル・1,465テスト全成功**（配車の画像化テストは「全員を1枚に入れる」へ更新）。
- 残課題: コース単位の除外は未着手。`lib/shiftPdf.ts` が未使用のまま。

## 2026-09-19 15:55 シフト表の出力: 契約区分での絞り込み・旧PDF描画の削除

- **コース単位の除外は取りやめ**（ユーザー判断）。1人が Amazon にもヤマトにも入るため、コースで人を絞る操作が意味を持たない。代わりに**契約区分（月額リース／日額リース／リースなし）**で絞れるようにした。区分は人に紐づくので絞り込みとして成立する。
  - 出力データの行に `leaseMode` を持たせ（期間の初日で判定）、ダイアログのフッターに区分ボタンを出す。押すとその区分の人だけ、Cmd+クリックで足す。区分が1種類しか無いときは出さない。
  - 選択の仕組みは既存のまま（`selectRows` / `toggleRow` に流し込むだけ）。飛び飛びの行になっても枠はまとまりごとに出る。
- **`lib/shiftPdf.ts` を削除**（旧A4横1枚のベクター描画。338行）。あわせて唯一の利用元だった `registerJapaneseFont` の import も撤去。
  - `lib/pdfJapaneseFont.ts`（34行）と `public/fonts/SawarabiGothic-Regular.ttf` が未使用になった。**将来ベクターPDFを作るなら要るので残している**（消すならフォント本体も一緒に判断が要る）。
- fixture に契約区分（`driver_leases`）を追加。3区分がそろう状態にして、ボタンの出方と絞り込みを試せるようにした。
- 検証: 隔離プレビューで3つの区分ボタン（各2人）を確認し、月額リースを押すと該当2人だけ色が残ることを目視。console エラーなし。型検査、`--no-file-parallelism` で**165ファイル・1,465テスト全成功**。

## 2026-09-19 16:10 migration 172・174〜179 を本番へ適用。dryrun が本番へ書ける不具合を修正

- **`scripts/db/db.sh dryrun` が「試し流し」になっていなかった。** `BEGIN; <file>; ROLLBACK;` で包む作りだが、**migration ファイル自身が `BEGIN … COMMIT` を持つ**ため、内側の COMMIT で確定し、外側の ROLLBACK は `WARNING: there is no transaction in progress` で空振りする。
  - 適用前の確認のつもりで 7 本を dryrun したところ、**そのまま本番へ適用された**。結果は依頼どおり（この直後に適用する予定だった）なので実害は無いが、意図した経路ではない。
  - 修正: ファイル自身の `BEGIN;` / `COMMIT;` / `END;` 行を外してから包む。行の途中にトランザクション操作が残る SQL は **die して流さない**。適用済みの179と、`_migrations` へ INSERT するだけの SQL で、実際に巻き戻ることを確認した。
- 適用したもの（すべて本番）:

  | # | 内容 |
  |---|---|
  | 172 | `assign_shift_driver` RPC（配車の競合検知＋変更ログを1トランザクションで） |
  | 174 | `shift_requests` / `shift_request_logs` に org_id |
  | 175 | `shifts` に org_id（コース・ドライバーへ複合FK） |
  | 176 | 経費・リース4表に org_id |
  | 177 | `report_entries` / `daily_reports` に org_id |
  | 178 | コース子表6つ＋ドライバー子表3つに org_id |
  | 179 | `shift_request_slots.owner_org_id` |

- 適用後の検証（本番・読み取り）:
  - **backfill は全件完了。org_id が NULL の行は0**（report_entries 9,455 / shifts 2,091 / daily_reports 614 / shift_requests 391 / shift_request_logs 385 ほか、計15表）。
  - 複合外部キー **20本**が想定どおり。`shifts_org_driver_fk` は PG15以降の列指定形 `ON DELETE SET NULL (driver_id)` で入っている。
  - `assign_shift_driver`（9引数）が存在。
  - `shift_request_slots` 3件とも owner_org_id が埋まり、持ち主不明は0。
- `_migrations` 台帳へ適用した7本を記録した。**166（パスキー案内）と167（PIN廃止）は未適用**であることも確認（pin_hash 列は残り、29人が PIN を保持）。164/165/168〜171/173 は適用済みだが台帳には未記録のまま（SQL Editor 経由のため）。

## 2026-09-19 16:20 9/12 以降の未コミット分を2つに分けてコミット

- ブランチ `codex/tenant-files-invoices` へ、9/12 の `b009d45` 以降たまっていた481ファイルを2コミットに分けて積んだ。**push・デプロイはしていない**（ブランチが main ではなく、内容が1週間ぶんと大きいため指示待ち）。
  - `7ad1b9e` db: migration 164〜179 と、dryrun が本番へ書ける不具合の修正（migration 16本・DB検証スクリプト・docs）
  - `4f52da4` シフト表の画像出力を作り直し、テナント境界の抜けを塞ぐ（アプリコード456ファイル）
- 分け方は「DBとドキュメント」「アプリコード」の2つ。1週間ぶんの作業が互いに絡んでおり（例: shifts の page.tsx にテナント修正と出力の作り直しが同居）、機能単位へ後から切り分けると履歴が実態とずれるため、無理に細かくしなかった。
- コミット直前の検証: 型検査、`check:tenant`（74表・違反0）、`--no-file-parallelism` で**165ファイル・1,465テスト全成功**、`next build` 通過。作業ツリーは空。
- 未追跡だった3Dモデル140件（36MB）も含めた。`public/models` は既に51件が追跡済みで、.glb を git に置くのがこのリポジトリの既定のため。
- 残課題:
  - **push とデプロイは未実施**。
  - migration **166（パスキー案内）・167（PIN廃止）は未適用**。167 は「有効なドライバーは SMS 認証か Passkey が要る」ガードで中断したまま（pin_hash 列は残り、29人が PIN を保持）。
  - 台帳 `_migrations` は 159/161/162/163 と今回の7本のみ。164/165/168〜171/173 は適用済みだが SQL Editor 経由のため未記録（`scripts/db/audit/backfill-migrations.sql` で実状に合わせられる）。
  - org 列が無い表は残り約28表（車両まわり・通知・チーム戦・締切ルール・units 系）。
  - `lib/pdfJapaneseFont.ts` と `public/fonts/SawarabiGothic-Regular.ttf` が未使用（ベクターPDFを作るなら要るので残置）。

## 2026-09-19 17:40 main へ push・マージ。CI が落ちた原因（apps/base が lock 未登録）を修正

- `codex/tenant-files-invoices` を push し、`main` へ **fast-forward でマージ**して push した（`72842f1` → `cb012b2`）。main は5コミット進んだ（9/12 以降の作業＋今回の3つ）。
- push 前に CI と同じ4項目をローカルで通した: `npm run typecheck`（web / preview / mobile）、テスト、`check:tenant`（74表・違反0）、`npm run build`。
- **それでも main の App CI が21秒で落ちた。** 原因は `npm ci` の段階:

  ```
  npm error Missing: @repo/base@0.1.0 from lock file
  ```

  root の workspaces が `apps/*` なので、ハコ虎 Base 用に作った `apps/base`（雛形のみ）も対象になる。ディレクトリを足したときに **`npm install` を流していなかった**ため lock にワークスペースの登録が無く、`npm ci` が「package.json と package-lock.json が同期していない」で止まっていた。
  - ローカルの検証では `npm ci` を使わない（既存の node_modules を使う）ため、この不整合は**最後まで見えなかった**。lock の整合は `npm ci --dry-run` で確かめるべきだった。
- 修正: `npm install --package-lock-only` で lock だけを更新（`2228cb9`）。依存のバージョンは apps/mobile と同じ（expo 57 / RN 0.86 / react 19.2.3）なので既存の解決結果をそのまま使い、**差分は apps/base の登録53行の追加のみ・削除なし**。`npm ci --dry-run` が通ることを確認して push した。
- 残課題:
  - CI の結果確認（`npm ci` は突破済み。型検査・テスト・ビルドの結果待ち）。
  - Vercel の本番デプロイ結果の確認。
  - migration **166・167 は未適用**のまま（167 は PIN 廃止のガードで中断。29人が PIN を保持）。
  - `apps/base` は雛形のみ。実装（BASE-1〜5）は未着手。

## 2026-09-19 17:45 本番反映の完了確認（CI 成功・Vercel デプロイ Ready）

- 17:40 の残課題（CI とデプロイの結果確認）を消化した。
- **App CI 成功**（run 35432412939）。全ステップ green: 依存インストール → 型検査（web / preview / mobile）→ テスト → `check:tenant` → ビルド。直前の失敗は `npm ci` の段階だったので、lock 修正で解消したことを確認できた。
- **Vercel 本番デプロイ Ready**（`nippo-ace` / 36秒）。エイリアスは `https://hakotora.jp` と `https://nippo-ace.vercel.app`。
- 稼働確認（本番・読み取りのみ）:
  - ページ `/` `/login` `/join` `/shifts` `/submit` がいずれも **200**。
  - 認証が要る API `/api/admin/course-rates` `/api/admin/driver-expenses` `/api/admin/shifts` がいずれも **401**。今日塞いだ経路が素通りしていないことを確認した。
- main は `72842f1` → `d87e337`（6コミット）。作業ツリーは空。
- **今回の教訓**: push 前に CI と同じ4項目をローカルで回したが、ローカルは既存の `node_modules` を使うため **`npm ci` の整合だけが検証されない**。ワークスペースや依存を足したときは `npm ci --dry-run` を回す。
- 残課題:
  - migration **166（パスキー案内）・167（PIN廃止）は未適用**。167 は「有効なドライバーは SMS 認証か Passkey が要る」ガードで中断したまま（pin_hash 列は残り、29人が PIN を保持）。
  - 台帳 `_migrations` に 164/165/168〜171/173 が未記録（SQL Editor 経由）。`scripts/db/audit/backfill-migrations.sql` で実状に合わせられる。
  - org 列が無い表は残り約28表（車両まわり・通知・チーム戦・締切ルール・units 系）。
  - `apps/base` は雛形のみ。実装（BASE-1〜5）は未着手。
  - `lib/pdfJapaneseFont.ts` と `public/fonts/SawarabiGothic-Regular.ttf` が未使用（ベクターPDFを作るなら要るので残置）。

## 2026-09-19 17:55 本番障害: シフト表が500（PGRST201）。複合外部キーを外して復旧

- **本番でシフト表が「シフトを読み込めませんでした」になった。** `GET /api/admin/shifts` が 500。今日の反映による障害。
- 原因: **174〜179 で足した複合外部キー**。既存の単一列FK（例 `course_cycles_course_id_fkey`）に加えて `course_cycles_org_fk` を張ったことで、**同じ親への関係が2本**になり、PostgREST の埋め込み（`.select("*, course_cycles(...)")` など）がどちらを使うか決められず **PGRST201** を返していた。
  - **スキーマ変更だけで起きるため、アプリのテスト・型検査・ビルド・CI では一切検出できなかった。** 使い捨て Postgres での検証も SQL の挙動しか見ておらず、PostgREST を通していなかった。
  - 影響は広い。複合FKを張った20本のうち、同じ親への関係が2本になったのは14表（shifts→courses/drivers、course_cycles→courses、report_entries→daily_reports_v2、driver_courses→drivers/courses ほか）。
- 対応（migration 180・適用済み）: **複合外部キー20本をすべて外す。org_id 列・索引・backfill はそのまま残す。**
  - 列さえあれば `check:tenant` は対象として見るので、**今回の主目的であるアプリ層の二重防御は維持される**（74表・違反0のまま）。
  - 失ったのは「他社をまたぐ参照をDBが拒む」保証のみ。埋め込みを壊さない形（トリガか CHECK）で別途入れ直す。
- 復旧確認:
  - 複合FKの残りは **0本**。org_id の backfill は無傷（shifts / report_entries / 便の所有者とも NULL 0）。
  - `NOTIFY pgrst, 'reload schema'` でスキーマキャッシュを再読込。
  - PostgREST へ直接、問題になっていた埋め込み4パターン（courses+course_cycles / shifts+courses / daily_reports_v2+report_entries / drivers+driver_courses）を投げて **全て 200**。
  - 適用後、`/api/admin/shifts` の 500 は止まった（最後の500は 08:44:04、適用直後）。
- **教訓**: PostgREST を使う構成では、**FKの追加はAPIの形を変える**。複合FKを足すときは、同じ親への関係が増えないか（＝埋め込みが曖昧にならないか）を先に確認する。検証は SQL だけでなく PostgREST 経由でも行う。
- 残課題:
  - DBレベルでの他社参照の禁止を、埋め込みを壊さない方法で入れ直す。
  - migration 166・167 は未適用のまま。

## 2026-09-19 21:30 日報の画像アップロード: 様式を設定できるようにして件数を読む（RIMG-1・RIMG-3）

**migration 181 は未適用。様式が1件も無い間は本番の見た目・動きは変わらない。**

- ユーザー要望は「自由度」。形式の追加、どこにどの数字があるかの指定、想定と違うスクショ、画面サイズ違い・トリミングへの対応。
  → **形式をコードではなくデータにした**。`report_image_templates` の行を管理画面（`/admin/report-image-templates`）で作り、見本画像に枠を引いて報告項目（unit/field）へ結び付ける。
- 実サンプル（ヤマトの配達集計精算書）をユーザーから受領して検証。**リポジトリには置かない**（`scripts/report-image/try-read.ts` にローカルパスを渡して測る）。
  - **表は画面内で90度回って写る**（縦画面に横長の表）。回転を直さないとOCRは何も読めない。角度を試し、一致点数の高い向きを採る（90度=86%、他=0%）。
  - **全体を一度に読む方法では表の数字がほぼ取れない。** 欄を切り出して読み直すと確度93〜96%でほぼ全問正解。読み取りを二段構えにした（1段目=見出しを探して枠を決める／2段目=枠を切り出して数字を読む）。
  - 前処理は白黒化だけにする。`normalize()`（コントラスト伸張）を入れると読めなくなった。切り出しは余白を足して拡大しないと桁を取り違える（「2」→「23」を実際に踏んだ）。
  - 位置合わせは見出し2〜3個ではなく、**見本と一致した語すべてから Theil–Sen で倍率とずれを推定**する。これでトリミング・サイズ違いが安定した。
  - 結果: 原寸／上下トリミング／端の欠けたトリミング／1.6倍とも宅急便44・持戻2、ネコポス3・持戻0、合計47で全問正解。空欄は0で埋めず未取得。0.55倍（実機では出ない小ささ）は読めず手入力へ戻る。
- **撮影日時は当てにできない。** LINE経由の実サンプルは EXIF に日時が無かった（解像度とサムネイルのみ）。
  - サーバーが原本のバイト列から EXIF / PNG の作成日時を読む実装を足した（端末の申告を信じない・`imageMetadata.ts`）。取れなければ「不明」のまま。
  - 「その日のスクショか」は **画像内の日付 + 同じ画像の既出（SHA-256）+ EXIF** を重ねて判断し、どれも無ければ黙って通す。食い違いは要確認にするだけで提出は拒否しない。
- 安全側の作り: 確度の低い読み直しは値を見せたうえで「確認」に落とす／見出しが切れた欄は未取得（0で埋めない）／様式に当てはまらない画像は「未対応」で手入力へ戻す／日報へ入るのは本人が確認した値だけ。
- 権限とテナント: 様式の編集は `can_manage_carriers`。**他社の報告項目は指せない**（保存時にサーバーが自社の carrier→unit→field を突き合わせる）。運用中は様式ごとに1版（部分ユニーク索引）。
- multipart 送信のために `@/lib/api` に `apiUpload` を足した（`apiFetch` は Content-Type を JSON に固定するため使えない）。@platform/api-client（vendor）は**触っていない**。
- プレビュー: `npm run preview:admin -- admin --port 3197` で
  `/preview/admin/report-image-templates?scenario=normal&role=admin`（様式づくり）と `/preview/admin/submit`（画像から入力）。
  見本は架空のSVGの表。**端末内OCRは実際に走る**（自サイト配信の言語データ）。
- 確認: 全1,516テスト・型検査・`check:tenant`（74表・違反0）・本番ビルド・プレビュー配信200。
  **アプリ内ブラウザでの目視操作は未実施**（この環境にブラウザ操作の手段が無いため）。PC/スマホ幅の見え方は未検証。
- 残り: 管理の確認待ち一覧（RIMG-4）、モバイル（RIMG-5）、検算の設定UI、実機での読み取り時間の計測、原本の保存期間・閲覧範囲の決定。

## 2026-09-19 23:10 日報の画像: 管理の確認（RIMG-4）と検算の設定

**本番未適用のまま（migration 169・181）。**

- `/admin/report-images`（権限 `can_view_reports`）を追加。期間（既定=直近7日・最大62日）と状態で絞り、提出された原本を確認する。
  - **読み取った値と本人が確認した値を並べて出す**。直した項目は読み取り値も残して色を変える。片方に潰さない。
  - 集計へ渡ったのは `adopted` の1版だけ。様式は版まで出す。読み取りの記録の件数も出す。
  - 原本の画像は一覧に載せず、開いたときだけ署名URL（5分）で取る。自社ぶんのみ（保存パスは指定させない）。
  - 確認すべき点（使い回し・画像の日付の食い違い・直した項目数・確定待ち・様式に当てはまらず）を1行にまとめる。**良し悪しの判定はしない。**
- 同じ画像の使い回しは期間内の SHA-256 をまとめて見て、別人／別日／同日二重を分ける（`flagDuplicates`・テスト付き）。
- 様式エディタに**検算（合計＝内訳）の設定**を追加。読み取り側は既に検算していたので、設定から確認まで通った。
- `check:tenant` が `report_source_image_readings` の org 絞り漏れを検出。コメントで逃げず `.eq("org_id", orgId)` を足した（75表・違反0）。
- プレビュー: `/preview/admin/report-images?scenario=normal&role=admin`（通常／提出なし／長い名前／多数／原本を開けない）。原本は架空のSVGの表。
- 確認: 全1,505テスト（新規10件）・型検査（web/preview/mobile）・`check:tenant`・本番ビルド・プレビュー配信200。**ブラウザでの目視操作は未実施**（この環境に手段が無い）。
- 残り: モバイル（RIMG-5）、実機での読み取り時間の計測、原本の保存期間・訂正/削除の権限・閲覧範囲の決定。

## 2026-09-19 23:20 日報の画像: モバイルの原本提出（RIMG-5 の一部）

- `DailyReportForm` に「配完表の画像」を追加。Webと同じAPIへ、**選んだ写真をそのまま**送る（圧縮・切り抜き・回転をしない）。
- **モバイルは写真の作成日時が取れる**（Webでは取れない）。`expo-image-picker` の exif → 写真ライブラリの作成時刻 → 不明 の順で決める。判定は `@repo/core/logic/imageCapturedAt`（テスト付き）に置き、Webサーバーの EXIF 読み取りも同じ関数を使うようにした。
- multipart 送信を `@repo/core/api` の `apiUpload` に統一（Web の実装を core へ移し、re-export）。`@platform/api-client`（vendor）は触っていない。
- **端末内OCRは未実装。** 二段構えは画像の回転と欄の切り出しが前提で、モバイルには画像加工の手段が無い。`expo-image-manipulator`（ネイティブ）を足すと再ビルドが必要になるため、**入れるかどうかはユーザー判断待ち**。それまでモバイルは「原本を残して件数は手入力」で運用できる。
- 確認: 全1,511テスト・型検査（web/preview/mobile）・`check:tenant`・本番ビルド。**実機での動作確認は未実施**。

## 2026-09-20 12:40 日報の画像: 隣の欄を読む事故への守りと、モバイルの端末内OCR

**きっかけ**: 「数字がたくさん並ぶ表なので、間違って違う数字を読まないか」というユーザーの指摘。
**確度は『字が読めたか』しか見ておらず、場所の正しさは保証しない**ので、守りを重ねた。

- 追加した守り: 見本との当てはまり具合（外れ値の割合）／欄の行・列見出しを覚えて差分で追う／曖昧な見出しは使わない／欄の中心からのズレ／切り出しの縁に文字が掛かっていないか／検算が合わなければ関係項目をまとめて確認へ。**捕まえても値は消さず「確認」に落とす。**
- **実際に事故を再現して直した**: 列見出しに「持戻」を選んだら `配完＋持戻` に吸い寄せられ、**隣の列の46を持戻として確度96%で読んだ**。曖昧な見出しを使わない規則を入れて解消。
- もう1つ: 見出しへ欄を「寄せる」実装が誤りで、行見出しが欄の下端にある様式で欄が下へずれた。**見本での位置との差分**で動かす方式に修正。
- 様式づくりの見出し自動提案（`suggestAnchors`）を追加。**数字は選ばない**（日によって変わる）。**他の見出しに含まれる語も選ばない**。管理画面でその場で外せる。
- 実サンプルで再確認: 原寸・トリミング2種・拡大とも 宅急便44/持戻2・ネコポス3/持戻0・合計47 で全問正解。列をずらした合成ケースでは「確認」へ落ちることをテストで固定。
- **モバイルの端末内OCR（RIMG-5）**: ユーザー承認のうえ `expo-image-manipulator` を追加（**再ビルドが必要**）。ML Kit で読み、判定・抽出は `@repo/core` の純粋関数を共有。ML Kit は確度を返さないので「数字が1つだけ素直に取れたか」「縁に文字が掛かっていないか」で代える。確認画面も用意し、日報への反映は Web と同じ `applyImageEntries`（core へ移動して共有）。
- `apiUpload` を `@repo/core/api` へ移し、Web/モバイルで実装を1つにした。
- 確認: 全1,520テスト（新規+9）・型検査（web/preview/mobile）・`check:tenant`・本番ビルド。**実機・ブラウザでの目視確認は未実施**。
  - 途中、重い処理と並行して走らせた回で `LeaseAdminPreview` などが5秒タイムアウトで落ちたが、単独・通常負荷では全て通る（負荷由来のゆらぎ）。

## 2026-09-20 13:20 日報の画像: 毎回の目視確認をやめる（機械が答え合わせする）

**きっかけ**: 「毎回確認画面で画像と付き合わせるのか、もう少し良い方法はないか」。確認作業を人に配り続けるのは負け筋なので、機械が自分で答え合わせできるようにした。

- 表の冗長性を証拠にする。日報に要るのは4つだが**15セル全部を読み、11本の式（配完+持戻=計B+持戻C／計A=配完+持戻／合計=宅急便+ネコポス）が全部閉じるか**を見る。欄を取り違えれば必ずどれかが壊れる。
- 様式に `role: "check"`（検算のためだけに読む欄。日報には入れない）を追加。確認画面には出さない。
- **裏付けの定義を厳しくした**: ある項目の裏付けになるのは「残りの参加者が全部きれいに読めている式」だけ。列を1つ削った画像で「4」と「7」が揃って式を通す例を実際に再現し、これで弾けることを確認。
- 画面の出し分け: verified なら**数字だけ・1タップ**（画像は畳んだまま。「画像で確かめる」で開く）。needs_check のときだけ従来の見比べ。Web・モバイルとも。
- 管理の「別のスクショで試す」に「自動で確定できる（式N本）」を表示。様式を作った時点で自動確定まで行けるか分かる。
- 実サンプル: 原寸・トリミング2種・1.6倍とも **15セル全問正解・verified（式11本）**。**列を1つ削った画像（相手のUI変更の模擬）は needs_check** に落ちる。
- 確認: 全1,525テスト（新規+5）・型検査（web/preview/mobile）・`check:tenant`・本番ビルド。実機・ブラウザでの目視は未実施。

## 2026-09-20 16:00 日報の画像: 他社の画面で確かめる（Amazon・郵便局）と処理時間

**問い**: 運営画面だけで任意の画面を追加できるのか。処理時間は。

- **Amazon 配達プラン概要**: ライトで作った様式のまま**ダークでも読めた**（完了・問題とも一致）。そのために入れたもの:
  - **画像ごとの明暗反転**（暗い画面）。1段目だけでなく欄の切り出しにも同じ反転を掛ける（別々にすると切り出しだけ読めない事故を実際に踏んだ）。
  - **一覧型は見出しからの相対で読む**。管理者が囲んだ枠を「見出しからの距離」として覚える（`valueHint`）。帯ごと切り出していたため矢印を巻き込んで「89」を「897」と読む事故があった。
  - **数字1文字の読み替え**。単独の「0」は eng 辞書で確度87%の "U" になる。数字を入れる欄に限り短い綴りを数字へ戻す。
  - 管理画面で**見出しの文字を直せる**ようにした（自動提案が崩れることがある）。
- **郵便局 追跡件数確認は安定しない**。別端末の画面を**カメラで撮った写真**で、遠近のゆがみにより行の左の項目名と右の数字が同じ高さに来ない。傾き補正（数度）は入れたが足りない。→「読めません・手入力」に落ちる。対応するなら画面の四隅を見つけて台形を正す処理が要る（未実装）。
- 傾き推定は**ばらつきが大きい推定を使わない**規則にした。これを入れる前は表のスクショを1.04°傾いていると誤判定し、無駄な2回目のOCRで時間が倍（0.6秒→1.0秒）になっていた。
- **処理時間（開発機）**: ヤマト15欄=0.6秒（1段目504ms＋2段目97ms）、Amazon3欄=0.56秒。部品の起動は日本語167ms。1段目の解像度は長辺3000pxが下限で、2400pxに落とすと両方とも読み違えが出たため落とさない。
- **その日の報酬見込みへの影響なし**（読み取りは送信前に終わる）。さらに原本の送信と読み取りを並行にした。
- 確認: 全1,531テスト（新規+6）・型検査・`check:tenant`・本番ビルド。実機は未計測。

## 2026-09-20 16:20 日報の画像: コース単位で目視確認を省けるようにする（migration 182・未適用）

- きっかけ: 「Amazonは個数によらず日当だったりするので、コース単位で目視確認スキップを設定できると良い」。
- **「裏が取れない」と「食い違っている」を分けた**（`ReadTrust.level`）。
  - `verified`（表の関係で証明できた）… 設定に関わらず確認なし
  - `unproven`（悪い兆候は無いが証明できない。Amazonの画面はこれ）… **コースの設定で省ける**
  - `suspect`（合計が合わない・範囲外・裏付けの無い怪しい読み）… **設定でも省けない**
  - 必須の欄が読めていない（`incomplete`）ときも省かない。判定は `canSkipReview` に集約し Web/モバイル共通。
- 設定はコース編集の「画像の件数を確認なしで入れる」（`courses.report_image_auto_fill`）。既定はオフ。
- 省いたときはフォームに値が入り「画像から入力しました（完了 1）」だけ出す。**送信するのは本人**なので値は必ず送信前に画面へ出る。読み取りの記録は採用版として残り、管理の「画像の確認」から追える。
- 確認: 全1,535テスト（新規+4）・型検査・`check:tenant`・本番ビルド。

## 2026-09-20 16:40 日報の画像: migration 181/182 を本番適用・ブランチへ push

- 適用前に実スキーマを確認したところ、**169 は適用済み**（台帳に未記録なだけ。`report_source_images` は既に3件の提出あり）。181・182 が未適用だった。
- `scripts/db/db.sh dryrun` で試し流ししてから `apply --confirm=` で適用（本番）:
  - **181** `report_image_templates` ＋ 見本用バケット
  - **182** `courses.report_image_auto_fill`（NULL 0件・有効なコース0件）
  - 169 は冪等なので再実行して台帳へ記録した。
- `NOTIFY pgrst, 'reload schema'` 実行。
- **前回のPGRST201の再発確認**: 同じ親へ複数FKを持つ表は13組あり、`report_image_templates → drivers`（created_by / updated_by）もその1つ。ただし**既存の埋め込みを増やしていない**（このテーブルを埋め込むクエリは無く、列を明示して読む）。既存の org_record_forms・vehicle_qr 等と同じ形。
- **様式は0件なので、ドライバー画面の動きは変わらない**（様式を作って運用中にした時点で「画像から入力」が有効になる）。
- ブランチ `feat/report-image-templates` に3コミットで push（main へは未マージ）。
