# 日報をスクリーンショット・画像から提出する

2026/09/17 ユーザー追加要望。設計・残タスクの登録段階。同日に RIMG-2（原本提出・保存基盤）だけを実装した（[実装の節](#20260917-実装-原本提出保存基盤rimg-2本番未適用)）。migration 169 は本番未適用で、画面の導線・OCR・管理確認は未実装。

## 方針

既存の件数入力に「画像から入力」を追加する。ヤマトの配完個数表などを選び、読み取った日付・件数を確認して提出する。画像を選ぶ前に報告形式を細かく決めさせず、当日のコース・荷主と画像の見出しから候補を絞り、不明な場合だけ選択を求める。既存の手入力は維持する。

**原本の提出とOCRの成功は別**。読めない画像も原本を保存でき、日報は「原本提出済み・件数確認待ち」を区別する。読み取れない値を0件で埋めない。集計・報酬・請求へ反映するのは本人が確認して確定した値だけとし、確認待ちを通常の提出完了として扱わない。

## 既存実装との接続

- Webの土台: `apps/web/src/app/(user)/report/page.tsx`、`/api/reports/v2`、`/api/me/report-form`、`server/reports/courseReportFields.ts`。荷主/コースごとの既存項目・単位へOCR結果を対応付ける。
- 添付の土台: `/api/reports/attachments` と `server/reportKinds/attachments.ts`。現在は「諸報告」の非公開添付（PDF/JPEG/PNG）、内容形式の検査・ランダム名・上書き禁止を持つ。日報との原本関連付け、日時の取得元、ハッシュ、OCR履歴は未実装。既存の「報告削除時に添付を消す」処理を原本保持へそのまま流用しない。
- OCRの土台: Webの端末内Tesseract、モバイルのML Kitの既存導入を評価する。既知フォーマットでも精度・速度は実サンプルで計測し、外部OCRへの画像送信は初期案に含めない。
- プレビューは本番日報ページと管理の日報詳細を再利用し、架空画像・架空OCR応答で作る。実際の配完表を公開fixtureへコピーしない。

## 原本と日時

アップロード時に受領したファイルのバイト列を再圧縮・回転・切り抜きせず非公開保存する。縮小表示用とOCR用の画像は派生物として別にする。「原本」はハコ虎が受領したファイルを指し、端末や共有元ですでに編集されたかどうかを保証する言葉にはしない。

| 記録 | 取得元・扱い |
|---|---|
| 受領日時 | サーバー時刻。常に記録。画像作成日時・報告対象日と混同しない |
| 画像の作成日時 | 取得できたEXIF/画像内メタデータ、または写真ライブラリの作成日時。値・取得元・タイムゾーン/不明状態を保存 |
| ファイル更新日時 | Webの `File.lastModified` など。作成日時の代用にはしない |
| 画像に記載された対象日 | OCRの候補値と本人の確認後の値を別に記録 |
| 原本の属性 | サーバー計算のSHA-256、サイズ、検出MIME、画像寸法、元ファイル名、保存ID、提出者・所属・対象日報 |
| 確認履歴 | OCRの抽出値・テンプレート版・修正後の値・確認者・確認時刻 |

作成日時を取得できないスクショは「不明」のまま受け付ける。現在時刻を作成日時として捏造しない。Webのファイル選択では写真ライブラリの日時を必ず取得できるとは限らない。ネイティブでは選択画像のメタデータ取得を検証し、日時のためだけに写真ライブラリ全体の権限を必須にしない。

Appleの写真作成日時は変更可能な属性であり、画像内メタデータも信頼済みの証明ではない。ハッシュは**受領後に同じファイルか**の照合に使い、撮影日時や報告内容の真実性を保証しない。

## OCRと確認画面

1. 「画像から入力」→スクショ/写真を選択。原本を保持して送信待ち状態を出す。
2. 荷主・コースと見出しからテンプレート候補を特定。ヤマトの配完個数表を最初の対応候補とする。
3. 見出し・表の罫線・相対位置で読む範囲を絞る。固定ピクセル座標だけに依存せず、画面サイズ・倍率・余白・テーマ・版を検査する。
4. 配完・持出・不在・返品などを混同しない。日別/累計、件数/個数、便・コース、表の合計と明細を確認する。複数枚の重複部分を単純加算しない。
5. 原本と抽出値を見比べ、必要な値だけ直して提出。読み取り失敗・未対応形式では手入力または確認待ちへ。OCRの内部処理説明を常設しない。

テンプレートは版を付け、確定済み日報を再OCRで自動上書きしない。同じ原本の再送は同じ提出要求として扱い、二重日報・二重集計を防ぐ。別日・別人への原本流用や日付不一致は要確認とし、画像の日時だけで提出拒否・不正判定はしない。

## 保存・権限・復帰

- 原本の保存IDをサーバーで発行し、本人・所属・日報への関連付けを検証する。他人の添付パス指定、別会社での流用、署名URLの無認可発行を拒否する。管理画面の閲覧/書込権限も既存と揃える。
- 原本を差し替えず、訂正は新しい提出版を追加して旧版との関係を残す。誰が訂正したかと集計への採用版を記録する。
- アップロード途中・OCR中・送信失敗・オフラインを区別し、再試行で原本や提出が増殖しない。未提出の一時ファイルは猶予を設けて掃除し、提出済み原本は別の保持方針を適用する。
- 原本の保存期間、訂正/削除の権限、個人情報を含む画像の閲覧範囲は実装前に決定する。「原本を残す」を無期限保管と決めつけない。

## 残タスク

| ID | 順序 | 完了条件 |
|---|---|---|
| RIMG-1 | 対応形式と項目の確定 | ヤマト配完個数表の実サンプルを非公開で照合し、画面の版・列の意味・日別/累計・既存日報との対応を確定 |
| RIMG-2 | 原本提出・保存基盤 | 非公開保存・日時取得元・ハッシュ・日報の版/権限・再送/失敗復帰を検証。手入力と併用可能 |
| RIMG-3 | 既知形式のOCR | 検出/抽出/合計検算・未対応判定、端末別の時間と精度を計測。原本と結果の確認UIを用意 |
| RIMG-4 | 管理確認・集計 | 確認待ち・修正履歴・採用版・原本閲覧、他社/他人拒否。確定値だけを既存集計へ反映 |
| RIMG-5 | モバイル統合 | 写真選択/共有、取得可能な作成日時、通信失敗・オフライン・再送、PC/スマホ/iPadの実画面プレビューを検証 |

Webの原本提出はモバイル全面移行を待たず着手可能。RIMG-1のフォーマット確認前にOCR精度・速度を確約しない。

## 2026/09/17 実装: 原本提出・保存基盤（RIMG-2・本番未適用）

**migration 169 は未適用で、本番の動作は変わっていない。** OCR（RIMG-3）と管理確認（RIMG-4）は未着手。

| 実装したもの | 場所 |
|---|---|
| 原本の記録、読み取り・確認の履歴 | `supabase/migrations/169_report_source_images.sql` |
| 受け取りの検証・保存（純粋関数＋保存） | `apps/web/src/server/reports/sourceImages.ts`（`sourceImages.test.ts`） |
| API | `POST|GET /api/reports/source-images` |

決めたこと:

- 添付（`report_kind_answers`）とは**別の表・別のバケット**（`report-source-images`）にした。報告を消したら添付も消す既存の後始末を、原本の保持へ流用しないため。
- 4つの日時を別の列で持つ。取得元が `unknown` なら作成日時は NULL のまま（CHECK 制約で現在時刻の混入を防ぐ）。出どころのない作成日時は「不明」に落とす。
- SHA-256 はサーバー計算。同じ人・同じ識別子の再送は**元の提出を返して原本を増やさない**。別日・別人への同じ原本は `other_date` / `other_driver` として返し、**提出は拒否しない**（要確認にするだけ）。
- 形式は中身（マジックバイト）で判定する。申告 MIME・拡張子は使わない。元ファイル名は記録だけで、保存パスには使わない。
- 訂正は原本を差し替えず `supersedes_id` で前の提出を指す。同じ提出枠で中身の違う画像を出し直した場合も、一意制約に当てず新しい行を足して前を指す。集計へ採用する版は `report_source_image_readings.adopted` に1版だけ（部分ユニーク索引）。

未実装・要決定:

- **RIMG-1 はサンプル待ち**。ヤマト配完個数表の実サンプルが手元に無いため、列の意味・日別/累計・既存日報項目との対応を確定できない。ここが決まるまで OCR の精度・速度を約束しない。
- 日報画面の「画像から入力」導線、縮小表示・OCR用の派生画像。バケット `report-source-images` は migration 169 で作る。
- 原本の保存期間、訂正・削除の権限、個人情報を含む画像の閲覧範囲。

## 一次資料（2026/09/17 確認）

- [Apple PHAsset.creationDate](https://developer.apple.com/documentation/photos/phasset/creationdate)
- [Apple PHAssetChangeRequest.creationDate](https://developer.apple.com/documentation/photos/phassetchangerequest/creationdate) — 作成日時は変更可能
- [MDN File.lastModified](https://developer.mozilla.org/en-US/docs/Web/API/File/lastModified) — ファイルの更新日時
