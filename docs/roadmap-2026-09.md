# 作業洗い出し 2026-09（AI作業の加速 × セキュリティ精査の裏取り）

2026-09-06 作成。2本の ChatGPT スレッド（認証不要プレビューの設計論、Codex によるセキュリティ精査の要約）を hakotora の実コードに照らして整理した。未完タスクの従来の正本は `docs/roadmap-2026-07.md`。ここは「これから着手する順番」を決めるための一覧で、各項目は着手時に設計文書へ昇格させる。

精査レポート本体（`HakoTora-Security-Review-2026-09-05.md`）はリポジトリに保存されていない。**最初に Codex 側から取り出して `docs/security/` に置く**（根拠・再現手順を失わないため）。

## 直近の進行順（2026年9月17日 18:36 整理）

現在の作業ツリーには、公開済みコードのmain同期準備、B-7/B-8/日報移行案内、B-6のPIN停止候補、シフトメモ正式反映、車両モデル/車検証読取が混在している。**このdirty tree全体をそのまま公開しない**。本番影響を避けるため、次の順に分けて進める。

| 順序 | 状態 | 作業 | 次にやること | 完了条件 |
|---|---|---|---|---|
| 1 | 承認待ち | 公開済みコードのGitHub main同期とApp CI | 公開先 `TTH15/nippo` へのpush承認後、同期用ブランチCI→main fast-forward→Vercel確認 | mainが本番 `aa40e2d` 相当へ追いつき、App CIとVercel確認が通る |
| 2 | 選別して先行公開 | RPC166 + B-7 Passkey管理 + 日報のSMS/Passkey移行案内 + B-8依存更新 | B-6のPIN停止差分を除いた公開差分を本番基点から作る。CSP下のPDF実保存、地図、OCR、実SMS/実端末Passkeyの確認を追加 | 本人が日報からSMS確認・Passkey登録でき、鍵追加/削除は5分以内の本人確認を要求する。PIN停止はまだ入れない |
| 3 | 移行完了後 | B-6 PIN停止 | active DRIVERのSMS確認/Passkey不足0、旧モバイル移行、内部配布確認後にSQL167のガードを通す | ドライバーPINログイン/新規発行/変更が閉じ、救済はSMS/本人確認手順で行える |
| 4 | 本番未公開 | シフトメモの正式反映・日別人数 | migration 165適用手順、正式反映の競合、PNG/PDF端末保存を再確認して公開差分へ切り出す | メモから正式シフトへ安全に反映でき、実PNG/PDFも保存確認済み |
| 5 | 本番未公開 | 車両モデル/車検証読取/部位別色 | GLB採用範囲、OCR原本保存、車両ページのプレビュー、migration 164を公開前に再検証 | 車両の型式・外観・プレート生成が本番データ範囲で崩れない |
| 6 | 一部実装・本番未適用 | 業務事故の事前検知、日報画像提出、駐車検知 | migration 168/169/170 の適用手順を決め、必要人数の編集画面・OCR・モバイル送信を続ける | 原本照合、本人確認、通知、現場端末の検証条件が固まる |
| 7 | 設計のみ | iPadOS・ハコ虎 Base | 初回範囲・識別子・共有端末の利用者切替をユーザーが確定し、RNのプレビュー方式を決める | 現場の一連の操作が Base で完了し、金銭・条件変更APIへ到達できない |

優先度は「公開済み内容の同期」→「PINを止めない認証強化」→「利用者移行後のPIN停止」。本番DB・GitHub・Vercel・EASに影響する操作は、それぞれ送信先と内容を明示して承認を得てから実行する。

## 業務事故の事前検知（2026年9月15日 追加）

ユーザー指定の最優先残タスク: **シフトの入力ミスを当日まで誰も発見できない構造を改める**。今回の豊中照合が一致したこと、メモ反映機能の実装だけで解決済みにはしない。原本との照合・共有する日別必要人数・予定の版に対する本人確認・期限前の未解決一覧を検討する。

関連して、通知の作成/配信/本人確認の分離、通常編集の同時上書きと監査履歴、車両の時間重複/受渡し、旧データを含む過去金額の再計算を残す。根拠・既存対策・完了条件は [業務事故の事前検知](design/operational-risk-detection-2026-09.md)。以下の公開同期・セキュリティ等の優先項目も維持する。

**2026/09/17 追記（実装・本番未適用）**: O-1 のうち「必要人数を配置とは別に共有する」「本人が版に対して確認する」「期限前に未解決を集める」を実装した（migration 168・未適用）。空白を「休み／未入力／人数未確定」に分け、配置が1件も無い日でも不足を検知する。未解決一覧は状態から毎回導くので、閉じても解決するまで消えない。**解消期限は仮値**で、実運用の締切・未確認時の連絡担当・正当な例外の記録方法は未決定。必要人数の編集画面、取り込み時の抽出結果の保存（原本照合の起動）、O-2 の配信追跡、O-3 の通常編集の競合検知は未着手。

## セキュリティB-1〜B-8の更新（2026年9月17日 実装・未公開）

B-1〜B-5の既存対策を再確認し、B-6のPIN全廃候補、B-7の直近本人確認・鍵の追加/削除・本人通知、B-8の依存更新を実装・検証した。**npm監査は全区分0件**。Web/モバイルのPIN UI・変更/認証API・新規発行を閉じるコード、HTTPS/ATS設定、移行ガードSQL167を用意。認証の詳細と検証結果は [B-1〜B-8対応記録](design/security-b1-b8-2026-09.md)。

**本番完了ではない**。active DRIVER 17所属のうち11所属に確認済みSMS/Passkeyがなく、今のコードを一括公開すると再ログインできなくなる。本人の電話確認、旧モバイルの移行・内部配布後にB-6を切り替える。166/167は本番未適用。B-7/B-8を先行公開する場合はB-6を除いた公開差分へ選別する。CSP下のPDF実保存・全管理画面の確認も公開前に残る。下の9月14日/17日早朝の「未実装」は当時の記録で、現在の適用状態は対応記録を正本とする。

iPadOS/Baseと既存の公開GitHub同期の送信承認待ちは維持する。

## 日報の画像提出・駐車検知（2026年9月17日 残タスク・設計更新）

- **日報のスクショ提出 RIMG-1〜RIMG-5**: ヤマト配完個数表などから件数を読み取り、本人確認後に既存日報へ反映。原本は非公開で保存し、画像の作成日時/取得元・ファイル更新日時・サーバー受領日時を区別する。既知形式のOCR、未対応/読み取り失敗、訂正履歴、二重提出、管理確認を設計。[設計・完了条件](design/report-image-evidence-2026-09.md)。実装は未着手。形式を確定する実サンプルの照合から進める。
- **駐車 PARK-M1〜PARK-M5**: 座標を送る保存基盤を先行し、モバイルの配布基盤後に停車/活動変化→確認通知→位置/写真の流れを実装する。配送中の短い停車では通知を乱発しない。駐車写真と車体点検を分け、区画に入れてから左右撮影を強制しない案を検証。GPSだけで区画確定しない。[更新した駐車設計](design/mobile-parking-auto-detect.md)。検知・通知・撮影UIは未実装。

セキュリティの先行指定、シフト入力ミスの事前検知、iPadOS/Baseの既存優先関係を維持する。日報の原本提出はモバイルの全面移行を待たずに進められる。

**2026/09/17 追記（実装・本番未適用）**:

- RIMG-2 の原本提出・保存基盤を実装（migration 169・未適用）。添付とは別の表・別バケットにし、受領/作成/更新/対象日の4つの日時を分け、取得元が不明な作成日時は NULL のままにする。同じ原本の再送は元の提出を返し、別日・別人への流用は「要確認」にするだけで拒否しない。**RIMG-1 は実サンプル待ちのため未着手**で、OCR（RIMG-3）・管理確認（RIMG-4）も未実装。
- PARK-M1 のサーバー側を実装（migration 170・未適用）。座標だけの駐車申告を通し、誤差円が1つの拠点にすっぽり収まるときだけ拠点を確定する。区画は座標から決めない。点の拠点の既定半径は運営の地図と同じ 120m に揃えた。**検知（M2/M3）・通知・撮影フロー・モバイル側の送信は未実装**。

## 招待登録のPasskey優先・PIN経路の整理（2026年9月17日 実装・未公開）

追記: 既存利用者の移行案内を日報 `/submit` に実装（未公開）。SMS確認→Passkey登録をその場で進め、日報の入力・提出を維持。勤務予定・稼働実績の有無では案内を除外せず、個別通知は送らない。B-7/RPC166と案内を先行公開して本人の設定を確認し、11所属の移行前にB-6のPIN停止を一括公開しない。[最新の移行条件](design/security-b1-b8-2026-09.md)。

「特に進めたい」という追加指定を受け、下の共通順序からこの認証導線を先行した。Passkey登録を主操作とし、設定できない場合だけ説明を開いてSMSで続行する。途中再開・SMSログイン後も未登録なら案内し、登録済みは重複要求しない。

招待時・承認時のPIN新規発行は既に停止していたが、本人のPIN変更APIから後付けできたため拒否条件を追加。WebはPasskey/SMSを先頭にし、招待ユーザーのマイページにはPIN欄を出さない。**既存PINの認証API・管理者による手動作成時の初期PIN・旧モバイルは残るため、B-6の全廃完了ではない**。B-7後半の直近本人確認も未実施。[実装・検証・戻し方](design/onboarding-passkey-2026-09.md)。GitHub同期の承認待ちとiPadOSの残タスクは維持する。

## iPadOS・ハコ虎 Base（2026年9月17日 残タスク追加）

参照: [ハコ虎機能整理](chatgpt-conversation://6aaa78eb-d780-83ee-9de6-016d03fd0519)。ユーザー依頼により、**現場運営用アプリ「ハコ虎 Base」の開発を未着手の残タスクとして追加**。ドライバー用は「ハコ虎」のまま、Baseは別アプリとする。シフト入力ミスの事前検知の最優先指定と、公開・基盤の着手順は維持し、Baseの初回範囲・着手時期は設計時に決める。

- **iPad推奨・スマホでも利用可能**。iPadは対面の本人確認・電子契約・手書き署名・研修・複数車両の確認、スマホは車両移動・鍵管理・写真記録・駐車位置更新など、その場の操作を中心にする。画面幅に応じて一覧と詳細の配置・操作導線を設計する。
- **Baseは現場の事実を登録・確認する役割**。請求・報酬確定・控除・振込・単価や契約条件の変更・会社設定・権限管理はWeb側に置く。金銭処理を持たせなくても、会社・担当者・操作ごとの認可と記録は必要。
- 機能候補: 受付・本人確認、確定済み契約の提示と署名、車両/鍵の受渡し、点検・傷の撮影、車両移動・駐車位置、事故/故障受付、研修、未出勤・未返却などの現場状況確認。すべてを初回必須とはせず、既存機能との分担を確認して絞る。

| ID | 残タスク（すべて未着手） | 完了の確認 |
|---|---|---|
| BASE-1 | 業務範囲・初回機能の設計 | ハコ虎 / Base / Webの分担、現場で最初に完結させる一連の操作、利用者・共有端末の運用、金銭処理との境界を確定 |
| BASE-2 | 別アプリの構成・配布準備 | 既存React Native / Expo資産と共有パッケージの再利用範囲を調査。モノレポ内の配置、アプリ識別子、認証、iPad/iPhone向け設定、内部配布方法を決めて基盤を用意 |
| BASE-3 | iPad・スマホの操作プレビュー | 既存の対象画面・共通UIを土台に、本番認証・DB・API・通知から隔離した架空データのプレビューを用意。iPadの縦横・分割表示とスマホ幅で、遷移・編集・保存・空・読込中・エラーを監査 |
| BASE-4 | 初回機能とAPI・認可の接続 | BASE-1で選んだ業務を実装し、会社・担当者の範囲、共有端末の利用者切替、操作記録、金銭/条件変更APIへ到達できないことを検証 |
| BASE-5 | iPadOS実機検証・内部配布 | 対象端末でカメラ・署名など採用機能の対応可否を確認。権限拒否・通信失敗・再試行・保存復帰とiPhoneでの現場操作を検証し、内部配布で一連の業務を完了。QR/NFC等は端末別の対応確認後に採否を決める |

**2026/09/17 追記**: BASE-1 の分担案と BASE-2 の構成調査を [ハコ虎 Base の業務範囲と構成](design/hakotora-base-2026-09.md) にまとめた。境界は「その場で起きた事実を記録・確認するのが Base、事実から金額や条件を決めるのが Web」。初回範囲は**車両と鍵の受渡し1本**を提案（O-4 のリスクに直結し、金銭に触れない）。`apps/base` は workspaces に置けば取り込まれるが、metro / babel / tsconfig / root の typecheck / `.vercelignore` に追記が要る。**アプリ実装・プレビュー作成・配布は未着手**で、初回範囲・識別子・共有端末の利用者切替・EAS・RNのプレビュー方式はユーザーの確定待ち。

ドライバーアプリの既存移行計画は [モバイル完全移行ロードマップ](mobile-migration-roadmap-2026.md) を参照する。

## 公開・基盤の着手順（2026年9月14日時点）

本番DB163とコミットb009d45のWeb公開が完了。B-1のDB公開権限、B-2の所属失効、B-7前半のPasskey再送防止に加え、B-3/B-4/B-5とNext.js/sharp修正を本番へ反映した。後段の古い順序・未適用表記は当時の記録として扱う。

| 状態 | 作業 | 確認結果 |
|---|---|---|
| 本番反映済み | B-3: 他社データの保護 | 他社IDの承認・却下・関連更新を拒否。正式な当日貸与車は維持。DB163で報告種別を各社4件に分離し、既存設定/既定IDを保持。48テーブルのAST検査とAPI回帰を確認 |
| 本番反映済み | B-4: 非公開ファイル | 認可済み会社・本人・ドライバーの保存範囲を保存/署名/削除で強制。他社パス・不正パス・外部URLを拒否。17テスト通過 |
| 本番反映済み | B-5: 請求書の住所表示 | br改行を保ち文字列として描画。タグの非実行を確認。通常・長文・特殊文字・多行をPDF実ファイルで確認 |
| 本番反映済み | B-8の最優先分: Next.js/画像処理 | Next.js 16.3.5、sharp 0.35.4（libheif 1.23.2）。9月14日のnpm監査で対象指摘0、critical 0 |

全1,177テスト、型、本番ビルドを通過したコードをVercelでもビルドしReadyを確認。対象API一時停止→DB163→Web公開→復帰の順で適用し、ログイン200・未認証API401・画像最適化200を確認。[公開記録](deployment/tenant-scope-163.md)。

| 次の順序 | 作業 | 完了の確認 |
|---|---|---|
| 1（準備済み・送信承認待ち） | 公開内容のGitHub main同期とアプリCI | 9月17日、本番aa40e2dを基点に同期用68e62bcを作成。1,184テスト・Web/プレビュー/モバイル型・48テーブルの所属検査・Node.js 24ビルド成功。公開GitHubへのpushは自動承認審査で拒否され、mainは72842f1のまま。承認後にブランチCI→main同期→Vercel確認。[同期記録](deployment/main-sync-ci-2026-09-17.md) |
| 2（実装・検証済み、未公開） | B-8: 依存更新の公開 | 9月17日の更新後監査0件。Expo57/React19を維持。公開差分の選別と配布物の確認を残す |
| 3 | UI・PDF・画像の監査と統一 | 請求書の改ページ/保存失敗/閲覧権限→QR単体/一括PDFの圧縮・画像待機→受託者控→シフトメモ/日別配車の補正と後片付け。実PNG/PDFを検証 |
| 4（実装・検証済み、未公開） | B-7後半: Passkey管理の公開 | 5分以内のSMS/Passkey、最後の鍵保護・同時削除防止・本人通知を実装。RPC166適用→選別したWeb公開→実端末確認を残す |
| 5 | モバイルの配布・認証準備 → B-6のPIN廃止 | EAS接続・本番HTTPS設定・ATS例外除去・SMS/Passkeyを整え、内部配布で確認してからPIN経路を閉じる |
| 6 | 駐車位置・車両移動・地図の仕上げ | DB157の3関数本体は現物と一致するが台帳未記録。未適用と断定して再実行せず、制約/権限と正規操作を確認して記録を整える。駐車の実機測位・番号表示・後片付けを検証 |

- [週次レポートの照合結果](development/weekly-report-review-2026-09-14.md): GitHub Actionsは0件ではなくGitHub Pagesの公開処理が存在。アプリのテストCIとは区別する。DB157も台帳だけでは未適用と判断できない。
- UI監査は各改修の標準手順。[監査記録](development/ui-export-audit.md)の45行請求書の改ページ・合計欄反復は未解決。住所対策の公開と帳票全体の修正完了を混同しない。
- 請求書再作成・二重控除・過去単価は、対象月と契約期間の最新照合、変更前後の試算、運用の回答を揃えて進める。週次レポートの金額/件数は過去調査値で、今回再計算していない。個人別の内容は公開文書へ転記しない。
- 各画面の改修時は本番コードを使う隔離fixtureを追加・更新する。シフト自動作成はメモとの役割・正式シフトへの反映方法を整理してから着手する。

## 車両登録・型式別GLB（2026年9月14日 相談・設計案）

制作元の現行台帳は20仕様、ハコ虎の接続は3車種。型式入力と保存経路は既存だが、世代別表示と車検証読取は未実装。型式を任意に保ち、写真/記録事項PDFから確認付きで登録する案を整理した。[設計案・制作台帳の照合](design/vehicle-registration-and-models-2026-09.md)。上記の公開同期・セキュリティ等の残タスクも維持する。

| 順序 | 作業 | 完了の確認 |
|---|---|---|
| V-1 | 型式とGLBの接続 | 型式正規化・世代/OEM/外観の対応表、旧キーとの互換、地図/編集プレビュー/プレートの一致 |
| V-2 | 車検証登録の隔離プレビュー | 既存車両ページを使い、読取候補の確認・修正・未読・失敗・重複をPC/スマホで操作 |
| V-3 | 写真/PDFの読取接続 | 読取方式と原本保存方針を決め、権限・会社範囲・実書類の精度・失敗復帰を検証 |
| V-4 | 既存車の型式補完 | 既存値との差分確認、GLB変更に伴うプレート再生成とキャッシュ更新。公式JSON取込は追加候補 |

今回は調査・設計案まで。制作元は全20仕様とも台帳上 `in-progress` であり、そのまま本番採用可能とは判定していない。

## 判断: 認証バイパスの本番モードは作らない

ChatGPT 案の `PREVIEW_AUTH_BYPASS=true` は採用しない。hakotora は dev Supabase を持たず、ローカルも本番 DB 直結（`.env.local`）のため、認証を抜いた同一アプリを立てると「認証なし → 本番 DB」の経路そのものになる。代わりに既存の隔離 runner（esbuild で本番ページを束ね、API・認証・Router だけを fixture に差し替え、CSP で外部送信を遮断）を、ChatGPT 案の要点である **同一コード・fixture・シナリオURL・役割URL** で拡張した（2026-09-06 実装済み・`docs/development/preview-workflow.md`）。

上段は9月6日時点の環境説明。9月12日時点では専用の開発DBでmigrationを検証できるが、通常のローカルアプリと本番接続を混同しないよう隔離runnerを引き続き使う。

## A. プレビュー基盤（AIのコード→ブラウザ→修正ループを速くする）

| # | 作業 | 目的・内容 | 規模 |
|---|------|-----------|------|
| A-1 | fixture の追加（シフト／報告／売上／請求書／記録） | 改修頻度の高い画面を `/preview/admin/<slug>` で開けるようにする。シフトは既存の `scripts/previews/shifts-services.tsx` を fixture 形式へ移し、`-- shifts` をエイリアス化 | 中 |
| A-2 | 日付依存ページの固定 | `todayJST` を読むページ（ダッシュボード・日報）が日によって見た目が変わる。`?date=` で「今日」を固定できるよう `@/lib/date` を runner で差し替える | 小 |
| A-3 | 全シナリオ巡回の手順化 | 一覧の全リンク（ページ×シナリオ×役割）をアプリ内ブラウザで順に開き、崩れ・console エラーを拾う手順を `preview-workflow.md` に固定。ui-auditor サブエージェントの入力にする | 小 |
| A-4 ✅ 2026-09-06 | 本番側の崩れ修正（プレビューで発見） | 車両カードの長い車種名がカード右端を突き抜ける（`/preview/admin/vehicles?scenario=long-name`）。ui-ux-pro-max の指針（省略せず折り返す）で修正済み | 小 |
| A-7 ✅ 2026-09-07 | 地図の3車種取り込みと夜のヘッドライト | keivan-3d のハイゼット19／エブリイ88／アクティ75 を `finish-glb-for-mapbox` → 3分割（車体・固定色・灯火）で `VEHICLE_MAP_MODELS` に登録し、`model_key` を車種単位で接続（型式は扱わない）。灯火層は夜（17〜5時）だけ発光させ、稼働中の車のヘッドライト・テールランプを地図上で点ける（2026-09-06 ユーザー要望）。材質名が HH5 規約と一致するかを先に確認 | 中 |
| A-5 | Next の `/preview/*` 一覧との統合 | `apps/web/src/app/preview/page.tsx` から runner の起動方法と URL 規約へ誘導する（ポートが違うため直リンクは不可） | 小 |
| A-6 | fixture の再利用 | `fixtures/users.ts` の架空ドライバー・コースを他ページの fixture からも使い、画面間で同じ人物・車両が出るようにする | 小 |

## B. セキュリティ（Codex 精査の裏取り → 修正）

精査の主張をコードで確認した結果を「裏取り」に記す。修正順は「DB公開権限 → 共通認可・失効 → 他社更新／ファイル参照 → 管理画面のHTML描画 → PIN・Passkey」で、精査の提案と一致する。**金額・認証に触る変更は、影響ユーザーと戻し手順を先に書いてから着手する。**

| # | 指摘 | 裏取り（2026-09-06） | 起こり得る実害 | 作業 |
|---|------|----------------------|----------------|------|
| B-1 ★最優先 | anon key を配る経路に対し DB 権限・RLS が不十分 | `api/admin/map/share-session/route.ts` が `SUPABASE_ANON_KEY` を返す設計（地図共有ビュー Stage1）。RLS は全 migration で未使用。`REVOKE ... FROM anon, authenticated` は migration 154〜157 の新規テーブル・関数だけ | anon key と URL を知る誰でも、Supabase 既定の権限が残る全テーブルへ REST で直接アクセスできる可能性（全社データ） | 1) 本番 DB で `information_schema.role_table_grants` を anon／authenticated で棚卸し 2) `REVOKE ALL ON ALL TABLES/FUNCTIONS IN SCHEMA public FROM anon, authenticated` ＋ `ALTER DEFAULT PRIVILEGES` の migration 3) 共有ビューは anon 直読みをやめ、サーバー経由の署名付きスナップショット or 専用 RPC（SECURITY DEFINER で org・期限を検査）に変更 4) `SUPABASE_ANON_KEY` の本番設定有無を確認（未設定なら 3 まで設定しない） |
| B-2 | 停止・削除済み利用者を共通権限チェックが通す／承認待ちユーザーが業務APIを使える | `requireAuth`（`src/server/auth/index.ts`）は `authProvider.verify` の JWT 検証のみで、membership の status／削除を DB 照合していない。capability は発行時点の束 | 退職者・削除済み ADMIN・盗難トークンが期限まで有効。承認待ち（PENDING）が自社データ取得や出勤記録作成に到達し得る | 1) `verify` 後に membership を軽量照合（status∈{active}, deleted_at IS NULL, org 一致）。短 TTL キャッシュ可 2) 停止・削除・ロール変更時にトークン世代（`token_version`）を上げて旧 JWT を失効 3) PENDING は `/api/join/*` と本人プロフィール以外を 403 4) 影響: 全ユーザーの再ログインは不要にする（世代は変更時だけ上げる） |
| B-3 | 他社報告の承認・却下で関連データ（車両走行距離・経費）を変更できる | `daily/approve`・`daily/reject` は `resolveOrgId` で org を絞っている（精査の主張とは異なる）。ただし approve 内の車両更新は `.eq("id", vehicleId)` のみ（report 行由来なら安全）。**報告種別（misc-reports／report-kinds）の承認経路は未確認** | 他社の報告 ID を知る運営が、他社車両の走行距離や経費を書き換える | 1) `npm run check:tenant`（既存の静的検査）を全 admin ルートに拡張し、id 指定 UPDATE／DELETE に org 条件が無い箇所を列挙 2) misc-reports／report-kinds／attendance の承認・却下を org 付き UPDATE に統一 3) itest で他社 ID を投げて 404 になることを固定 |
| B-4 | 他社 Storage パスを受け入れて署名する | `createSignedUrl` は `server/kyc/storage.ts`・`vehicleQr/*Storage.ts`・`reportKinds/attachments.ts`・`storage/dataUrl.ts` の5箇所。パスの org 接頭辞を強制しているかは未確認 | 他社ファイルのパスを知る利用者が、自社請求書経由で非公開添付の閲覧URLを作らせる | 各署名関数に「`<org_id>/` 接頭辞の一致」をサーバーで強制し、DB の添付行から path を引く（クライアントの path をそのまま使わない） |
| B-5 | ドライバーの住所が請求書で HTML として描画される | `InvoiceSheet.tsx` が `toAddrHtml`／`fromAddrHtml` を `dangerouslySetInnerHTML` で描画。`addrHtml()`（`editorModel.ts:545`）は `〒${p}<br/>${a}` の連結で**エスケープしていない** | 細工した住所（会社設定・ドライバー編集から入る）を管理者がプレビューするとスクリプト実行。本番 CSP の有無で実害の大きさが変わる | 1) `addrHtml` を廃止し、住所は文字列＋`whitespace-pre-line` で描画（`<br/>` は改行へ変換） 2) 既存保存データ（`toAddrHtml` に HTML が入っている）を移行時にタグ除去 3) `next.config` の CSP に `script-src 'self'` を入れる（PDF 生成への影響を確認） |
| B-6 | PIN 廃止を API が保証していない／初期 PIN がコード末尾 | `api/auth/login` は `pin_hash` があれば通す。`admin/users`・`admin/users/[id]`・`reports/profile` が `pin_hash` を発行・変更できる | PIN 未設定の利用者が後から PIN を作って PIN ログインでき、推測可能な初期 PIN が残る | 精査の順番どおり: 1) 既存ユーザーの電話認証・Passkey 状況を管理画面で可視化（`users` の `phone_verified_at`／`has_passkey` は既にある） 2) mobile を SMS OTP／ネイティブ Passkey へ 3) Web ログイン画面からコード＋PIN 欄を外す 4) `login` の driver PIN 経路と PIN 発行 API を閉じる 5) `pin_hash` を無効化。旧 Vercel URL は hakotora.jp へリダイレクト |
| B-7 | Passkey: 認証応答の再利用（counter=0）／盗んだセッションから Passkey 登録 | `webauthn/login/verify` は `counter` を保存・更新している。チャレンジの一回限り消費と 5 分の有効期限の実装は未確認。登録 API が直近の強い本人確認を要求しているかも未確認 | 有効期限内に完全な認証応答を入手されると 2 回ログインできる。一時的な乗っ取りを恒久化される | 1) チャレンジを使用時に削除（`DELETE ... RETURNING` で原子的に） 2) counter=0 同士は許容しつつ、同一チャレンジの再提出を拒否 3) Passkey 追加・削除は直近 5 分以内の SMS OTP or Passkey 再認証を要求 4) 新規登録を通知（LINE／SMS） |
| B-8 | Next.js の画像処理アドバイザリ | `next ^16.2.0`。該当バージョンと修正版は要確認 | 細工画像が最適化処理へ届いた場合の DoS／メモリ破壊 | `npm audit` と Next 公式告知の突合 → パッチ版へ更新（`vercel:next-upgrade` スキル） |

## C. 既存の未完（参照のみ）

- migration 135（チャット集計）・152（単価再構築）・157（車両移動）が未適用。157 は SQL Editor で再実行待ち
- 地図の車両移動・空中アーチ（未コミット差分あり）。`git status` の `map/` 系はこの作業のもの
- 認可の own 化（本人系ルート約 33〜41 本が `requireAuth` のみ）。B-2 と同時に進めると効率がよい

## D. 残タスク全体像（2026-09-08 整理）

Passkey・モバイルログイン・セキュリティ・モバイル画面・駐車位置・地図作戦盤の残タスクを 6 トラック（S セキュリティ／A 認証／M モバイル／P 駐車／K 地図／X 基盤）に束ね、着手順と判断待ちを1枚にまとめた。閲覧用: [ハコ虎 残タスク作戦盤](https://claude.ai/code/artifact/4f5d995c-87e6-4fd2-8652-44e27d5b34c2)（Claude アーティファクト・要ログイン）。設計の正本は次の文書。

- 地図の監査結果と段階: [design/map-board-usability-2026-09.md](design/map-board-usability-2026-09.md)
- モバイルの駐車自動特定: [design/mobile-parking-auto-detect.md](design/mobile-parking-auto-detect.md)
- 認証: [platform-design.md §2-1a](platform-design.md)、モバイル: [mobile-migration-roadmap-2026.md](mobile-migration-roadmap-2026.md)

着手順の提案: 1) B-1 の棚卸し（✅ 2026-09-08 migration 159 で完了） → 2) 地図 段階1「見える」（次の一手） → 3) B-7 Passkey 応答の一回性（challenge token が 5 分の JWT で消費されないことをコードで確定） → 4) B-2 失効 → 5) モバイル足回り（eas init・env・ATS） → 6) 駐車自動特定 M1 → 7) 地図 段階2「探せる」 → 8) PIN 撤廃とネイティブ Passkey（Apple 待ち）。

2026-09-08 の棚卸しで判明した事実: `feat/mobile-capture-flow`（VisionCamera 版の撮影フロー・4 コミット）が未マージ・未 push。main は expo-camera の静止画版。取り込み方は判断待ち（J-3）。

## 進め方の提案

1. **今週**: B-1 の棚卸し（読むだけ・書き込みなし）と精査レポートの保存。A-4 は 30 分で終わるので同日に
2. **次**: B-2（照合＋失効）→ B-3（check:tenant 拡張）。両方とも itest を先に書く
3. **その後**: B-5・B-4 → B-6（利用者影響があるので告知とセット）→ B-7
4. A-1〜A-3 は上記の合間に。改修する画面の fixture を「その画面に触る直前」に作るのが最も無駄がない
