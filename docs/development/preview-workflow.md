# ログイン不要プレビューの標準手順

2026-08-31 ユーザー指定。新機能・UI改善・不具合修正を含む今後の作業で、依頼のたびに確認せず適用する。入口はルートの `AGENTS.md`。

画像・PDF出力の方式選択、文字下ずれの切り分け、生成ファイルの確認項目は [画像・PDFエクスポートの共通方針](image-pdf-export.md) を併用する。

1. 対象の実画面のソース・表示・遷移・必要な状態を確認し、`apps/web/src/app/preview/<feature>/` にモックを作成、または既存プレビューを更新する。内部処理の変更も、影響を確認できる画面・ケースを用意する。
2. **実画面のUIを再利用・複製する。** 共通部品を使うだけでは不十分。既存のロゴ・サイドバー・メニュー構成・余白・見出し・表の密度と表示期間・編集方法を維持し、今回の追加・変更だけを載せる。認証やAPIと密結合している場合は、表示用JSXを複製して入出力をローカル状態に置き換え、複製元をコメントとこの文書へ記録する。独自デザインで別画面を作らない。架空データの通常・未設定・空・複数所属・エラーを含め、保存先・リセット方法・未実装範囲を明示する。
3. 本番の認証・API・DB・通知は使わない。認証を無効化した本番画面を作らず、独立したモック状態を使う。実在データ・秘密情報を埋め込まない。
4. 起動コマンドと入口を残す。共通ランナーは `npm run preview:admin -- <feature> --port 3191`。`--build` はビルドのみ。標準は `driver-leases`、入口は各フォルダの `page.tsx`（ブラウザのみで動作するコンポーネント）。
5. 型チェック・必要なロジック検証・ビルドを行い、アプリ内ブラウザで画面遷移、編集・保存・取消、絞り込み、エラー、PC/スマホ幅を確認する。実画面のスクリーンショットと実装も照合し、変更対象外のUIを勝手に変えていないことを確認する。外部へ送る操作は原則モックに置き換える。今回ユーザーが依頼したMapbox地図・住所検索に限る接続モードは下記に記録し、本番APIや通知送信の許可とは分ける。
6. ブラウザタブを成果物として残す。URL・再起動方法・確認済み操作・制限を伝え、月別作業ログと索引を更新する。

ランナーは127.0.0.1だけで待ち受け、生成したHTML/CSS/JSと許可リストにある公開アセット（公式ロゴ・ナンバープレートのSVG/フォント）だけを配信する。通常モードは環境ファイルを読み込まず、CSPの `connect-src 'none'` によりAPIへの通信も遮断する。生成物はOSの一時ディレクトリに置き、ソースと起動手順はリポジトリに残す。変更時はサーバーを再起動して再ビルドし、未保存入力を保存・破棄してからブラウザを再読み込みする。

## 本番ページをシナリオURLで開く（2026-09-06）

管理画面の本番 `page.tsx` を**そのまま**使い、認証・API・Next Router だけを fixture（架空データと読み書きの定義）へ差し替えて動かす runner。ページ・データ状態・閲覧者の権限を URL だけで指定できるので、AI や人が「この状態の画面を開いて直す」を1手で始められる。ChatGPT で整理した方針（別コピーを作らず同一コード・fixture・シナリオURL・役割URL）を hakotora の既存 runner に載せたもの。

- 起動: `npm run preview:admin -- admin --port 3197` → `http://127.0.0.1:3197/preview/admin`（一覧）
- ページ: `/preview/admin/<slug>`。登録済みは `dashboard`（/admin）・`vehicles`（/admin/vehicles）・`users`（/admin/users）
- データ状態: `?scenario=normal|empty|long-name|large|loading|error`（fixture 固有＋共通の loading/error。未知の値は normal）
- 権限: `?role=admin|accounting|viewer`（本番のプリセット ADMIN / ACCOUNTING / ADMIN_VIEWER の capability 束を写す。メニューのロック・書込ボタン・コスト表示が本番と同じ判定で変わる）
- 例: `http://127.0.0.1:3197/preview/admin/vehicles?scenario=long-name&role=viewer`
- 画面上部のバーでシナリオ・役割の切替、「次の保存を失敗させる」、「初期化」ができる。サイドバー・クイックリンクは pushState で遷移し、scenario/role を引き継ぐ。未登録ページへのリンクは一覧へ戻して案内を出す
- 旧コマンド `npm run preview:admin -- vehicles` は同じ bundle の `/preview/admin/vehicles` を開くエイリアス（`scripts/previews/vehicles-services.tsx` は fixture へ統合して削除）

### 仕組みと追加方法

- 差し替えるのは `@/lib/api`・`swr`・`swr/infinite`・`next/link`・`next/navigation`・`next/image`・`next/dynamic`・`AdminLayout`・`VehicleModelPreview` だけ（`scripts/serve-admin-preview.mjs` の `adminReplacements`）。`@/lib/useApi`・`@/lib/swr`・`@/lib/capabilities` は本物を使うので、本番コードとの乖離は最小
- 純粋ロジックは `apps/web/src/lib/preview/`（`scenario.ts`: URL解析・役割→capability、`fixtureStore.ts`: 読み書き・loading/error・保存失敗・リセット）。vitest で検証済み
- runner 側は `scripts/previews/kernel/`（差し替えモジュール）・`scripts/previews/fixtures/`（ページごとの fixture）・`scripts/previews/admin.tsx`（URL→ページの振り分けと一覧）。型は `npx -w apps/web tsc -p ../../scripts/previews/tsconfig.json --noEmit`
- ページを増やす: `scripts/previews/fixtures/<page>.ts` に `PreviewFixture`（`scenarios` / `createState` / `read` / `write`）を書き、`fixtures/index.ts` に1行足す。ページが読む API キーは `grep -n '/api/' <page>.tsx` で列挙し、`read` で返す。対象外のキーは自動で「プレビュー対象外」エラーになるので、足りない分は画面のエラー表示で分かる
- 本番ページが `process.env.NEXT_PUBLIC_*` を読む場合は runner の `define` に空文字で足す（会社コードは DEFAULT 扱い）
- ブラウザの CSP は `connect-src 'none'`。外部送信・本番API・通知は物理的に到達しない

### 2026-09-06 の確認

PC 1280 幅と 375×812 で一覧・車両（通常／長い名前×閲覧のみ／大量／取得エラー）・ドライバー一覧（通常／読み込み中）・ダッシュボード（通常）を表示。ダッシュボードのクイックリンクから車両へ pushState 遷移。警告・エラー 0 件。副産物として、車両カードで長い車種名がカード右端を突き抜ける本番側の崩れを確認した（未修正・`docs/roadmap-2026-09.md` に記載）。

## 今回のプレビュー

車両移動・受け渡しと通知時刻の新しい検討用画面は、本番シフトを直接使う `/preview/shifts` の「車両移動を試す」から開く。下記の旧リースプレビューとは本番反映の状態を混同しない。詳細は末尾の「車両移動・受け渡しと時刻指定通知」を参照。

- 機能: リース契約・ラベル・シフト・一時貸出
- Next.jsの入口: `/preview/driver-leases`（認証不要・モックのみ）
- 推奨起動: `npm run preview:admin -- driver-leases --port 3191`
- Mapboxの実地図・住所検索を試す: `npm run preview:admin -- driver-leases --port 3191 --mapbox`（今回の依頼で追加。通常モードと同じポートのサーバーは先に停止する）。
- データ: 架空のドライバー・車両・コース・2026-08-31〜09-30の予定。初期表示は9月前半。画面内保存のみ、再読み込みで初期化。
- 通知: 文面の表示だけ。DB更新・請求確定・通知送信は行わない。Mapboxモードでは地図と住所検索のみ外部通信する。
- 既存メニューは全体配置の確認用に残す。操作対象外の画面・シフトメモ・設定・新規ドライバー追加は案内を表示し、本番へ遷移しない。出力は下記の日別PNGのみを実装し、半月表/PDFは対象外。

### シフト表の車両過不足と重複割り当て（2026-09-06）

- 起動: `npm run preview:admin -- shifts --port 3198` → `http://127.0.0.1:3198/preview/shifts`。上部バーの「田中に佐藤と同じ車両を割り当て」で重複を作る（再押下で解除）。
- 確認: PC 両軸ヘッダーとスマホ日別ヘッダーの「稼働 N」の下に「車 余りN台／不足N台／ちょうど」。重複したナンバーは赤枠（`data-duplicate-vehicle`）。架空データは車両12台・毎日1人休みなので余り1〜2台になる。不足の表示はロジックのテスト（`shiftVehicleBalance.test.ts`）で担保。

### 車両地図・移動作戦盤（2026-09-02）

- 入口: `/preview/map-operations`。`npm run preview:admin -- map-operations --port 3196 --mapbox` で起動する。Mapbox地図と自己ホストのHH5モデルだけを取得し、本番認証・API・DB・通知には接続しない。
- 再利用元: 本番 `/admin/map` のMapbox Standard、3Dモデル、ナンバー札、現在／履歴の考え方と、`AdminPreviewLayout`、`VehiclePlate`、`CustomSelect`、`EditorModal`、`SmoothCollapse`、`CheckboxField`、`TimePicker`。右の車両詳細と空中の移動アーチを今回の検討として追加した。
- 画面内の「この画面で確かめる設計」に、シフトを予定の正本、移動手配を橋渡し、日報・将来QRを駐車実績の入口とする流れを表示する。地図はそれらを見比べて判断する作戦盤とし、独立した正本にはしない。
- 架空の4台で、通常／移動担当未設定／駐車場所未記録、要確認絞り込み、番号・車種検索、いま／車両移動／履歴、2D／3D、選択車両への近接表示、駐車記録、移動手配の画面内保存を試せる。「車両移動」では未完了で別の場所へ運ぶ2台だけを表示する。再読み込みまたはプレビュー初期化で戻る。
- 「履歴」は連続スライダーを使わず、日付・時刻の指定と「前の記録」「次の記録」で時点を移す。各車両は指定日時以前の最後の記録へスナップし、記録間の移動を補間しない。右欄も当時の場所・記録日時・記録者・出どころに限定し、現在の次回利用や移動手配を混ぜない。
- 3Dは添付のアクティHH5 Blockout 70を使用。元ファイルは8,602三角形・149メッシュ・33材質で、Mapbox用に幾何を変えず頂点配置を分離し、同材質の描画単位を統合した。さらに車体93KB、固定色部品274KB、車両別プレート36〜38KBへ分けて重ねる。車体色だけを強く出し、プレートは既存SVG字形から生成した画像を前後の実材質面へ直接貼る。車両長は地図幅の9%を保ち、実寸倍率に達した後は等倍、最大ズーム24とする。足元の細いリングで地図との境界を補う。
- 地図上の車両札も実画面の `VehiclePlate` を直接使い、同じSVG字形・配色・縦横比とする。車体前端に画面向きのプレートを浮かせる試作は不採用。
- 選択車両の移動予定は地表の破線を使わず、表示中の車両位置から届け先を画面へ投影した白縁付きの太い空中アーチで表示する。モデルより前、ナンバー札より後ろに置く。始点・行き先が画面外なら線だけをそれぞれ地図外へ抜き、実座標が表示されたときだけ始点の丸・矢尻を出す。車両倍率・札・アーチをMapboxの `move` ごとに更新し、ズーム・移動・回転・2D／3D切替へ追従。「いま」と履歴では非表示。
- 共通プレートの比率は添付実車写真と `/preview/plate` を比較し、以前の大きな文字比率を維持する。3D生成は `vehiclePlateLayout.json` の同じ上段・かな・一連・字間を使い、SVG内のハイフンを数字と同じカテゴリ倍率で描く。生成後のPNGをGLBから抽出して地図札と見比べる。
- 検証画像: [京都が画面外のPC近接](../design/assets/map-operations-preview/aerial-movement-desktop.png)、[京都の実座標が見える広域](../design/assets/map-operations-preview/aerial-movement-destination-visible.png)、[京都が画面外のスマホ近接](../design/assets/map-operations-preview/aerial-movement-mobile.png)、[補正後の3Dプレート面](../design/assets/map-operations-preview/desktop-plate-proportions-corrected.png)、[共通プレート標準／compact](../design/assets/map-operations-preview/plate-proportions.png)、[GLBから抽出した一致テクスチャ](../design/assets/map-operations-preview/plate-texture-matched.png)、[スマホ記録モーダル](../design/assets/map-operations-preview/mobile-modal.png)。
- 制限: 駐車・移動・履歴は固定の架空データとローカル状態のみ。GPS、日報、QR、シフト保存、通知予約、権限、同時編集、本番の地図設定には未接続。履歴の再生・軌跡・推定区間も未実装。エブリィ等の制作中モデルはモデル登録口を共通化した後に追加する。
- 2026-09-03本番接続: 本番地図側には履歴日時指定と車両移動の登録・変更・取消・完了記録を実装した。空中アーチの描画部品は本番とプレビューで共用する。プレビュー自体は従来どおり固定データであり、migration 157の適用や本番APIの検証には使わない。

### 日報の走行距離チェック（2026-09-02）

- 入口: `/preview/submit-meter`。Next の dev サーバー（`npm run dev` → http://localhost:3001）で開く。静的ランナー `preview:admin` は対象外（実画面が SWR / apiFetch を使うため）。
- 再利用元: 本番の実画面 `src/app/(user)/submit/SubmitPageClientV2.tsx` を複製せずそのまま描画し、`(user)/layout.tsx` と同じ `Nav variant="user"` + `main` の枠に載せる。プレビュー側は `window.fetch` の `/api/` 宛リクエストだけを架空データへ差し替える（本番の認証・DB・API・通知には接続しない。それ以外のリクエストは本来の fetch に委ねる）。
- 架空データ: 走行距離 50,000 km 登録済み（1201）／走行距離が未登録の車両（2752）／EV でメーター欄が出ない車両（4303）の3台と、コース1件・個数2項目のシフト。
- 確認できること: 登録値以下（同値含む）の赤字表示と送信ブロック、未登録車両の初回入力、EV の入力欄非表示、送信後画面への遷移。上部バーに送信試行の回数と「最初から」を置く。
- 制限: 送信しても保存されない（POST はモックが成功を返すだけ）。送信後画面・報酬・通知は最小限の架空データ。

### UIの再利用・複製元

- 外枠: `src/lib/components/AdminLayout.tsx` → `AdminPreviewLayout.tsx`。JSX・クラス・全メニュー・フライアウト・モバイルナビを複製。認証・API・Next Routerを読み込まず、架空の管理者とプレビュー専用遷移に置換。独立したラベル管理・一時貸出のメニューは設けない。
- シフト: `src/app/(admin)/admin/(ops)/shifts/page.tsx` の半月ツールバー・固定列・列幅・希望休・ナンバー付きセル → `ShiftBoard.tsx`。ラベル/リースフィルター、契約区分の区切り行、一時貸出を追加。複数コースの表示も反映。ドラッグ・共同編集は今回のモック対象外。
- シフトの編集: 同ファイルの `editingCell`・`offModal`・`VehicleOptionList` → `ShiftEditor.tsx`、`courseCellModal` → `CourseAssignments.tsx`。コースを＋追加／×解除、プレートから車両を選択して即時反映。希望休解除の確認を維持。月額車を選んだ場合だけ、同じ編集モーダルを再利用した `MonthlyVehicleDialog.tsx` で通知内容を確認する。時間・集合場所・担当可能コース・定員の編集や検証は対象外。
- ドライバー: `src/app/(admin)/admin/(resource)/users/page.tsx` の名簿テーブル・検索・稼働タブ・アバター・編集モーダル → `DriverBoard.tsx` / `ui.tsx`。ラベル・契約欄と一覧内の「ラベルを編集」を追加し、既存属性は架空の表示のみ。
- 共通部品: `MonthYearPicker`、`VehiclePlate`、`CustomSelect`、`CheckboxField`、`SmoothCollapse`、`DatePicker`、`ConfirmDialog` は実物を直接利用。
- 複製したUIは次回更新時に複製元と差分を確認する。本番の認証・保存処理は変更しない。
- 本番反映に向けたコード・SQLの調査と移行順序は [リース・ラベル・配車の本番反映調査](../design/driver-leases-production-rollout.md) を参照。モックの日単位重複禁止や単純な人×日のモデルを本番へそのまま移さない。

### リースの連動を試す（2026-08-31追記）

操作画面は「シフト」と「ドライバー一覧」の2つ。ラベル編集はドライバー一覧内、月額車の一時利用は配車セル内で完結する。実画面から複製したナビ・名簿・半月シフト・編集モーダルは維持する。

- ドライバー一覧 → 「ラベルを編集」で共通ラベルの追加・名前変更・削除。ドライバーを選ぶと複数ラベルの付与・契約を編集できる。削除は確認後に付与先からも外し、戻り先のシフトに削除済みの絞り込みを残さない。
- シフトの名前 → その人の契約設定。シフトのセル → コース追加・解除／配車。通常車は従来どおり即時反映し、空いている月額契約車のプレートを選ぶと確認モーダルを開く。
- 確認モーダルは日付・利用者・車両・契約者を引き継ぐ。受取・返却場所は登録済みの駐車場所から選択（初期候補は豊中車庫、未登録なら空欄）、時刻は初期値入り。貸出前の満タン給油と移動・受け渡しの連絡は必要な項目だけ選ぶ。貸出前の給油は月額契約者を初期依頼先とし、利用者の返却時給油とは分ける。「確認して配車」で確定し、別ページへの移動、日付や借り手の再選択、追加の確認チェックは不要。
- 配車後は選択中のプレート、または貸し手／利用者の「この日の利用内容」から内容変更・取消・返却済みにできる。取消時は元の配車へ戻す。利用記録は内部に残し、月額契約や料金は変えない。
- ドライバー設定の「保存してシフトを確認」 → 選んだ日付の本人のシフト。関連する利用記録も該当日の確認モーダルへ開く。上部の「○○に戻る」で元の画面・表示条件へ戻る。
- 「プレビュー操作」→「試すサンプル」で、月額車を配車／月額同士の貸し借り／整備中の代車／月途中の紐付け変更／日額から月額へ／貸出とシフトの競合／駐車場所が未設定を選ぶ。確認後に保存済み変更も含めて初期化し、対象の編集画面を開く。
- 月額車を配車: 9/7の高橋の車両に1201を選択 → 通知内容を確認して配車 → 両者のシフトへ反映。キャンセルでは未確定の配車は残らない。
- 日額から月額へ: 伊藤を月額・車両5678へ変更 → 保存してシフトを確認 → 契約設定で日額へ戻して比較。
- 貸出とシフトの競合: 9/2の佐藤の全休を「解除」してコースを追加。1201は高橋が使用中で選択できないが、空いている6789で稼働できる。貸出は取り消さず維持する。

ラベルは複数トグルでいずれかに一致する人を表示（OR）。選択済みを押して個別解除し、「すべて」で全解除。ラベル未設定も併用できる。契約・名前とはANDで組み合わせ、画面を戻っても選択を保持。契約選択・まとめる・検索は高さ44px、契約の2操作を隣接させる。

一時利用の取消時に、元の借り手の通常車両が別の人に使われていれば、他者の配車を奪わず元の借り手を未配車にして通知する。貸出は日単位の実配車で重複を防止し、希望休・未割当・別車両での稼働を理由には止めない。受け渡し・次の利用への支障がないことは確定操作で確認する。借り手は月額・日額のどちらも可能。普段の紐付けは予約ではなく、重複や未設定を許可する。時間帯ごとの空き判定は未実装。利用日・利用者・車両の変更は取消後、対象セルから改めて配車する。

普段使う車両の紐付けは変更日ごとに保持する。過去や次の変更予定、既存の配車は書き換えず、貸出済みの貸出側・利用者の契約区分も保持する。月額・日額の切替や紐付けの変更を貸出の存在だけで禁止しない。一時利用がある借り手の「リースなし」への切替は確認を求める。料金契約の適用開始日・期間別履歴・正式な請求スナップショットは未実装で、車両の紐付け履歴とは区別する。

### シフト上部の表示整理（2026-08-31追記）

- ラベルのチェック印とその予約幅を削除。ラベル・表示項目のトグルは色だけで選択状態を示し、読み上げ用の `aria-pressed` とキーボードのフォーカス表示を維持する。ユーザー指定による今回のトグル限定の見た目であり、編集フォームの共通チェック部品は変更しない。
- 「簡易／標準／詳細」を廃止し、「表示」を開いて「シフト」「車両」「集合時刻」を個別にオン・オフする。初期状態はシフト＋車両。PCの表示軸も同じパネルへ移動する。
- シフトは担当コース・時間帯、車両はナンバープレート・持込／未配車・一時貸出の表示を切り替える。集合時刻は従来と同じ架空の07:30固定。PCのドライバー軸・コース軸・スマホ日別に適用する。コース軸では行見出しと割当ドライバー名を常に残し、シフトの切替は時間帯に適用する。
- 項目をすべてオフにしても稼働状態を残す。希望休・未割当や編集入口は消さず、予定・配車データは変更しない。画面を離れて戻っても表示項目を保持する。
- プレビュー専用ナビとサンプルは「プレビュー操作」にまとめ、表示パネルとともに初期状態は閉じる。ロゴ・全体ナビ・半月表・編集方法は従来の複製を維持。
- 確認手順: ラベルを複数選択→個別解除／すべて、表示項目の組み合わせ、コース軸、契約設定から戻った際の保持、プレビュー操作からサンプルを開く。PCと390px幅で操作・はみ出しを確認する。
- 検証済み（2026-08-31 15:59）: ユーザーのローカル接続承認後、アプリ内ブラウザでPC1440×1000／スマホ390×844を確認。ラベルのチェック領域なし・左右12pxの余白、スマホの44px操作領域、ページ横はみ出しなしを実測。個別表示・全項目オフ・コース軸・表示条件の保持・検索0件・サンプル切替の確認と取消を操作確認し、ブラウザの警告・エラーなし。通常幅と初期表示へ戻してプレビューを開いた状態で引き渡し。


### ラベル・月額車の導線整理と検証（2026-08-31）

ラベルの別ページを撤去し、ドライバー一覧の「ラベルを編集」で管理する。月額車の一時利用も別ページを撤去し、配車セルで月額車を選択→通知内容の確認→「確認して配車」の流れへ置換。通知の貸出前の給油・移動連絡は必要な項目だけ選択する。未選択の任意指示は文面・保存データから除外するが、返却時の給油は標準案内として残す。スマホでは受取・返却場所を横並びにし、確定・キャンセルをモーダル下部に固定する。

- 自動検証: Web全86ファイル831テスト、対象プレビュー35件、TypeScript、独立プレビュービルド、差分の空白チェック成功。新しい操作テストでは、未確定配車の破棄・確定後の取消と元車復元、通知項目の必須判定と非選択項目の除去、返却済みの編集禁止、ラベル削除後の戻り先フィルターの整合性を検証。
- PC1440×1000: 共通ナビから一覧内ラベル編集、追加・名称変更・削除・未保存のキャンセル、月額車の選択・通知文面・空の指示エラー・配車・再表示・取消を確認。
- スマホ390×844: 同じ配車導線、入力破棄のキャンセルで内容保持、配車・再表示・返却済みの確認、ドライバー一覧とラベル名称変更を確認。ページ幅／scrollWidthはともに390px、確定・キャンセルは高さ44px。ラベル編集の閉じるボタンの折返しも防止。
- 最終版は再ビルドし、通常のブラウザ幅で配車確認を開いて引き渡す。架空データを使う画面内保存のみで、再読み込みで初期化。当時は日単位・契約者の希望休を必須としていたが、後述の柔軟性対応で実配車による判定へ変更。時間帯の空き判定・実通知・本番適用は未実装。

### 給油の依頼先指定（2026-08-31）

- 同じ月額車の確認モーダルで「貸出前の満タン給油を依頼する」をオン → 「依頼する相手」と「依頼する内容」を指定。追加確認に合わせ、初期値を月額契約者と「貸出前に満タンにして引き渡してください。」へ変更。利用者・他の登録ドライバーも候補に残す。CustomSelect・CheckboxField・SmoothCollapseと既存のモーダルを再利用する。
- 「通知文を見る」では、配車通知と給油依頼を宛先別に表示。同じ人なら一通にまとめる。別の担当者には車両・日付・利用者・依頼内容を表示し、利用者向けの受け渡し連絡は混ぜない。文面の確認のみで、外部送信は行わない。
- 保存して同じプレートから再度開き、依頼先・内容の保持と変更を試せる。貸出前の依頼をオフにして保存すると任意の内容と宛先を削除するが、利用者の「使用分を給油し、満タンで返却」は常に案内する。取消済みの利用からは通知文を生成しない。未保存の変更は既存の破棄確認で保護する。
- 試す例: 9/7の小林の車両に1201 → 貸出前の給油依頼をオン → 初期依頼先が佐藤、指示が満タンでの引き渡しと確認 → 小林宛ては返却時給油、佐藤宛ては貸出前給油と分かれることを確認 → 配車を確定して再表示。スマホでも相手を選択し、貸出前の依頼なしへ変更して保存できる。

- 検証: Web全86ファイル835テスト（対象39件）、TypeScript、独立ビルド、差分の空白チェック成功。PC1440px／スマホ390pxで依頼先選択・内容入力・空欄エラー・保存後の保持・選択解除と宛先別文面を確認。スマホの依頼先は高さ56px、保存は44px、横はみ出し・ブラウザ警告・エラーなし。通常幅で給油欄を開いて引き渡す。

### 駐車場所の事前登録・選択（2026-08-31）

- 入口: 事前に「プレビュー操作」→「駐車場所」。配車の確認モーダルからは「駐車場所を編集」。独立ページを増やさず、同じ登録一覧を編集する。
- 再利用元: `DriverBoard` のラベル編集と実ドライバー画面由来の `EditorModal` を土台に `ParkingPlacesEditor` を追加。受取・返却の選択は既存の `CustomSelect`、削除と未保存保護は `ConfirmDialog`。共通部品や本番画面は変更しない。
- 架空の豊中・吹田・新大阪車庫を初期登録。名称と任意の住所・目印を編集できる。空白・重複名はエラー。配車・利用記録の場所はIDで参照し、名称変更を選択欄と通知へ反映。履歴の地点名スナップショットは未実装で、過去分の表示も現在の名前になる。
- 配車入力中に場所を追加して戻っても、返却時刻・選択先・給油依頼先などの下書きは残る。場所の編集だけの破棄確認と、配車全体の破棄確認を分ける。使用記録（取消済みを含む）や入力中の選択に使う場所は削除できない。
- 未登録を試す: 「駐車場所が未設定」→ 1201を選択 → 確定時の案内を確認 → 場所を登録して閉じる → 受取・返却を選択 → 配車を確定し再表示。新規登録した候補は勝手に選択せず、利用者が指定する。
- 検証: Web全86ファイル843テスト（対象47件）、TypeScript、独立ビルド成功。PC1440×1000／スマホ390×844で登録・名称変更・未使用の削除・使用中の削除禁止・重複エラー・下書き保持・選択と通知・保存後の保持・未登録からの復帰を確認。ページ幅とscrollWidthは390px、場所選択は56px、保存ボタン44px。ブラウザ警告・エラーなし。
- 制限: 画面内保存のみで再読み込み時に初期化。集合場所とは別の登録候補であり、現在の駐車位置やGPS・本番の地点テーブルには接続しない。通常モードは外部送信なし。地図・住所検索は次節のMapboxモードで追加した。

### Mapboxによる住所検索・ピン指定（2026-08-31）

- ユーザーの追加依頼により、`--mapbox` 起動時だけ実際のMapboxへ接続する。環境の `NEXT_PUBLIC_MAPBOX_TOKEN`、なければ `apps/web/.env.local` の同名1項目から公開キー（`pk.`）だけを取得。ほかの環境変数はロードせず、秘密キー・本番API・DB・認証は使わない。公開キーをログやドキュメントへ書き出さない。通常ビルドにはキーを含めない。
- 入口は同じ「駐車場所を編集」。名称→住所を入力→「住所を検索」→候補を選択→地図のクリック／ピンのドラッグで微調整→固定フッターで保存。地図を開く前は地図通信せず、入力中の自動検索もしない。住所がなくても地図から手動で指定でき、旧来の名前だけの場所も維持する。
- 再利用元は本番 `admin/(ops)/map/page.tsx` のMapbox初期化、日本語表示、AttributionControl、NavigationControl、検索結果選択、クリック配置とMarkerのdragend。駐車区画の指定用に2DのStreets v12を使い、Font Awesomeのピンを追加。地図ロゴ・帰属表示は維持。本番の施設検索や保存APIは変更しない。
- 検索は [Mapbox Geocoding v6](https://docs.mapbox.com/api/search/geocoding/) のforwardを `country=jp`、`language=ja`、`autocomplete=false`、`permanent=true` で使用。保存用検索にはMapboxアカウントの利用条件と料金がある。Search Boxの一時利用結果をマスターへ転用しない。API応答は住所・緯度経度だけ取り込み、Cookieを送らずキャッシュしない。
- 住所・緯度経度と目印を分けて保持。名前だけを変えても位置は維持する。住所を入力し直すと古いピンを解除し、候補選択または地図指定前の保存を拒否。ピン移動では住所を自動で変えず、逆ジオコーディングは行わない。検索中の入力変更・編集終了はリクエストを中断し、古い応答を表示しない。0件・通信失敗・上限・認証／利用条件・タイムアウトを案内する。
- MapboxモードのCSPは地図スタイル・タイル・フォント・map-sessions・events・Geocoding forwardの既知の送信先に限定。ローカル本番API・任意ドメインは許可しない。画面にも「Mapbox接続あり・通知送信なし」と住所・表示範囲の送信を明示する。
- 検証: Web全88ファイル851テスト（対象55件）、TypeScript、両モードの独立ビルド成功。PC1440×1000／スマホ390×844で公開施設住所を検索し、候補・実地図・ピンドラッグ・クリック配置・保存再表示・住所変更時の位置解除を確認。スマホのページ幅／scrollWidthは390px、固定保存ボタン44px。地図モードのCSPと、`/api/admin/map/places`・`/.env.local` の404、通常ビルドに公開キーが入らないことも確認。
- 制限: 名前・住所・位置の保存先は画面内のみ。再読込で初期化。本番の駐車場所登録・車両の現在位置・GPS・逆ジオコーディング・ルート・通知の送信は未実装。施設名／POI検索は今回追加していない。日単位の貸出判定や料金の制約も変更しない。

### 貸出前・返却時の給油と確認ボタン（2026-08-31）

- 貸出前の依頼は月額契約者へ満タンで引き渡すよう依頼するもの。新規配車では任意の依頼は初期オフで、有効にすると月額契約者・満タンの指示を初期表示する。9/2の初期サンプルも佐藤への満タン依頼に変更。返却時は日額利用者が使用分を給油し、満タンで返却する標準案内として分離した。
- 確認ボタンは「破棄する」「削除する」「初期化する」「切り替える」「解除する」「希望休にする」「一時利用を取り消す」「返却済みにする」。共通ConfirmDialogの呼び出し時に必ず操作名を渡し、プレビューの一律「実行する」を撤去。本番の共通部品・他画面は変更しない。
- 確認手順: 配車の入力を変更→キャンセル→「変更を破棄しますか？」でキャンセルすると入力を保持、破棄すると確定前の配車は残らない。登録場所の未保存保護でも「破棄する」、未使用の場所の削除確認では「削除する」を表示する。返却・取消の記録更新もそれぞれの操作名で確認する。
- 検証: Web全88ファイル852テスト（対象56件）、TypeScript、Mapboxモードの独立ビルド成功。PC1440×1000／スマホ390×844で満タン給油・月額契約者の初期値、宛先別の文面、保存・再表示・給油依頼解除、破棄確認の取消で入力保持／破棄で元の配車保持を確認。駐車場所の削除・未保存破棄、返却確認の文言も確認。スマホのページ幅／scrollWidthは390px、依頼先56px、保存・キャンセル44px。ブラウザ警告・エラーなし。通常幅で給油欄を開いて引き渡す。
- 残タスク: 返却する人が実際の返却場所を選択し、その記録から地図UIで車両を管理する。現在の予定返却先・場所マスターの地図とは別機能で、実際の返却記録と車両管理地図は未実装。詳細は `docs/design/driver-leases-and-labels.md` と `docs/parking-location-flow.md`。

### 貸出側の駐車案内・柔軟な車両紐付け（2026-08-31）

- 月額車の確認に「貸し出す人」と「受取時刻」を追加。貸出側には受取時刻までに受取場所へ駐車するよう常に案内する。借り手の受取先と同じ登録場所・時刻から文面を生成し、給油をオフにしても駐車案内を残す。同じ人への給油は同じ文面へまとめ、別の給油担当なら3人に分ける。通知の実送信は行わない。
- 借り手は月額・日額の両方を許可。貸出側の希望休を必須とせず、実配車の重複、使用不可、借り手の稼働、受取・返却の場所と時刻、受け渡し確認を検証する。月額利用者へ通常日額は表示せず、代車の追加料金・精算は未確定と表示する。
- ドライバー編集の料金と車両を分離し、「普段使う車両」「紐付けの変更日」「保存済みの紐付け履歴」を追加。既存 `DriverBoard` と共通DatePicker・CustomSelect・SmoothCollapseを再利用した `VehicleLinksField` が土台。紐付けなし・同じ車両への複数人の紐付け・整備中の車両の紐付けも可能。紐付けは予約ではなく、実際の配車はシフトで別に変更する。
- 変更日前の紐付け、次の変更予定、配車済みのシフトは維持。保存済みの貸出側・利用者の契約区分を保持し、車両の紐付け変更から貸出通知先を再推定しない。1台に複数人が紐付く場合は、貸し出す人を明示選択する。
- サンプル「月額同士の貸し借り」: 9/7の田中に1201を選択→受取場所・時刻を変更→通知を確認して配車。田中の普段の車両2345と月額契約は維持。
- サンプル「整備中の代車」: 9/8の佐藤の1201を整備中に設定済み。5678へ配車を変更し、9/9の配車と普段の紐付けが変わらないことを確認。別の使用中の車両や使用不可車両は選べない。
- サンプル「月途中の紐付け変更」: 9/16から佐藤の普段の車両を6789へ変更→保存→再表示→紐付け履歴を開く。日付を変えるとその日の保存済み紐付けを読み込み、保存してシフトを確認する日付も合わせる。
- 検証: Web全88ファイル862テスト（対象66件）、TypeScript、独立ビルド・空白チェック成功。PC1440×1000／スマホ390×844で月額同士の配車、貸出側の駐車案内、受取場所・時刻の即時反映、給油なし／あり、保存再表示、時刻エラー、整備中の代車、紐付け変更の保存・履歴・変更日前の読み込みを確認。スマホのページ幅／scrollWidthは390px、貸出側・紐付け日・車両選択56px、保存44px。ブラウザ警告・エラーなし。日付欄と車両欄の高さを揃え、時刻欄はinputイベントでも即時反映する。
- 制限: 1日単位・同日内の受取と返却、架空データ・画面内保存のみ。料金契約の期間履歴・月途中精算・時間帯別の複数貸出・整備記録の管理・返却時の実際の駐車記録・車両管理地図・本番通知は未実装。今回も本番画面・API・DBを変更しない。

### スマホの絞り込み・日付・画像保存（2026-08-31）

- 再利用元: 既存プレビューのShiftFilters・ShiftBoard（実シフト画面の複製）とSmoothCollapse・DatePicker・CheckboxField・EditorModal。画像は実画面 `admin/(ops)/shifts/page.tsx` の日別配車リストを基に、同じ段分け・氏名／ラベル・コース札・車両の構造でCanvas描画する。`plateImage.ts` は実際のVehiclePlateを日別セルと同じ100px幅で一時描画し、文字・枠・影の計算済みCSSを取得して、同じ公開SVG素材をCanvasへ写す。画像専用の座標・文字間隔は持たない。計測用DOMは即時破棄する。
- スマホで「ラベル」を開く→複数選択→閉じる。閉じても条件を保持し、名前と追加件数を表示する。「すべて」で解除。ラベルは高さ32px、PCは36pxで、長い名前も折り返す。スマホの前半／後半は非表示、日付カレンダー・前日／翌日で15/16日や月末を越える。PCの半月表示は維持。
- スマホの日付直下に「全員／稼働／未割当」の人数付きタブ（初期は稼働）。「画像保存」→日付選択（サンプル期間のみ）→必要なら同じ3タブで対象を変更。現在のタブ・ラベル・契約・名前と表示項目・段分けを引き継ぐ。未割当はコース割当なしで、希望休を含む。PCの半月表はタブで絞らず、画像保存は全員から開始。PNGを表示してから「共有・写真に保存」または「画像をダウンロード」。画像の長押し保存も端末が対応していれば利用できる。画像に架空データの注記を入れる。
- SVGグリフは同梱アセットをImageで読み込み、着色してCanvasに描く。CSS maskやhtml2canvasのテキスト代替を使わない。最大10人ずつ分割し、約400万画素を上限に解像度を調整する。黒プレートの架空データを対象とし、未収録のかな等は画面と同様にフォントへフォールバックする。 18:50の調整で初期モック8台のかなをSVG素材がある「り」「れ」へ変更し、表示画面・通知文・PNGで統一した。初期モックのかなにはフォントへのフォールバックを使わない。
- [Web Share API](https://developer.mozilla.org/en-US/docs/Web/API/Navigator/share)は端末側の共有UIを開く方式。生成済みのFileをcanShareで確認し、ユーザーのクリックからshareを直接実行する。共有先の自動指定・自動送信はしない。キャンセルや利用拒否時も画像を残す。OSの写真ライブラリに無操作で追加する機能ではない。ローカルホスト以外では安全なコンテキストと対応ブラウザが必要。
- 検証: Web92ファイル880テスト（対象84件）、最終版の対象84件の再実行、型・独立ビルド成功。PC1440×1000／スマホ390×844・320×740で実操作、ラベル32pxと横はみ出しなし、15/16日・9/30と10/1の移動、画像の稼働者絞り込み・日付変更・ダウンロード操作を確認。PNG本体（1170×3168px）でSVGプレート・全9人の内容を目視。共有の成功／保存完了は実機未確認で、取消・非対応・拒否、再生成・古い生成結果の破棄、複数画像を自動テストした。
- 制限: 本番シフトの既存エクスポート処理は未変更。iPhone／Android実機の「画像を保存」と保存先の確認が残る。半月表/PDFと期間一括出力は対象外。通知・DB・本番API・認証に変更なし。Mapboxモードは維持し、今回の画面確認で地図通信を使う操作はしていない。

### PCの表示期間をまとめる（2026-08-31）

- ShiftBoardのヘッダーで、右上に離れていた既存MonthYearPickerを前後半タブの隣へ移動。「前の半月 → 年月 → 前半／後半 → 次の半月」を同じ行・高さ36pxの操作グループにした。選択中の年月と前後半を一続きで確認できる。
- 狭いPC幅でも期間のグループは分割せず、表示・画像保存・更新・設定を次の行へ回す。スマホではこのグループを隠し、従来の日付カレンダー・前日／翌日を維持する。年月・前後半の状態や移動処理、本番画面は変更しない。

## 契約保存失敗の修繕プレビュー（2026-08-31）

- 起動は既存の `npm run preview:admin -- driver-leases --port 3191`（許可済みのMapboxを使う場合は `--mapbox`）。「プレビュー操作」→「試すサンプル」→「契約保存の失敗と再試行」を選ぶ。
- 既存のドライバー編集プレビューを再利用し、本番ドライバー編集と同じ `SaveFailureNotice`・`saveDriverSections` を使う。見た目だけの別画面にはせず、従来のラベル／契約欄で操作する。
- 月額料金とラベルを変えて「ドライバー情報を保存」。ラベルは保存され、契約は一度だけ失敗する。入力と編集画面、エラーが残ることを確認する。「未保存の項目を再試行」で契約を保存し、再度開いて確認する。
- PC1440px／スマホ390pxで失敗・入力保持・再試行・再表示を確認。スマホのページ幅390px・ダイアログ幅358pxで横はみ出しなし。ブラウザの警告・エラーなし。失敗直後のプレビューを開いて引き渡す。
- プレビューは画面内保存のみでDB・本番APIに接続しない。本番ページの自動保存・新規登録後の再試行は別途コンポーネントテスト、APIとSQLは隔離テストで確認。[修繕の適用手順](lease-vehicle-repair-rollout.md)を参照。プレビューの成功だけで本番DBの適用確認を代用しない。

## 車両をナンバープレートから選ぶ（2026-08-31）

- 再利用元は実シフト画面 `shifts/page.tsx` の `VehicleOptionList` と共通 `VehiclePlate`。車両紐付けの `Choice` を共通 `VehiclePlatePicker` へ置換し、選択中・候補とも実物と同じSVGグリフで描画する。配車側の既存プレート選択は維持する。
- シフト表のドライバー名→契約設定→「普段使う車両」を開く。PCは3列・スマホは2列のプレートを選び、保存後の再表示を確認。番号（全角・ハイフン入りも可）・車種で検索し、0件から検索クリアで戻れる。
- 「紐付けなし」は専用ボタン。整備中は注意を表示し、従来どおり普段の紐付けの選択は可能。配車の使用不可判定や既存の配車・料金は変更しない。Escapeは候補だけを閉じ、編集モーダルと入力を保持する。
- PC1440×1000／スマホ390×844・320×740で表示・選択・検索・紐付け解除・保存再表示・操作領域を確認。ローカルプレビューのみへ接続し、通知・DBへの書き込みは行わない。このUI差分は追加migration不要で、前回の保存修繕に必要な155は別途未適用。

## 継続的な指示の置き場所

このリポジトリの継続ルールは `AGENTS.md` に置く。[OpenAIの公式説明](https://learn.chatgpt.com/docs/agent-configuration/agents-md)に沿ったプロジェクト単位の指示であり、定期実行タスクではない。

### プレート画像と日別タブの確認

- スマホ幅で「稼働」→画像保存を開き、PNG本体と一覧の「り」「れ」、数字の字間・配置・枠・影を比較する。PNG内のプレート本体は100×50px相当、SVGは高解像度で描画する。
- 「未割当」から画像保存に進み、希望休を含む同じドライバー・人数・選択タブ・保存名になっていることを確認。画像側でも全員／稼働へ切り替えられる。
- 検索で「佐藤」に絞り、9/1と9/2を移動して人数と対象が更新されること、0件でもタブを戻せることを確認。PC幅では希望休を含む全員の半月表を保ち、年月・前後半が隣接したままであることを確認する。

- 今回の検証: Web92ファイル883テスト（対象87件、maxWorkers=2）・型・独立ビルド成功。PC1440／768pxとスマホ390／320pxで日別タブ・検索・日付・画像への引継ぎと横はみ出しなしを確認。1170×2751pxのPNG本体を実画面と比較し、かな・数字の配置と影を確認。本番は未変更、実機の写真保存は未確認。

### シフトだけの表示をコンパクトにする（2026-08-31）

- 既存ShiftBoardの名前列・日別リスト・コース軸を土台に、表示メニューへ「ラベル・契約」を追加。初期状態では車両の表示に合わせて補足情報を出し、車両を隠せば名前だけになる。手動で補足表示を変更した後は、車両表示を切り替えてもその選択を維持する。
- 「ラベル・契約」の表示と、上部のラベル／契約による絞り込み・契約区分での段分けは独立。表示を隠してもドライバーのラベルや契約、シフト・配車の保存内容を変更しない。段分けを外しても契約バッジが名前の下へ残らない。
- 車両と補足情報を隠すと名前・セルの縦余白も縮小。1コースのPC行は46px（従来はラベル付き88px、契約区分も別行だと118px）、スマホは53px。名前と予定セルは44px以上の操作領域を保つ。複数コースや集合時刻は必要な分だけ広がる。
- 日別PNGも同じ補足表示の判定を使い、非表示時のラベル・契約行と空白を取り除く。フィルター条件の説明と「全員／稼働／未割当」は維持する。
- 確認手順: 「表示」で車両をオフ→契約区分での段分けをオフ→名前1行と低い行高を確認。「ラベル・契約」で再表示し、ラベル絞り込み・対象人数・予定が変わらないことを確認。複数コースの追加／解除、スマホの日別表示と画像保存も試す。本番画面・API・DB・通知の変更はない。
- 検証: Web885テスト（対象89件）・型・独立ビルド成功。PC1440pxで行高46px／2コース62px、スマホ390／320pxで53px、名前とセルの操作領域44pxを確認。1170×1353pxのPNG本体で補足の行と空白の省略を確認。編集・再表示・期間変更・日別タブは維持、横はみ出し・ブラウザ警告・エラーなし。

### 表示パネルの開閉負荷を分離（2026-08-31）

- ShiftBoardの開閉状態を共通のShiftDisplayPanelへ移し、「表示」の開閉だけでは表全体を再描画しないようにした。SmoothCollapseの250ms・共通easingを使い、表示項目の値は画面側で保持する。共通のアニメーション定数は変更していない。
- PC1440px／スマホ390px・320pxで、繰り返し開閉・ラベルのアコーディオン・設定保持・横はみ出しなしを確認。Web93ファイル886テスト、型、独立ビルド成功。再描画の分離と設定保持の回帰テストを追加。
- 本番反映に向けてコード・SQLの影響調査を実施。会社境界、契約の部分保存、社外貸出との区別、時間帯別利用、日報・費用回収・通知・所在を確認し、上記の調査文書へ記録した。本番画面・API・DB・通知はまだ変更していない。
## 本番シフト画面の表示確認（2026-08-31追記）

`npm run preview:admin -- shifts --port 3191` で `/preview/shifts` を開く。これは本番 `admin/(ops)/shifts/page.tsx` 自体を使い、standaloneビルド時に依存を架空データへ差し替えた検証画面。通常のNext公開ルートを追加せず、DB・API・通知・外部通信を遮断する。年月／前後半・表示項目・スマホの3タブ・PNG保存を確認できる。起動・制限・本番との差分は [本番シフトUIの移植](shifts-ui-rollout.md) を参照。

### 契約区分の表示確認（2026-08-31 23:18追記）

- 今回の起動先は `npm run preview:admin -- shifts --port 3193` → `http://127.0.0.1:3193/preview/shifts`。本番ページを引き続き直接使用。契約選択・段分けは既存 `preview/driver-leases/ShiftFilters.tsx` の契約欄と `ShiftBoard.tsx` の区切りを土台に `ShiftLeaseFilters` へ切り出し、共通CustomSelect・CheckboxField・表示パネルを使う。
- 契約データは架空の12人分。8月は月額3・日額5・なし4、9月は佐藤の契約が切り替わり各4人。「月額リース」で絞る→スマホで8/31と9/1を移動→全員の人数3→4を確認。稼働／未割当の内訳は日によって変わる。
- 「契約区分でまとめる」を外すと名簿順と名前の補足バッジ。「表示」の「契約区分」で補足だけ隠せる。車両と補足を隠すと既存の46pxのPC行高を保つ。
- 「契約が全員未設定」→リースなし12人。「月額」で0人に絞っても「すべての契約」で戻れる。「契約の取得失敗」→エラー・シフト維持・区分選択の無効化→「再読み込み」で正常復帰する。
- 「佐藤の契約を日額へ変更」はDB保存をせず、保存済みの契約変更をメモリ上で模す。9月の月額4→3・日額4→5、既存シフト・配車が変わらないことを確認。「サンプルを初期化」で戻る。
- PC1440px／スマホ390px・320pxで表示・操作を確認。PC半月初日／スマホ選択日が区分の判定日。日別PNGにフィルターと段分けを反映し、分割画像の見出しでは全体の区分人数を重ねて表示しない。コース軸は全員、半月PNG/PDFは既存仕様。契約金額・DB・本番API・通知には接続しない。

### スマホのエクスポートアイコン（2026-08-31 23:54）

同じ `/preview/shifts`（`npm run preview:admin -- shifts --port 3193`）で確認する。本番ページを直接再利用し、スマホではダウンロードの1アイコンだけを表示。Font Awesomeのdisplay指定との競合を避け、矢印を包むspanに `hidden md:inline-flex` を付ける。PCの「エクスポート＋矢印」と日別／半月PNG／PDFメニューは維持する。スマホ430px／320pxで1アイコンと画像保存画面への遷移、PC1440pxでメニュー表示を実操作確認。DB・本番API・通知への接続や出力処理の変更はない。

### 日別PNGの文字位置（2026-09-01 00:00）

- 同じ起動方法と本番ページを再利用。「日別配車の画像」で氏名・コース札の下側が欠けないことを確認する。html2canvas 1.4の文字計測用1px画像に限り、生成中だけTailwindのblock指定をinline-blockへ戻す。補正は元documentで必要なためoncloneには置かず、成功・失敗とも撤去する。通常一覧・プレート・行高は維持。
- スマホ430pxで段分け・車両あり・未割当、320pxで車両なし・契約バッジ・集合時刻・全員の2枚分割、PC1440pxで日別画像を再生成して文字位置を確認。生成・ダイアログ・ページの11テストと型チェック成功。架空データ・本番API／DB／通知からの隔離を継続。半月PNG/PDFと共有方法は変更せず、写真ライブラリ保存は実機未確認。

### 日別PNGの1枚優先と行順（2026-09-01 17:27）

- `npm run preview:admin -- shifts --port 3194` で本番シフトページを再利用する。表示パネルの「行の並び順」からドラッグ、契約区分、選択日のコースで並べ替え、保存後に半月表・日別一覧・日別PNGが同じ順になることを確認する。プレビューの保存はメモリ内だけで、本番DB・通知へ接続しない。
- 12人までは1枚、13人以上は最低限の枚数で、契約区分／コース境界と1人だけの次ページ回避を確認する。画像見出しは中央の `YYYY/MM/DD（曜）` のみとし、「日別配車」や端末内生成・写真への保存方法の補足文は表示しない。
- PC1440×1000とスマホ390×844で、稼働11人と全員12人が各1枚、画像1224×2763px／1224×2895px、スマホのページ幅とscrollWidthが390pxであることを確認する。13人以上の境界は改ページの単体テストで補う。実機の共有・写真保存は未確認。

### PDFの容量（2026-09-01）

- 引き続き `npm run preview:admin -- shifts --port 3193` → `http://127.0.0.1:3193/preview/shifts` を使う。「シフトメモ」は本番の `PersonalShiftMemoBoard.tsx` を直接使用し、独自の帳票画面は作らない。
- 「メモ12枠のサンプル」→「シフトメモ」→「エクスポート」→「表全体を選択」。保存ボタンが容量を付けず「PDF」と表示されることを確認する。サンプルは8月・9月の架空データで、プレビュー専用ユーザーのローカル保存領域だけを置き換える。既存シフト・契約サンプルと本番API／DB／認証には触れない。
- 「メモ40枠のサンプル」でも保存ボタンは「PDF」のみで、範囲を絞る案内が出ないことを確認する。容量表示・案内はユーザー指定で撤去。数百KBは通常出力の目安で、大きな表を低画質化して強制的に収める処理ではない。容量は生成したファイルで検証する。
- PC・スマホ幅で保存ボタンと横はみ出しがないことを確認する。圧縮導入時には実際のPDFを描画し、12枠・40枠は元PNGとの全画素一致、12人分のシフト表も圧縮前後の描画一致を検証済み。旧 `/preview/shift-memo` の複製実装も同じ圧縮関数を使用するが、実操作確認は本番ページを使う3193番で行う。
- 圧縮設定・サイズ実測・検証基準は [共通方針](image-pdf-export.md) に集約。PDF/PNGのダウンロード開始までを確認し、スマホ実機の保存先・共有先は未確認。

### オイル交換マークの補足（2026-09-01）

- 起動方法と入口は同じ3193番のシフトプレビュー。本番 `shifts/page.tsx` と共通 `VehiclePlate.tsx` をそのまま使い、架空の佐藤・車両1201の既存オイル交換警告で確認する。「表示」→「車両」をオンにする。本番API／DB／通知への接続はない。
- PCでは佐藤の割当セルのコース名にマウスを乗せても補足が出ず、赤いオイルマークに乗せたときだけ「オイル交換が必要です」が全文表示される。左の氏名固定列に隠れないこと、マークのクリックで従来の配車編集が開くことを確認する。接近時の表示は「もうすぐオイル交換です」で、判定基準は変更しない。
- PC1440px、スマホ390／320pxで前面表示・横はみ出しなし・画面下端で上向きへの切替を確認。離脱、Escape、マウス表示中のスクロールで閉じ、キーボードフォーカスの自動スクロール後も補足を読めることを確認した。タッチでホバー表示しないことは自動テストで確認し、スマホ実機のタッチ操作は未確認。
- `Tooltip.tsx` を共通化し、固定列やスクロール枠の外に補足を描画。PNGに補足を含めず、警告マーク・プレート本体・既存の配車／保存処理は維持する。関連3ファイル14テスト、型チェック、独立ビルドに成功。画像出力の実ファイルは今回再生成していない。

### 車両移動・受け渡しと時刻指定通知（2026-09-01）

- 起動: `npm run preview:admin -- shifts --port 3193` → `http://127.0.0.1:3193/preview/shifts` → 上部「車両移動を試す」。本番シフトページは直接再利用し、独立アダプターに入口と架空データを追加した。新しい本番公開ルートやAPIは作らない。
- 車両1201について9/3豊中→9/4京都、9/4京都→9/5豊中、9/6→9/7豊中での受け渡しを用意。元の `MonthlyVehicleDialog` の通知確認を土台に、共通EditorModal・車両プレート・選択・日時・チェック・開閉・確認部品を使用する。
- 「移動を手配」→運ぶ人→期限→「次の利用日の前日」または「日時を指定」で通知予約。同じ場所なら「受け渡しを手配」。給油は担当者を選べ、宛先別の文面を開いて確認できる。18:00はサンプル初期値で会社共通設定は未実装。
- 未設定で保存→エラー。「保存失敗を試す」→編集→保存失敗→入力維持→再保存。「通知失敗を試す」→「通知時刻まで進める」→失敗→「通知を再予約」で復帰。手配を取り消すと未送信の予約を解除する。連絡済みなら取消通知も予約する。
- 「完了を記録」で実際の場所・日時を記録するまで最後の到着場所は変わらない。未完了／完了／すべてで絞り込める。プレビューでは未来のサンプルを試すため時計を進め、本番の実績入力とは区別する。
- PC1440px／スマホ390px・320pxで入力、19:30への時刻変更、給油・宛先別文面、保存・再表示、未保存保護、失敗復帰、取消、完了を確認。320pxでページとダイアログに横はみ出しなし。状態遷移7件・本番シフトページ4件のテスト、型チェック、独立ビルド成功。
- 保存は画面内だけで、パネルの閉じ直しでは保持、再読込／サンプル初期化では消える。本番DB・API・通知・地図への接続なし。固定サンプル以外の配車変更からの自動追従、プレート選択直後の確認、会社共通の通知設定、同日複数便は未対応。[本番への接続条件](../design/vehicle-handoffs-and-notifications.md) に残課題を記録した。

### 車の場所・返却のUI計画と画像（2026-09-01）

ユーザーの依頼に合わせ、今回は [UI計画とPC／スマホ画像](../design/shift-vehicle-location-ui.md) を作成した。既存の3193番をPC1440×1000・スマホ390×844で開き、現在のシフト画面を画像生成の参照にした。新しい車両軸・シフト内の返却先・駐車記録は**静止画の案のみで、このURLの操作プレビューには未追加**。次の実装では同じ本番ページの再利用を維持し、架空データで操作・未設定・失敗・PC／スマホ幅を検証する。本番・DB・通知への接続は行っていない。

日報と同時に駐車場所を送るという追加方針は [共通基盤の計画](../design/daily-report-parking-foundation.md) に記録した。次回の入力側プレビューは実際の `/submit`（`SubmitPageClientV2`）を再利用し、シフト側も同じ架空の駐車履歴を見る形にする。今回は調査・設計更新のみで、新しい日報プレビューや駐車送信機能はまだ作成していない。

### 表示パネルへ契約操作をまとめる（2026-09-01 12:53）

- 本番シフトページを直接使うプレビューで、「表示」の開閉を共通250msからこのパネル用の140msへ短縮。契約区分の絞り込み・段分け・判定日を、独立した白い欄から表示項目と同じパネルの2段目へ移した。
- 上下余白と段間を詰め、契約の選択・段分けはPCで高さ36px、スマホは操作領域44pxを維持する。表示項目・軸・契約の状態、表・日別タブ・画像への反映は変更しない。
- ラベルでドライバーを絞る操作は旧 `/preview/driver-leases` の設計モックにだけあり、本番シフト・`/preview/shifts` へは未移植。会社別ラベルの保存・管理・認可を含む段階3のため、今回の微修正では架空のラベルだけを本番ページへ足していない。
- `npm run preview:admin -- shifts --port 3194` で独立ビルドし、Web全103ファイル968テストとTypeScriptに成功。PC1440×1000でパネル115px・契約操作36px・段間8px、スマホ390×844でパネル181px・契約操作44px・ページ幅／scrollWidth 390pxを確認。本番DB・API・通知への接続はない。

### 車両一覧の登録順と新規追加エラー（2026-09-01 17:55）

- 起動: `npm run preview:admin -- vehicles --port 3195` → `http://127.0.0.1:3195/preview/admin/vehicles`（2026-09-06 にシナリオ runner へ統合。旧 `/preview/vehicles` は一覧に戻る）。本番の `admin/(resource)/vehicles/page.tsx` を直接使い、認証・API・DB・3Dモデルだけを架空データへ差し替える。通常のNext公開ルートは追加しない。
- IDの大小と登録日時を逆転させた同一車種を用意し、「稼働中 → 使用不可 → 廃車」の各状態内で登録日時の古い順になることを確認する。画面にも表示順を明記する。
- 「新規追加」を空欄で保存すると、車種の必須表示・入力一覧・赤枠・フォーカスが同じモーダル内に出る。メーター欄は任意で、空欄のまま保存すると0km・交換間隔3,000kmになる。
- 「次の保存を失敗させる」でサーバー失敗を模し、短い案内だけが表示されること、選択した車種と他の入力が残ることを確認する。「サンプルを初期化」で架空データを戻せる。
- PC1440×1000とスマホ390×844で一覧・入力・保存・失敗後の入力保持を確認する。本番API・DBへの接続と実際の3Dモデル読込は行わない。
